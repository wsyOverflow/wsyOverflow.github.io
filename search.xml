<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Vulkan Render Design</title>
    <url>/Render-Engine/Vulkan-Render-Design.html</url>
    <content><![CDATA[<h1 id="summary">Summary</h1>
<p>本文介绍了本渲染器的 Vulkan 管线管理策略以及 Shader 管理策略。</p>
<h1 id="vulkan-管线管理策略">Vulkan 管线管理策略</h1>
<p>Vulkan 包含三种管线
graphics、compute、raytracing，管线只是一种状态描述，描述了整个工作流中涉及的状态。人们经常提
OpenGL 是个状态机，OpenGL 的状态是由根据使用者的 API
调用来进行切换的，属于 high-level 的 API 设计。而Vulkan 使用
<strong>PSO(pipeline state object)</strong> 来描述管线状态，类型为
<code>VkPipeline</code>，这个类型的对象需要用户自己创建。</p>
<p>PSO 只是描述管线的各种状态，状态相同的管线可以使用同一个
PSO，不相容的状态则需要创建新的 PSO。因此对于 PSO
的管理采用将当前状态进行 hash 处理，如果存在当前状态 hash 的 PSO
则直接使用，否则创建新的 PSO 并记录到 unordered_map 中。这部分工作由类
<code>VulkanPipelineStateManager</code> 完成，目前的 hash
算法只是简单的移位与异或，留待之后更改。</p>
<p>创建 PSO 的 API 有三个，<a href="https://www.khronos.org/registry/vulkan/specs/1.3-extensions/man/html/vkCreateGraphicsPipelines.html"><code>vkCreateGraphicsPipelines</code></a>
创建 graphics PSO，<a href="https://www.khronos.org/registry/vulkan/specs/1.3-extensions/man/html/vkCreateComputePipelines.html"><code>vkCreateComputePipelines</code></a>
创建 compute PSO， <a href="https://www.khronos.org/registry/vulkan/specs/1.3-extensions/man/html/vkCreateRayTracingPipelinesKHR.html"><code>vkCreateRayTracingPipelinesKHR</code></a>
创建 raytracing PSO。创建 PSO 需要传入描述管线状态的结构体，管线状态的
hash 也主要对这些结构体中的字段进行。另一个 cache
参数，是允许用户将执行过程中创建的 PSO 保存到文件，然后下次启动加载到
cache 参数中，Vulkan 可以通过 cache
来减少创建过程中需要的底层开销，可以提高运行时帧数的稳定性。</p>
<p>对于 PSO 的封装有类 <code>VulkanPipeline</code> 及其三个子类
<code>VulkanGraphicsPipelineState</code>、<code>VulkanComputePipeline</code>
以及 <code>VulkanRayTracingPipeline</code>。下面以 graphics PSO
为例，介绍管线的创建流程。</p>
<h1 id="graphics-pso-的创建">Graphics PSO 的创建</h1>
<h2 id="vulkan-graphics-pipeline-介绍">1. Vulkan Graphics Pipeline
介绍</h2>
<p>创建 Vulkan Graphics PSO 的描述封装在结构体 <a href="https://www.khronos.org/registry/vulkan/specs/1.3-extensions/man/html/VkGraphicsPipelineCreateInfo.html"><code>VkGraphicsPipelineCreateInfo</code></a>
中，下面简要介绍它的成员：</p>
<ul>
<li><code>VkPipelineShaderStageCreateInfo*</code> ：管线使用的 shader
信息数组，每个元素包含使用的 shader
对象(VkShaderModule)、入口函数、shader 阶段(vertex/fragment
...)等。</li>
<li><code>VkPipelineVertexInputStateCreateInfo*</code>：每个元素描述顶点包含的输入属性(vertex
shader)，即
location、binding、数据格式以及相对于顶点起始位置的偏移量。</li>
<li><code>VkPipelineInputAssemblyStateCreateInfo</code>：描述图元数据的拓扑结构(VkPrimitiveTopology)，如<br>
POINT_LIST、TRIANGLE_LIST 等。</li>
<li><code>VkPipelineTessellationStateCreateInfo</code>：曲面细分阶段的配置参数，例如控制点数量</li>
<li><code>VkPipelineViewportStateCreateInfo</code>：用来描述当前渲染的
viewport 和 scissor 的区域，视口和裁剪可能变化较频繁，可以设置为 dynamic
state，创建管线时不设置，在渲染前使用 command 动态设置。</li>
<li><code>VkPipelineRasterizationStateCreateInfo</code>：描述光栅化阶段的配置参数，几何绘制模式(填充/线框)，正面的顶点环绕方向，剔除面(正面剔除还是背面剔除)。是否开启
depth bias，对 depth buffer
中的深度加上一个偏移量，可以在避免自遮挡时使用</li>
<li><code>VkPipelineMultisampleStateCreateInfo</code>：描述光栅化阶段的采样数，1
表示无超采样。</li>
<li><code>VkPipelineDepthStencilStateCreateInfo</code>：描述是否开启深度测试、模板测试，以及深度测试的操作、模板测试的操作。</li>
<li><code>VkPipelineColorBlendStateCreateInfo</code>：描述 frame buffer
中每个 render target 的 blend 操作，包含 blend 参数、blend
计算方式等。</li>
<li><code>VkPipelineDynamicStateCreateInfo</code>：描述管线状态中哪些是可以使用
command 指定的动态状态，其他则是创建管线时就确定的。</li>
<li><code>VkPipelineLayout</code>：包含管线中着色器的输入资源描述</li>
<li><code>VkRenderPass</code>：管线所使用的 render pass，包含了 render
pass 的 render target 的描述。</li>
</ul>
<h3 id="vulkan-render-pass">1.1 Vulkan Render Pass</h3>
<p>Vulkan Render Pass 也是一种描述，因此同样使用 hash
方式进行自动创建，主要包含三部分：</p>
<ul>
<li>描述其使用到的所有 attachment 的描述
<code>VkAttachmentDescription</code>，一个 attachment 即为一个 image
view，用于访问 vulkan texture。进入 renderpass 之前、renderpass
之内、renderpass 结束时的 image layout；加载与存储时的操作；attachment
格式，采样数。</li>
<li>描述其内部的 subpass，并且至少包含一个 subpass。每个 subpass
描述包含其 color/resolve/depth attachment 的引用描述，即 framebuffer 中
attachment index、image layout</li>
<li>subpass 之间的依赖</li>
</ul>
<p>Vulkan Render Pass 特殊之处在于其是由 subpass
来描述内部多个子阶段的，绘制指令也会被记录在一个当前激活的 subpass
中。subpass 的设计是为了能够高效利用某些 tile-based 架构的移动端 GPU，即
fragment shader 的执行是以 tile 为单位的，因此之后的 subpass
可以直接读到上一个 subpass 写入 attachment 的数据，而不需要等到整个
render pass 执行完。</p>
<p><a href="#%5B1%5D">[1]</a> 以由两个 pass 组成的 deferred shading
为例，第一个 pass 为了绘制 G-Buffer，第二个 pass 基于 G-Buffer
进行光照计算，得到最终绘制结果。可以设置 render pass 包含两个
subpass，第一个 subpass 绘制 G-Buffer，第二个 subpass 将第一个 subpass
的 G-Buffer 作为
<code>input attachment</code>，执行光照计算。流程如下述伪代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">cmdBeginRenderPass</span></span><br><span class="line"><span class="function">	<span class="title">cmdBindPipeline</span><span class="params">(pipeline0)</span></span></span><br><span class="line"><span class="function">	... <span class="comment">// bind vertex/index buffer or descriptor set of shader resource</span></span></span><br><span class="line"><span class="function">	cmdDraw</span></span><br><span class="line"><span class="function">	cmdNextSubpass</span></span><br><span class="line"><span class="function">	<span class="title">cmdBindPipeline</span><span class="params">(pipeline1)</span></span></span><br><span class="line"><span class="function">	... <span class="comment">// bind vertex/index buffer or descriptor set of shader resource</span></span></span><br><span class="line"><span class="function">	cmdDraw</span></span><br><span class="line"><span class="function">cmdEndRenderPass</span></span><br></pre></td></tr></table></figure>
<p>注意两个 subpass 使用的是两个 PSO，因为 geometry pass 的 shader 与
deferred shading 的 shader，以及其 shader 输入肯定不同。在第二个 subpass
使用到的 pipeline1 的 shader 则可以使用如下方式来声明 input
attachment，</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">layout</span> (input_attachment_index = <span class="number">0</span>, set = <span class="number">0</span>, <span class="keyword">binding</span> = <span class="number">0</span>) <span class="keyword">uniform</span> subpassInput inputColor;</span><br><span class="line"><span class="keyword">layout</span> (input_attachment_index = <span class="number">1</span>, set = <span class="number">1</span>, <span class="keyword">binding</span> = <span class="number">1</span>) <span class="keyword">uniform</span> subpassInput inputDepth;</span><br></pre></td></tr></table></figure>
<p><code>subpassLoad(inputColor)</code> 则会读取上个 subpass 在
inputColor 同样区域写入的数据。</p>
<h3 id="vulkan-pipeline-layout">1.2 Vulkan Pipeline Layout</h3>
<p>shader 中的输入参数需要对应描述符 <code>VkDescriptorSetLayout</code>
对其描述，而 vulkan pipeline layout 则包含了整个管线的所有 shader
输入参数的描述符。上述 set=0、set=1 则分别表示了索引 0、1 的
VkDescriptorSetLayout
对象，相当于整个管线的描述符分到了两个描述符堆上。一个输入资源的描述符
<code>VkDescriptorSetLayoutBinding</code>，包含了
binding、资源类型(如上例中的
<code>VK_DESCRIPTOR_TYPE_INPUT_ATTACHMENT</code>)、当为数组时的数组长度以及被哪些
shader 阶段使用。</p>
<p>Vulkan Pipeline Layout 只是对管线 shader
输入的描述，相当于函数签名中形参，真正使用的数据资源，如
sampler、texture、buffer 等，需要使用 command 在 draw call
之前指定。因此 Vulkan Pipeline Layout 同样使用 hash 自动创建。</p>
<p>特别注意，描述符堆是从 descriptorPool 中使用 VkDescriptorSetLayout
创建得到的，每个描述符堆 <code>VkDescriptorSet</code> 同时只能由一个
command buffer 使用，因此描述符堆的申请使用与停止使用应该由 command
buffer 完成。类 <code>VulkanDescriptorPoolManager</code>
维护已经创建的描述符堆以及其使用状态，command buffer
在更新描述符资源之前请求使用，如果没有可用的 descriptorSet
则新创建。command buffer 具有自己执行是否完成的 fence，类
<code>VulkanCommandBufferManager</code> 更新 command buffer 状态时，如果
command buffer 中的指令已经完成，则归还其使用的描述符堆。</p>
<h2 id="从上层到渲染层的管线创建">2. 从上层到渲染层的管线创建</h2>
<p>本渲染器的最初设计是面向多平台的，设置了平台无关层，只是目前只有
Vulkan 的渲染层，因此平台无关层更偏向于
Vulkan。下面介绍与管线创建相关的平台无关层的类。</p>
<ul>
<li><code>GPIRenderPassInfo</code>：对标 vulkan render pass
的描述，但不仅仅包含了对 render pass 的描述，还包含 render pass
中使用到的 render target ，因此 render pass 对应的 frame buffer
的创建所需参数也是从该类型中获取。</li>
<li><code>GPIGfxPipelineStateInitializer</code>：对标 Graphics PSO
的配置参数。</li>
</ul>
<p>抽象类 <code>GraphicsBridge</code>
定义了一系列到渲染层的接口，不同的平台实现其接口。例如，设置 render pass
以及管线的接口 <code>GPIBeginRenderPass</code>
、<code>GPISetGfxPipelineState</code>。下面是管线创建的上层逻辑的伪代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">GPIRenderPassInfo basePassInfo;</span><br><span class="line"><span class="comment">// ... // config basePass info</span></span><br><span class="line">gGfxBridge-&gt;<span class="built_in">GPIBeginRenderPass</span>(basePassInfo);</span><br><span class="line">&#123;</span><br><span class="line">    GPIGfxPipelineStateInitializer PSOInitializer;</span><br><span class="line">    <span class="comment">// ... // config PSOInitializer</span></span><br><span class="line">    gGfxBridge-&gt;<span class="built_in">GPISetGfxPipelineState</span>(PSOInitializer, basePassInfo);</span><br><span class="line">    <span class="comment">// ... // config the resources of descriptor set</span></span><br><span class="line">    gGfxBridge-&gt;<span class="built_in">GPIDrawPrimitive</span>();</span><br><span class="line">&#125;</span><br><span class="line">gGfxBridge-&gt;<span class="built_in">GPIEndRenderPass</span>();</span><br></pre></td></tr></table></figure>
<p><code>GPIBeginRenderPass</code> 会根据参数 basePassInfo 来生成 vulkan
render pass 的配置，以及 frame buffer 的资源参数，通过 hash
的方式查找是否存在对应配置的 render pass 与 frame
buffer，不存在则创建。同理 <code>GPISetGfxPipelineState</code> 根据
PSOInitializer 与 basePassInfo 参数以 hash 方式查找 PSO，并绑定到当前
command buffer。</p>
<p>同时 <code>GPIBeginRenderPass</code>
还做了一些开始管线前的准备工作，比如向
<code>VulkanCommandBufferManager</code> 申请 command buffer，以及将
render pass 需要的 render target 调整到其所指定的输入 layout 等。</p>
<h1 id="shader-的管理与配置">Shader 的管理与配置</h1>
<p>上述管线创建过程中，还需要 shader 的配置。<code>UltraShader</code>
基类定义了 shader 相关描述，如 shader 源码文件路径、shader
编译相关配置、shader 输入参数描述等。不同 shader
继承自此基类，声明自己的参数，其中输入参数会被用于管线创建
<code>VkPipelineLayout</code>
资源描述。而编译相关配置则包括一些全局编译配置，如平台、优化参数等，以及一些宏定义，用以开启或关闭某些代码。</p>
<h3 id="shader-源码编译与加载">1. Shader 源码编译与加载</h3>
<p>本渲染器的 shader 编译与加载由类 <code>ShaderCompilerManager</code>
完成。该类基于 shaderc，但为了在 renderdoc 能够源码级调试 shader，在
debug 时使用 glslangValidator 编译。目前编译触发条件，有两种：</p>
<ul>
<li>加载过程中，shader 文件及其 include
文件的时间戳要比记录文件中的更新。或者 shader 的一些配置发生改变。</li>
<li>运行过程中，shader 的一些配置(如宏定义)发生改变。<a href="#todo">TODO</a> 运行过程中文件的改动主动触发编译</li>
</ul>
<p>Shader 文件的后缀分为作为头文件的 .glsl 以及表示不同阶段的源码文件如
.ver、.frag、.comp 等。编译过程中会记录源码文件以及其 include
文件的时间戳，用以辨别是否有更改。同时将 shader 编译配置进行
hash，作为编译输出文件的文件名一部分，用以辨别当前 shader
编译配置是否已经编译过。这些信息都会记录在 "intermediate/Shaders/
ShaderFileCache.json" 文件中，该文件在启动时加载。</p>
<p>由于 include 文件会有多级 include，因此只有加载了 shader
源码将所有层级的 include 文件进行时间戳比对才能确定是否需要编译此
shader，因此这一模式只能在加载过程中应用。在运行时需要采用主动触发编译，目前主动触发编译只有实现了
shader 的编译配置发生改变，文件发生改变主动触发编译留待之后实现。</p>
<h3 id="shader-输入参数描述">2. Shader 输入参数描述</h3>
<p>模板类 <code>TShaderResourceParameter</code> 可以容纳各种类型的
shader
参数描述，同时可以绑定实际的资源。但这些目前都需要大量的手写代码完成，手动与
shader 代码里的声明对齐，手动绑定资源。更合理的做法应该是，</p>
<ul>
<li>实现 C++ 反射宏，使用宏能够更快速声明 shader 参数描述</li>
<li>shader 编译过程应该利用 shader
反射生成包含输入资源描述的头信息。shader 参数在 C++
中使用相同的名称，使用头信息中的名称进行自动配置参数描述(layout信息等)，并进行验证。</li>
</ul>
<p>这些留待之后实现。</p>
<h1 id="todo">TODO</h1>
<p>由于时间有限，有很多搁置的策略未实现。</p>
<ul>
<li>描述符堆目前未实现同时多个堆策略</li>
<li>shader 编译流程生成 layout 头信息，使用 C++ 反射宏进行绑定</li>
<li>多线程渲染</li>
</ul>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a>
https://www.saschawillems.de/blog/2018/07/19/vulkan-input-attachments-and-sub-passes/</p>
]]></content>
      <categories>
        <category>Render Engine</category>
      </categories>
  </entry>
  <entry>
    <title>Scene Graph and Draw Call Grouping Strategy</title>
    <url>/Render-Engine/Scene-Graph-and-Draw-Call-Grouping-Strategy.html</url>
    <content><![CDATA[<h1 id="summary">Summary</h1>
<p>本文介绍了表示场景的 Scene Graph ，以及场景加载过程。基于 Scene Graph
组织 draw call 的策略。</p>
<h1 id="scene-graph">Scene Graph</h1>
<p>Scene Graph
表示场景中物体的层级结构，是场景物体的组织形式。在本渲染器中，Scene
Graph
主要由表示层级结构的基类<code>UltraNode</code>与组织场景物体的基类<code>SceneObject</code>组成。</p>
<h2 id="场景层级结构">1. 场景层级结构</h2>
<p><code>UltraNode</code>的子类<code>SceneNode</code>构建场景中的树形层级结构中的节点。除了表示层级结构，节点主要功能之一是支持变换，其管理的变换信息（旋转、平移、缩放）有：</p>
<ul>
<li>相对于父节点坐标系的局部变换，对应成员有<code>mRelativePos</code>、<code>mRelativeRot</code>、<code>mRelativeScale</code></li>
<li>相对于根节点坐标系的世界变换，对应成员有<code>mStackToRootPos</code>、<code>mStackToRootRot</code>、<code>mStackToRootScale</code>
以及将旋转、平移、缩放组合一起的变换矩阵<code>mStackToRootTransform</code></li>
</ul>
<p><strong>变换的更新逻辑</strong>：节点提供变换接口，如
translate、rotate、scale
等，这些接口都提供参数来表示此次变换相对于哪个坐标系，最后都会改变局部变换信息。下面以
rotate 为例说明，假设旋转参数 rot，相对坐标系为 relative：</p>
<ul>
<li><p>relative 是局部坐标系：mRelativeRot 是从 local 到 parent
的四元数旋转，因此先在 local space 应用 rot，有 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">mRelativeRot *= rot</span><br></pre></td></tr></table></figure></p></li>
<li><p>relative 是父节点坐标系：因此先应用 mRelativeRot (local 到
parent)，有 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">mRelativeRot = rot * mRelativeRot;</span><br></pre></td></tr></table></figure></p></li>
<li><p>relative 是世界坐标系：因此先应用 local 到 world 的变换，再应用
rot，再 world 到 local 的变换，有</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">mRelativeRot = mRelativeRot * glm::<span class="built_in">inverse</span>(localToWorldRot) * rot * localToWorldRot;</span><br></pre></td></tr></table></figure></li>
</ul>
<p><strong>世界变换</strong>相关成员的更新在其对应 GetXxx
函数中，如果检测到局部变换发生变化或者父节点局部变换发生变化时进行重新计算。</p>
<h2 id="场景物体的组织">2. 场景物体的组织</h2>
<p><code>SceneObject</code> 的子类组织场景中的物体，如
mesh、光源、相机等。<code>SceneObject</code> 需要将物体 attach
到场景的节点中才会被渲染。目前实现的有组织 mesh 数据的
<code>MeshEntity</code>，表示投影相机的 <code>FrustumObject</code>。</p>
<p><strong>MeshEntity</strong>：该类构建一个模型数据，由继承自
<code>RenderableInterface</code> 的 <code>PrimitiveEntity</code>
基本单元组成，表示使用一种材质的子 mesh。在场景加载过程中划分成
PrimitiveEntity，在渲染时通过 SceneManager 收集。</p>
<p><strong>FrustumObject</strong>：相机的父类，一共有 Orthogonal 和
Perspective
两种相机。其包含了投影变换的信息，而相机的旋转、平移、缩放等信息由其
attach 到的场景节点表示，注意相机的 view transform
表示从世界到相机空间的变换，而其 attach 到的节点的世界变换为从 local
到世界的变换，local 即相机空间，因此求逆得到 view
transform。相机实现了两种操作方式，ORBIT 与 FPS，在
<code>CameraMovement</code> 类中：</p>
<ul>
<li><p>ORBIT：以目标为中心，根据鼠标操作进行旋转，旋转限制
lock-y，即左右旋转时永远绕世界坐标系 y 轴，旋转过程如下，
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> dist = <span class="built_in">GetDistToTarget</span>();	<span class="comment">// 相机离目标距离</span></span><br><span class="line"><span class="comment">// 将相机放置目标位置</span></span><br><span class="line">mCamera-&gt;<span class="built_in">GetParentNode</span>()-&gt;<span class="built_in">SetRelativePosition</span>(mTargetCenter + mOffset);</span><br><span class="line"><span class="comment">// 相对于世界坐标系 y 轴旋转</span></span><br><span class="line">mCamera-&gt;<span class="built_in">GetParentNode</span>()-&gt;<span class="built_in">Yaw</span>(mouseMotion.x * rotSpeed.x, TransformSpace::TRANS_WORLD);	<span class="comment">// y</span></span><br><span class="line"><span class="comment">// 相对于局部坐标系 x 轴旋转</span></span><br><span class="line">mCamera-&gt;<span class="built_in">GetParentNode</span>()-&gt;<span class="built_in">Pitch</span>(mouseMotion.y * rotSpeed.y, TransformSpace::TRANS_LOCAL);<span class="comment">// x</span></span><br><span class="line"><span class="comment">// 平移保持与目标的距离</span></span><br><span class="line">mCamera-&gt;<span class="built_in">GetParentNode</span>()-&gt;<span class="built_in">Translate</span>(<span class="built_in">Vector3f</span>(<span class="number">0.0f</span>, <span class="number">0.0f</span>, <span class="number">1.0f</span>) * dist, 		</span><br><span class="line">                           		TransformSpace::TRANS_LOCAL);</span><br></pre></td></tr></table></figure></p></li>
<li><p>FPS：第一人称视角的运动方式，旋转限制 lock-y</p></li>
</ul>
<h2 id="场景加载">3. 场景加载</h2>
<p>场景加载由 <code>AssetLoader</code> 的子类完成，基于第三方库
gltf_loader 或 assimp
实现加载接口。场景节点按照层级结构进行创建，并加载其变换信息。节点下的
mesh 数据根据材质种类创建为不同的
<code>PrimitiveEntity</code>，材质的加载包括材质参数的加载以及贴图资源的加载，压缩贴图
DDS 使用 gli 库，其他贴图使用 FreeImage。</p>
<p>由于还没做好场景 UI
界面，因此临时实现将场景层级信息输出到文件进行查看。打开宏
<code>IS_LOG_HIERARCHY_SCENE</code>，场景的层级节点信息会 log 到文件
"intermediate/hierarchy_log.txt"，部分层级如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">|-vk_root</span><br><span class="line">|**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br><span class="line">|</span><br><span class="line">|----|-scene_root</span><br><span class="line">|    |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|    |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">0.0128205</span>,<span class="number">0.0128205</span>,<span class="number">0.0128205</span>), <span class="built_in">rot</span>(<span class="number">-90</span>,<span class="number">0</span>,<span class="number">0</span>)</span><br><span class="line">|    |</span><br><span class="line">|    |----|<span class="number">-5</span>a174a9db4f94280a298e075478d761a.fbx</span><br><span class="line">|         |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|         |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">90</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br><span class="line">|         |</span><br><span class="line">|         |----|-RootNode</span><br><span class="line">|              |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|              |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br><span class="line">|              |</span><br><span class="line">|              |----|-Diorama</span><br><span class="line">|                   |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|                   |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br><span class="line">|                   |               </span><br><span class="line">|                   |----|-Floor_Junk_Cluster_01_Clip2</span><br><span class="line">|                        |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|                        |**[<span class="built_in">pos</span>(<span class="number">-3.94234</span>,<span class="number">0</span>,<span class="number">8.75777</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">21.1553</span>,<span class="number">0</span>)</span><br><span class="line">|                        |</span><br><span class="line">|                        |----|-Floor_Junk_Cluster_01_Clip2_Metal_Aluminium_0</span><br><span class="line">|                             |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">|                             |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br><span class="line">|                              </span><br><span class="line">|----|-DefaultCamera</span><br><span class="line">     |**[trans: <span class="comment">// ...变换矩阵省略</span></span><br><span class="line">     |**[<span class="built_in">pos</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>), <span class="built_in">scale</span>(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>), <span class="built_in">rot</span>(<span class="number">0</span>,<span class="number">-0</span>,<span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p><strong>Mesh 数据的组织方式</strong>：目前只有静态
mesh，即不会发生变形的 mesh。在加载资源过程中，按照如下方式组织 mesh
数据：</p>
<ul>
<li>将所有 mesh 数据加载到连续区域的 buffer 中。目前所有 mesh
共享同一连续区域，之后如果支持动态 mesh 后，可能会根据 mesh
类型划分。</li>
<li>mesh 的数据结构：不直接包含 mesh 原数据，而是保存其 mesh 数据在
buffer 区域中的位置描述，由 <code>PrimitiveEntity</code> 持有，
<ul>
<li>buffer location: mesh 数据所在 buffer 的起始地址</li>
<li>offset: mesh 数据在 buffer 中相对于其实地址的偏移量</li>
<li>size: mesh 数据的大小</li>
</ul></li>
</ul>
<h1 id="draw-call-组织策略">Draw Call 组织策略</h1>
<p>对于一个复杂场景而言，具有很多种类的材质、mesh，往往需要很大量的 draw
call 进行绘制，但 draw call
的固有开销又限制了实时条件下硬件能支持的最大数量。在 Nvidia 有关 Draw
Call 优化 <a href="#%5B1%5D">[1]</a> 的演讲中有提到，draw call
会有固有的驱动层的参数验证开销，这在当时是最大的瓶颈，而这种验证开销是由管线状态改变导致的，下面是不同管线状态改变相对开销对比：</p>
<p><img src="/images/Render Engine/Scene Graph and Draw Call Grouping Strategy.assets/image-20220716235751202.png" alt="image-20220716235751202" style="zoom: 25%;"></p>
<p>可以看出 render target、shader、texture 的开销依次降低，想要支持的
draw call 数量更大，就要减少这些状态改变。其中提高一些策略：</p>
<ul>
<li>使用 texture array 可以一次性配置好所有 texture，减少 texture
binding 的改变</li>
<li>使用 SSBO 存储整个场景的 uniform 数据，减少 UBO binding 的改变</li>
<li>使用 indirect draw，可以一次性下发一批 draw
call，让验证一次完成，减少 draw call 的验证开销</li>
</ul>
<p>下面介绍上述 draw call 策略的实现</p>
<h2 id="绘制时由-mesh-数据结构到-render-proxy">1. 绘制时由 mesh
数据结构到 render proxy</h2>
<p>绘制不直接接触 mesh 数据结构，而是使用为每个 render object 构建的
render proxy。在初始化场景渲染信息时，遍历场景结点的 render object 创建
render proxy，并加入场景的 render proxy list 中。一个 render object
可能是一个完整的物体，例如车，而车可能包含多个使用不同材质的
mesh。render proxy 对其拥有的所有 mesh 进行组织，根据材质不同，划分出
meshBatch。每个 meshBatch 保持的是 render object 的一种材质的所有 mesh
信息。因此一个 render object 的 render proxy 包含：</p>
<ul>
<li>&lt;mat0, meshBatch0&gt;，&lt;mat1, meshBatch1&gt;，... ...</li>
<li>其所属场景节点的 <code>node ID</code></li>
<li><code>proxyIndex</code>：render proxy 在场景的 render proxy list
的索引位置</li>
</ul>
<p>来自相同的场景结点的 render proxy 可以使用同一个 UBO
传递变换矩阵。不同场景结点的 render proxy具有不同的变换，需要使用不同的
UBO。而不同材质又需要不同的材质 UBO、不同的 texture
等等。如果按照最直接的 indexed draw call，不同材质、不同场景结点的
render proxy 必须使用不同的 draw call，这会使得 draw call
数量非常多。</p>
<h2 id="将大量-render-proxy-组织到少量-render-group-中">2. 将大量 render
proxy 组织到少量 render group 中</h2>
<p>本渲染器使用 <code>MeshMaterialRenderGroup</code> 实现组织 draw call
策略，根据开启的策略，将 render proxy 组织成 render group，一个 render
group 即表示了一个 index draw call。下面介绍使用 SSBO、texture array
来从所有的 render proxy 构建出场景的 render group 列表。</p>
<h3 id="组织策略">2.1 组织策略</h3>
<p><strong>Scene SSBO</strong>：对于场景节点变换矩阵等节点数据，使用
SSBO 存储场景结点变换数据，存储位置为节点的 node ID。</p>
<p><strong>Group Mat
SSBO</strong>：对于不同材质的参数、贴图，如果材质可以合并（参数种类相同，贴图格式、大小相同），则合并到同一个
render group。使用 SSBO 存储材质的参数，合并多个材质的贴图为 texture
array。材质参数在 SSBO 中的位置为material index。每种材质贴图在 texture
array 中的位置也需要一个索引。</p>
<p><strong>Group Instance SSBO</strong>：由于一个场景节点可能会有多个
render proxy，因此还需要使用一个 SSBO 来存储 render proxy 所使用的 node
ID 与 material index，分别索引 Scene SSBO与Group Mat SSBO，获取 render
proxy 的变换、材质数据。</p>
<p>最后配置 render group 的 draw call 使得内置变量
<code>gl_InstanceIndex</code> 能够索引 Group Instance SSBO，也就是
render proxy 在场景的 render proxy 列表中的索引。</p>
<h3 id="实现-render-group-的组织">2.2 实现 render group 的组织</h3>
<p>经过场景中的所有 render proxy 得到很多材质和 meshBatch 的组合，例如
&lt;mat0, meshBatch0&gt;，&lt;mat0, meshBatch1&gt; ，&lt;mat1,
meshBatch0&gt;，&lt;mat1, meshBatch2&gt;
等等。依次尝试将每个组合合并到现有 render group
中，如果没有找到可以合并的 render group，则为该组合创建一个新的 render
group。对于输入 mat+meshBatch 组合遍历现存的每个 render
group，如果同时满足以下条件则可以合并：</p>
<ul>
<li>输入 mat+meshBatch 组合的材质种类是否与当前 render group
的材质种类相同，不相同则不可合并。</li>
<li>输入 mat+meshBatch 组合的材质贴图格式、大小等是否与当前 render group
的材质相同，不相同则不可合并。</li>
</ul>
<p>当一个输入 mat+meshBatch 组合可以合并到目标 render group
时，进行合并操作，为目标 render group 生成一个
<code>group instance</code>：</p>
<ul>
<li><p>材质信息存入目标 render group 的材质列表中。该材质位于 render
group 中的位置作为索引 material
index。由于多个材质可能引用同一贴图，贴图在 texture array
中的索引与材质索引可能不同，因此还需要存储不同贴图的索引，如 baseColor
贴图索引、normal map 索引。以及存储材质参数，如 base color
因子等。这些材质数据都存储在 <strong>Group Mat SSBO</strong>
的一个元素中，如下例结构体：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">MaterialUniformBufferParams</span></span><br><span class="line">&#123;</span><br><span class="line">    Vector4f BaseColorFactor;</span><br><span class="line">    <span class="type">float</span> RoughnessFactor;</span><br><span class="line">    <span class="type">float</span> MetallicFactor;</span><br><span class="line">    <span class="type">float</span> OcculusionStrength;</span><br><span class="line"></span><br><span class="line">    <span class="type">uint32_t</span> BaseColorTexIndex;</span><br><span class="line">    <span class="type">uint32_t</span> OccuRoughMetalTexIndex;</span><br><span class="line">    <span class="type">uint32_t</span> NormalMapIndex;</span><br><span class="line">    <span class="type">uint32_t</span> EmissiveMapIndex;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>meshBatch 信息存入目标 render group。如果已有相同的 meshBatch
则为对应 group instance 增加一个 instance 数据，即输入的 meshBatch
对应的 <code>&#123;node index、material index&#125;</code>。如果不存在相同的
meshBatch，则为输入的 meshBatch 新增一个 group
instance。这些信息都存储于 <strong>Group Instance SSBO</strong>
的一个元素中，如下例结构体：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">MeshInstanceData</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">uint32_t</span> NodeIndexInScene;</span><br><span class="line">    <span class="type">uint32_t</span> MatIndex;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>render group 的 group instance 创建完成，组织 draw call
信息，得到有效的 <code>gl_InstanceIndex</code> ，用以索引 <strong>Group
Instance SSBO</strong>。初始化 instanceOffset 为 0，按照 meshBatch 在
render group 中的顺序，依次进行：</p>
<ul>
<li>当前 meshBatch 的 group instance 的数量作为 instanceCount，当前的
instanceOffset 作为 firstInstance</li>
<li>instanceOffset = instanceOffset+instanceCount</li>
</ul></li>
</ul>
<p>经过以上步骤，可以通过 <code>gl_InstanceIndex</code> 索引
<strong>Group Instance SSBO</strong>，得到当前绘制 mesh 对应的 node
index 和 material index。node index 用来索引 <strong>Scene SSBO</strong>
获取当前 mesh 对应的场景 node 数据，如 model to world 变换。material
index 用来索引 <strong>Group Mat SSBO</strong> 得到当前 mesh
对应的材质参数，以及材质贴图在 texture array 中的索引。</p>
<h2 id="不同条件下的性能比较">3. 不同条件下的性能比较</h2>
<p>本渲染器目前的场景比较简单：只有静态
mesh，无实时交互，只有单线程，并且场景全部加载。因此直接使用 FPS
进行性能比较。三个相关宏：</p>
<ul>
<li><p><code>IS_SHARE_VTX_IDX_BUFFER</code>：是否整个场景公用同一个
index/vertex buffer</p></li>
<li><p><code>IS_USING_MERGE_MATERIAL</code>：是否合并材质，使用 texture
array</p></li>
<li><p><code>IS_USING_INDIRECT_DRAW</code>：是否开启 indirect
draw，</p></li>
</ul>
<p>测试场景一共创建了 170 个 mesh render proxies。</p>
<ul>
<li>关闭所有选项使用最直接的 index draw call：组织为 170 个 render
group，每个 render group 需要一次 index draw call，FPS 为 65 左右</li>
<li>只开启 <code>IS_SHARE_VTX_IDX_BUFFER</code> 使得 170 次 index draw
call 都使用同一个 index/vertex buffer，FPS 无明显变化</li>
<li>只开启 <code>IS_USING_MERGE_MATERIAL</code>，将材质可以合并的 mesh
合并为一个 render group，一共得到 6 个 render group。每个 render group
使用一次 index draw call，同时使用一个 index/vertex buffer，FPS 提高到
142 左右
<ul>
<li>同时开启 <code>IS_USING_MERGE_MATERIAL</code> 和
<code>IS_SHARE_VTX_IDX_BUFFER</code>，即 6 次 index draw call 共享同一个
index/vertex buffer。FPS 无明显改变。</li>
</ul></li>
<li>只开启 <code>IS_USING_INDIRECT_DRAW</code>，这时
IS_SHARE_VTX_IDX_BUFFER 也会同时开启。将所有 index draw call
参数存到一个 indirect buffer 中，使用一次 indirect draw 将所有 index
draw call 提交。因为没有合并材质，因此共生成 170 个 render group，即 170
个 index draw call，每个 render group 在一个 indirect draw 中提交。FPS
为 68 左右，比最直接的 index draw call
有些许提高。本测试场景瓶颈在材质种类数量，而不在节点数量，因此不合并材质
indirect draw 发挥不了作用</li>
<li>同时开启 <code>IS_USING_INDIRECT_DRAW</code> 和
<code>IS_USING_MERGE_MATERIAL</code>，一共生成 6 个 render group，即 6
个 index draw call，在一次 indirect draw 中提交。FPS 为 650
左右，相比于最直接的 index draw call，提升了 10
倍。就算对于仅合并材质的情况，即使用 6 次 index draw call，也有将近 4.5
倍提升。</li>
</ul>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a>
https://developer.nvidia.com/content/how-modern-opengl-can-radically-reduce-driver-overhead-0</p>
]]></content>
      <categories>
        <category>Render Engine</category>
      </categories>
  </entry>
  <entry>
    <title>Compute Shader 实现 Pyramid Algorithm</title>
    <url>/Vulkan-Basics/Compute-Shader-%E5%AE%9E%E7%8E%B0-Pyramid-Algorithm.html</url>
    <content><![CDATA[<h1 id="使用-compute-shader-生成-mipmap">使用 compute shader 生成
mipmap</h1>
<p>本文是参考 https://github.com/nvpro-samples/vk_compute_mipmaps
的算法复现。</p>
<p>将输入层级记为 <span class="math inline">\(L\)</span>，将 <span class="math inline">\(L\)</span> 的 2x2 样本区域生成一个样本则得到 <span class="math inline">\(L+1\)</span> 层级。下面介绍从 <span class="math inline">\(L\)</span> 到 <span class="math inline">\(L+M\)</span> 层级的生成算法，<span class="math inline">\(M\in[1,6]\)</span>，算法输入为 <span class="math inline">\(L\)</span> 层级的大小为 <span class="math inline">\(2^M\times 2^M\)</span> 的 tiles。</p>
<h1 id="宽高为-2-的幂次">宽高为 2 的幂次</h1>
<p>首先将输入层级划分为多个大小为 <span class="math inline">\(2^M\times
2^M\)</span> 的 tile，每个 tile 作为 compute shader 中一个 local
workgroup 的输入 tile，每个 local workgroup 负责生成其输入 tile
的层级。假设输入层级大小为 <span class="math inline">\(X,Y\)</span>，那么一次 dispatch 产生的 local
workgroup 数量为 <span class="math inline">\((X/2^M,
Y/2^M)\)</span>，因此使用 workgroup id 作为 tile id，进而得到输入 tile
在输入层级中的起始位置，用以获取输入层级的样本。</p>
<ul>
<li><p>假设 workgroup id 为 <span class="math inline">\((m,n)\)</span>，该 workgroup 的输入 tile id 为
<span class="math inline">\((m,n)\)</span>。该输入 tile
在输入层级中的起始位置设为 (xOffset, yOffset)，其计算方式为
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xOffset = m &lt;&lt; M;</span><br><span class="line">yOffset = n &lt;&lt; M;</span><br></pre></td></tr></table></figure></p></li>
<li><p>为了能够利用到 subgroup 内部数据同步的高效性，将 workgroup
的线程/invocation 划分为大小为 16 的子组。这里使用 1D 的 invocation
ID，<code>gl_LocalInvocationIndex</code> 来划分组，划分方式为
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">teamID = (ID &amp; <span class="number">0x00F0</span>) &gt;&gt; <span class="number">4</span>;</span><br></pre></td></tr></table></figure></p></li>
<li><p>想要生成当前层级的样本，则需要确定当前层级样本在上一层级中对应的
<span class="math inline">\(2\times 2\)</span>
区域，即该区域在上一层级中的起始偏移量 (xBlockOffset,
yBlockOffset)。该偏移量有三部分组成，当前 workgroup 对应的输入 tile
在输入层级中的起始偏移量 (xOffset, yOffset)、当前 invocation
所属组在输入 tile 中的起始偏移量 (tx, ty)、当前 invocation
在其所属组的起始偏移量 (x, y)。最终有 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xBlockOffset = xOffset + tx + x;</span><br><span class="line">yBlockOffset = yOffset + ty + y;</span><br></pre></td></tr></table></figure></p></li>
</ul>
<h2 id="level">1-Level</h2>
<p>输入层级划分为 <span class="math inline">\(2\times 2\)</span>
tiles，每个线程加载一个 <span class="math inline">\(2\times2\)</span>
tile 生成一个样本。1 level 算法比较简单，输入层级有多少个 <span class="math inline">\(2\times 2\)</span> tiles 就分发多少个
invocation，即 global group size 与 tile 数量相等。直接使用
<code>gl_GlobalInvocationID</code> 来索引 <span class="math inline">\(L\)</span> 层级的样本。以下为一个 tile 中的 4
个样本的索引</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="built_in">gl_GlobalInvocationID</span>*<span class="number">4</span>+<span class="number">0</span>, <span class="built_in">gl_GlobalInvocationID</span>*<span class="number">4</span>+<span class="number">1</span>, </span><br><span class="line"><span class="built_in">gl_GlobalInvocationID</span>*<span class="number">4</span>+<span class="number">2</span>, <span class="built_in">gl_GlobalInvocationID</span>*<span class="number">4</span>+<span class="number">3</span></span><br></pre></td></tr></table></figure>
<p>或者按照下述算法形式实现。</p>
<h2 id="levels">2-Levels</h2>
<p>输入层级划分为 <span class="math inline">\(4\times 4\)</span>
tiles，local workgroup size 使用 4。一个输入 tile 可以划分为 4 个 <span class="math inline">\(2\times 2\)</span> blocks，每个 block 对应 <span class="math inline">\(L+1\)</span> 层的一个样本。因此每个 local
workgroup 负责一个输入 tile，生成 4 个 <span class="math inline">\(L+1\)</span> 层级样本，4 个 <span class="math inline">\(L+1\)</span> 层级样本又可生成 1 个 <span class="math inline">\(L+2\)</span> 层级样本。算法过程如下示意，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">   <span class="built_in">L</span>(<span class="number">4</span>x4)            L+<span class="number">1</span>(<span class="number">2</span>x2)             L+<span class="number">2</span></span><br><span class="line">+---------+       +-----+-----+       +---------+</span><br><span class="line">|         |       |  <span class="number">0</span>  |  <span class="number">1</span>  |       |         |</span><br><span class="line">|   <span class="number">4</span>x4   | ===&gt;  +-----+-----+ ====&gt; |    <span class="number">0</span>    |</span><br><span class="line">|         |       |  <span class="number">2</span>  |  <span class="number">3</span>  |       |         |</span><br><span class="line">+---------+       +-----+-----+       +---------+</span><br></pre></td></tr></table></figure>
<p>确定 <span class="math inline">\(L+1\)</span> 层每个样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始位置，</p>
<ul>
<li><p>输入 tile 在 <span class="math inline">\(L\)</span>
层级的起始位置：<span class="math inline">\(M=2\)</span> 代入
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xOffset = m &lt;&lt; M;</span><br><span class="line">yOffset = n &lt;&lt; M;</span><br></pre></td></tr></table></figure></p></li>
<li><p>local workgroup 内线程的分组 ID：由于 local workgroup size 为
4，因此只有一个分组，teamID 恒为 0， <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">teamID = (ID &amp; <span class="number">0x00F0</span>) &gt;&gt; <span class="number">4</span>;</span><br></pre></td></tr></table></figure></p>
<p>当前分组在输入 tile 中的偏移量也恒为 tx=0, ty=0</p></li>
<li><p>当前 invocation
在其所属分组的偏移量：<code>gl_LocalInvocationIndex</code> 为当前
invocation 的索引，记为 id。<span class="math inline">\(L+1\)</span>
层的 4 个样本如下，以及对应的组内偏移量 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> L+<span class="number">1</span> 样本编号       L+<span class="number">1</span> 样本编号二进制    L+<span class="number">1</span> 样本的组内起始偏移量</span><br><span class="line">+-----+-----+       +-----+-----+       +-----+-----+</span><br><span class="line">|  <span class="number">0</span>  |  <span class="number">1</span>  |       |  <span class="number">00</span> |  <span class="number">01</span> |       |(<span class="number">0</span>,<span class="number">0</span>)|(<span class="number">0</span>,<span class="number">2</span>)|</span><br><span class="line">+-----+-----+       +-----+-----+       +-----+-----+</span><br><span class="line">|  <span class="number">2</span>  |  <span class="number">3</span>  |       |  <span class="number">10</span> |  <span class="number">11</span> |       |(<span class="number">2</span>,<span class="number">0</span>)|(<span class="number">2</span>,<span class="number">2</span>)|</span><br><span class="line">+-----+-----+       +-----+-----+       +-----+-----+</span><br></pre></td></tr></table></figure></p>
<p>当前 invocation 组内的偏移量的计算方式如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">x = (id &amp; <span class="number">2</span>);</span><br><span class="line">y = (id &amp; <span class="number">1</span>) &lt;&lt; <span class="number">1</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>最终得到 <span class="math inline">\(L+1\)</span> 层样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始偏移量：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xBlockOffset = xOffset + x;</span><br><span class="line">yBlockOffset = yOffset + y;</span><br></pre></td></tr></table></figure></p></li>
</ul>
<p>得到 (xBlockOffset, yBlockOffset) 后，则可加载该 2x2 区域的 4 个
<span class="math inline">\(L\)</span> 层样本，即</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(xBlockOffset+<span class="number">0</span>, yBlockOffset+<span class="number">0</span>), (xBlockOffset+<span class="number">0</span>, yBlockOffset+<span class="number">1</span>)</span><br><span class="line">(xBlockOffset+<span class="number">1</span>, yBlockOffset+<span class="number">0</span>), (xBlockOffset+<span class="number">1</span>, yBlockOffset+<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
<p>4 个 <span class="math inline">\(L\)</span> 层样本生成一个 <span class="math inline">\(L+1\)</span> 层样本，这样下来，每个 local
workgroup 都得到了 <span class="math inline">\(L+1\)</span> 层级的 4
个样本。<span class="math inline">\(L+1\)</span> 层样本的写入位置为
(xBlockOffset &gt;&gt; 1, yBlockOffset &gt;&gt; 1)。</p>
<p>由于 local workgroup size 为 4，即 local workgroup 的所有 invocation
执行于同一个 subgroup，因此可利用 subgroup 内部的高效数据同步。每个
local workgroup 的 0 号线程再使用 <code>subgroupShuffleXor</code> 得到
1、2、3 号线程的样本，生成 <span class="math inline">\(L+2\)</span>
层级的一个样本，写入位置为 (xBlockOffset &gt;&gt; 2, yBlockOffset
&gt;&gt; 2)。</p>
<h2 id="levels-1">3-Levels</h2>
<p>3 levels 通过不同区域分别应用 2 levels 算法。输入层级划分为 <span class="math inline">\(8\times 8\)</span> tiles，local workgroup size
使用 16。将 local workgroup 的 16 个线程分为 4 个 subtiles，每个 subtile
大小为 <span class="math inline">\(2\times 2\)</span>，具有 4
个线程，如下所示</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">workgroup 内的 subtile，<span class="string">&quot;|| =&quot;</span> 为 subtile 边界</span><br><span class="line">      +---+---++---+---+                              </span><br><span class="line">      | <span class="number">0</span> | <span class="number">1</span> || <span class="number">4</span> | <span class="number">5</span> |                               </span><br><span class="line">      +---+---++---+---+                              </span><br><span class="line">      | <span class="number">2</span> | <span class="number">3</span> || <span class="number">6</span> | <span class="number">7</span> |                              </span><br><span class="line">      +===+===++===+===+                              </span><br><span class="line">      | <span class="number">8</span> | <span class="number">9</span> ||<span class="number">12</span> |<span class="number">13</span> |                              </span><br><span class="line">      +---+---++---+---+                               </span><br><span class="line">      |<span class="number">10</span> |<span class="number">11</span> ||<span class="number">14</span> |<span class="number">15</span> |                               </span><br><span class="line">      +---+---++---+---+                               </span><br></pre></td></tr></table></figure>
<p>local workgroup 的每个 subtile 应用 2 levels 算法来生成 <span class="math inline">\(L+1\)</span>、<span class="math inline">\(L+2\)</span> 的样本，在执行完 2 levels
算法后，每个 subtile 得到一个 <span class="math inline">\(L+2\)</span>
层样本。每个 workgroup 共得到 4 个 <span class="math inline">\(L+2\)</span> 层样本，最后生成 1 个 <span class="math inline">\(L+3\)</span> 层样本，过程如下所示，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">     <span class="built_in">L</span>(<span class="number">8</span>x8)                  L+<span class="number">1</span>(<span class="number">4</span>x4)                  L+<span class="number">2</span>(<span class="number">2</span>x2)</span><br><span class="line">+---------------+       +---+---++---+---+       +-------++-------+</span><br><span class="line">|               |       | <span class="number">0</span> | <span class="number">1</span> || <span class="number">4</span> | <span class="number">5</span> |       |       ||       |</span><br><span class="line">|               |       +---+---++---+---+       |   <span class="number">0</span>   ||   <span class="number">4</span>   |</span><br><span class="line">|               |       | <span class="number">2</span> | <span class="number">3</span> || <span class="number">6</span> | <span class="number">7</span> |       |       ||       |</span><br><span class="line">|      <span class="number">8</span>x8      | ====&gt; +===+===++===+===+ ====&gt; +=======++=======+</span><br><span class="line">|               |       | <span class="number">8</span> | <span class="number">9</span> ||<span class="number">12</span> |<span class="number">13</span> |       |       ||       |</span><br><span class="line">|               |       +---+---++---+---+       |   <span class="number">8</span>   ||  <span class="number">12</span>   |</span><br><span class="line">|               |       |<span class="number">10</span> |<span class="number">11</span> ||<span class="number">14</span> |<span class="number">15</span> |       |       ||       |</span><br><span class="line">+---------------+       +---+---++---+---+       +-------++-------+</span><br></pre></td></tr></table></figure>
<p>首先确定 <span class="math inline">\(L+1\)</span> 层每个样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始位置，</p>
<ul>
<li><p>输入 tile 在 <span class="math inline">\(L\)</span>
层级的起始位置：<span class="math inline">\(M=3\)</span> 代入 (xOffset,
yOffset) 的计算</p></li>
<li><p>local workgroup 内线程的分组编号：local workgroup size 为
16，只有一个分组，teamID 恒为 0，当前分组在输入 tile 中的偏移量也恒为
tx=0, ty=0</p></li>
<li><p>当前 invocation
在其所属分组的偏移量：<code>gl_LocalInvocationIndex</code> 为当前
invocation 的索引，记为 id。<span class="math inline">\(L+1\)</span>
层的 16 个样本编号以及对应的组内偏移量，如下 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">         L+<span class="number">1</span> 层编号二进制              L+<span class="number">1</span> 层编号对应的输入 tile 的偏移量</span><br><span class="line">+------+------++------+------+       +------+------++------+------+</span><br><span class="line">| <span class="number">0000</span> | <span class="number">0001</span> || <span class="number">0100</span> | <span class="number">0101</span> |       |(<span class="number">0</span>,<span class="number">0</span>) |(<span class="number">0</span>,<span class="number">2</span>) ||(<span class="number">0</span>,<span class="number">4</span>) |(<span class="number">0</span>,<span class="number">6</span>) |</span><br><span class="line">+------+------++------+------+       +------+------++------+------+</span><br><span class="line">| <span class="number">0010</span> | <span class="number">0011</span> || <span class="number">0110</span> | <span class="number">0111</span> |       |(<span class="number">2</span>,<span class="number">0</span>) |(<span class="number">2</span>,<span class="number">2</span>) ||(<span class="number">2</span>,<span class="number">4</span>) |(<span class="number">2</span>,<span class="number">6</span>) |</span><br><span class="line">+======+======++======+======+       +======+======++======+======+</span><br><span class="line">| <span class="number">1000</span> | <span class="number">1001</span> || <span class="number">1100</span> | <span class="number">1101</span> |       |(<span class="number">4</span>,<span class="number">0</span>) |(<span class="number">4</span>,<span class="number">2</span>) ||(<span class="number">4</span>,<span class="number">4</span>) |(<span class="number">4</span>,<span class="number">6</span>) |</span><br><span class="line">+------+------++------+------+       +------+------++------+------+</span><br><span class="line">| <span class="number">1010</span> | <span class="number">1011</span> || <span class="number">1110</span> | <span class="number">1111</span> |       |(<span class="number">6</span>,<span class="number">0</span>) |(<span class="number">6</span>,<span class="number">2</span>) ||(<span class="number">6</span>,<span class="number">4</span>) |(<span class="number">6</span>,<span class="number">6</span>) |</span><br><span class="line">+------+------++------+------+       +------+------++------+------+</span><br></pre></td></tr></table></figure></p>
<p>当前 invocation 组内的偏移量的计算方式如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">x = (id &amp; <span class="number">2</span>) | (id &amp; <span class="number">8</span>) &gt;&gt; <span class="number">1</span>;</span><br><span class="line">y = (id &amp; <span class="number">1</span>) &lt;&lt; <span class="number">1</span> | (id &amp; <span class="number">4</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>最终得到 <span class="math inline">\(L+1\)</span> 层样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始偏移量：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xBlockOffset = xOffset + x;</span><br><span class="line">yBlockOffset = yOffset + y;</span><br></pre></td></tr></table></figure></p></li>
</ul>
<p>与 2 levels 算法相似，得到 (xBlockOffset, yBlockOffset) 后，即可得到
<span class="math inline">\(L\)</span> 层级的 4 个样本，从而生成 <span class="math inline">\(L+1\)</span> 层级样本。每个 local workgroup 共生成
16 个 <span class="math inline">\(L+1\)</span> 层级样本，写入位置为
(xBlockOffset &gt;&gt; 1, yBlockOffset &gt;&gt; 1)。</p>
<p>每个 local workgroup 在同一个 subgroup 内执行，因此利用 subgroup
内部的高效数据同步，每个 subtile 的第一个线程收集其他线程生成的 <span class="math inline">\(L+1\)</span> 层级样本，然后生成一个 <span class="math inline">\(L+2\)</span> 层级样本。每个 subtile 的第一个线程为
<code>id&amp;3 == 0</code>，通过 使用 mask 参数分别为 1、2、3
的<code>subgroupShuffleXor</code> ，得到 subtile 的其他线程生成的 <span class="math inline">\(L+1\)</span> 层级样本，过程如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> (<span class="number">0000</span>) ^ (<span class="number">0001</span>, <span class="number">0010</span>, <span class="number">0011</span>) = (<span class="number">0001</span>, <span class="number">0010</span>, <span class="number">0011</span>) = (<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"><span class="number">4</span> (<span class="number">0100</span>) ^ (<span class="number">0001</span>, <span class="number">0010</span>, <span class="number">0011</span>) = (<span class="number">0101</span>, <span class="number">0110</span>, <span class="number">0111</span>) = (<span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>)</span><br><span class="line"><span class="number">8</span> (<span class="number">1000</span>) ^ (<span class="number">0001</span>, <span class="number">0010</span>, <span class="number">0011</span>) = (<span class="number">1001</span>, <span class="number">1010</span>, <span class="number">1011</span>) = (<span class="number">9</span>, <span class="number">10</span>, <span class="number">11</span>)</span><br><span class="line"><span class="number">12</span>(<span class="number">1100</span>) ^ (<span class="number">0001</span>, <span class="number">0010</span>, <span class="number">0011</span>) = (<span class="number">1101</span>, <span class="number">1110</span>, <span class="number">1111</span>) = (<span class="number">13</span>, <span class="number">14</span>, <span class="number">15</span>)</span><br></pre></td></tr></table></figure>
<p>每个 local workgroup 生成 4 个 <span class="math inline">\(L+2\)</span> 层级样本，写入位置为 (xBlockOffset
&gt;&gt; 2, yBlockOffset &gt;&gt; 2)。每个 local workgroup
中的第一个线程使用 <code>subgroupShuffleXor</code> 得到 4、8、12
号线程的 <span class="math inline">\(L+2\)</span> 层级样本，生成一个
<span class="math inline">\(L+3\)</span> 层级样本，写入位置为
(xBlockOffset &gt;&gt; 3, yBlockOffset &gt;&gt; 3)。每个 local workgroup
的第一个线程为 <code>id&amp;15 == 0</code>，过程如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span>(<span class="number">0000</span>) ^ (<span class="number">0100</span>, <span class="number">1000</span>, <span class="number">1100</span>) = (<span class="number">0100</span>, <span class="number">1000</span>, <span class="number">1100</span>)</span><br></pre></td></tr></table></figure>
<h2 id="levels-2">4/5-Levels</h2>
<p>理论上，上述算法可以继续递归应用得到 4/5 levels 算法，例如对于 4
levels 算法，输入层级划分为 <span class="math inline">\(16\times
16\)</span> tiles，local workgroup 大小使用 64，划分为 4 组，每组 16
个线程，分别应用 3 levels 算法，然后 0 号线程使用 shuffle 得到
16、32、48 号线程的样本，最终生成 <span class="math inline">\(L+4\)</span> 的一个样本。但实际上，subgroup
大小有限，例如 NVIDIA 显卡的 subgroup 大小为 32，无法使用 32 及之后的
gl_SubgroupInvocationID，因此不能通过 subgroup 得到 32、48
号线程的样本。对于此，改用 shared memory。</p>
<h3 id="levels-3">4-levels</h3>
<p>对于 4 levels 算法，输入层级划分为 <span class="math inline">\(16\times 16\)</span> tiles，local workgroup
大小使用 64，划分为 4 组，每组 16 个线程。local workgroup 中的线程使用
<code>gl_LocalInvocationIndex</code> 分组，0~15 为组 0，16~31 为组
1，32~47 为组 2，48~63 为组 3，如下所示：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">workgroup 内的分组，<span class="string">&quot;|| =&quot;</span> 为分组边界</span><br><span class="line">        每组中间数字为分组编号</span><br><span class="line">        +---+---++---+---+</span><br><span class="line">        | <span class="number">0</span> | <span class="number">4</span> ||<span class="number">16</span> |<span class="number">20</span> |</span><br><span class="line">        +--<span class="number">-0</span>---++--<span class="number">-1</span>---+</span><br><span class="line">        | <span class="number">8</span> |<span class="number">12</span> ||<span class="number">24</span> |<span class="number">28</span> |</span><br><span class="line">        +===+===++===+===+</span><br><span class="line">        |<span class="number">32</span> |<span class="number">36</span> ||<span class="number">48</span> |<span class="number">52</span> |</span><br><span class="line">        +--<span class="number">-2</span>---++--<span class="number">-3</span>---+</span><br><span class="line">        |<span class="number">40</span> |<span class="number">44</span> ||<span class="number">56</span> |<span class="number">60</span> |</span><br><span class="line">        +---+---++---+---+</span><br></pre></td></tr></table></figure>
<p>每组分别执行 3 levels 算法。但每组的第一个线程不仅生成 <span class="math inline">\(L+3\)</span> 的一个样本，还要将该样本写入 shared
memory中。然后发出一个 barrier，barrier 之后在 shared memory 中存在 4
组线程分别写入的一个 <span class="math inline">\(L+3\)</span>
层级样本，即 <span class="math inline">\(L+3\)</span> 层级的一个 <span class="math inline">\(2\times 2\)</span> tile。最后 0 号线程使用 shared
memory 中的 4 个 <span class="math inline">\(L+3\)</span>
层级样本生成一个 <span class="math inline">\(L+4\)</span>
层级样本。过程如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">     <span class="built_in">L</span>(<span class="number">16</span>x16)               L+<span class="number">2</span>(<span class="number">8</span>x8)                       L+<span class="number">3</span>(<span class="number">2</span>x2)                   L+<span class="number">4</span></span><br><span class="line">+-------++-------+     +---+---++---+---+             +-------++-------+        +---------------+</span><br><span class="line">|       ||       |     | <span class="number">0</span> | <span class="number">4</span> ||<span class="number">16</span> |<span class="number">20</span> |             |       ||       |        |               |</span><br><span class="line">|  <span class="number">8</span>x8  ||  <span class="number">8</span>x8  |     +---+---++---+---+             |   <span class="number">0</span>   ||  <span class="number">16</span>   |        |               |</span><br><span class="line">|       ||       |(L+<span class="number">1</span>)| <span class="number">8</span> |<span class="number">12</span> ||<span class="number">24</span> |<span class="number">28</span> |             |       ||       |        |               |</span><br><span class="line">+=======++=======+ ==&gt; +===+===++===+===+ =&gt;omitted=&gt; +=======++=======+ ==|==&gt; |      <span class="number">0</span>        |</span><br><span class="line">|       ||       |     |<span class="number">32</span> |<span class="number">36</span> ||<span class="number">48</span> |<span class="number">52</span> |      |      |       ||       |   |    |               |</span><br><span class="line">|  <span class="number">8</span>x8  ||  <span class="number">8</span>x8  |     +---+---++---+---+      |      |  <span class="number">32</span>   ||  <span class="number">48</span>   |   |    |               |</span><br><span class="line">|       ||       |     |<span class="number">40</span> |<span class="number">44</span> ||<span class="number">56</span> |<span class="number">60</span> |      |      |       ||       |   |    |               |</span><br><span class="line">+-------++-------+     +---+---++---+---+      |      +-------++-------+   |    +---------------+</span><br><span class="line">                                               |                           |</span><br><span class="line">                        <span class="number">16</span>-thread team handles each <span class="number">8</span>x8 sub-<span class="built_in">tile</span>        (barrier)</span><br></pre></td></tr></table></figure>
<p>首先确定 <span class="math inline">\(L+1\)</span> 层每个样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始位置，</p>
<ul>
<li><p>输入 tile 在 <span class="math inline">\(L\)</span>
层级的起始位置：<span class="math inline">\(M=4\)</span> 代入 (xOffset,
yOffset) 的计算</p></li>
<li><p>local workgroup 内线程的分组编号：local workgroup size 为
64，分为 4 个组，0~15 为组 0，16~31 为组 1，32~47 为组 2，48~63 为组
3。如下所示， <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">    L+<span class="number">1</span> 层编号十六进制        L+<span class="number">1</span>层编号对应的输入 tile 的偏移量           L+<span class="number">1</span> 层分组编号</span><br><span class="line">+----+----++----+----+     +------+------++------+------+    +-----------++-----------+</span><br><span class="line">|<span class="number">0000</span>|<span class="number">0004</span>||<span class="number">0010</span>|<span class="number">0014</span>|     |(<span class="number">0</span>,<span class="number">0</span>) |(<span class="number">0</span>,<span class="number">4</span>) ||(<span class="number">0</span>,<span class="number">8</span>) |(<span class="number">0</span>,<span class="number">12</span>)|    |           ||           |</span><br><span class="line">+----+----++----+----+     +------+------++------+------+    |     <span class="number">0</span>     ||     <span class="number">1</span>     |</span><br><span class="line">|<span class="number">0008</span>|<span class="number">000</span>C||<span class="number">0018</span>|<span class="number">001</span>C|     |(<span class="number">4</span>,<span class="number">0</span>) |(<span class="number">4</span>,<span class="number">4</span>) ||(<span class="number">4</span>,<span class="number">8</span>) |(<span class="number">4</span>,<span class="number">12</span>)|    |           ||           |</span><br><span class="line">+====+====++====+====+     +======+======++======+======+    +===========++===========+</span><br><span class="line">|<span class="number">0020</span>|<span class="number">0024</span>||<span class="number">0030</span>|<span class="number">0034</span>|     |(<span class="number">8</span>,<span class="number">0</span>) |(<span class="number">8</span>,<span class="number">4</span>) ||(<span class="number">8</span>,<span class="number">8</span>) |(<span class="number">8</span>,<span class="number">12</span>)|    |           ||           |</span><br><span class="line">+----+----++----+----+     +------+------++------+------+    |     <span class="number">2</span>     ||     <span class="number">3</span>     |</span><br><span class="line">|<span class="number">0028</span>|<span class="number">002</span>C||<span class="number">0038</span>|<span class="number">003</span>C|     |(<span class="number">12</span>,<span class="number">0</span>)|(<span class="number">12</span>,<span class="number">4</span>)||(<span class="number">12</span>,<span class="number">8</span>)|(<span class="number">12</span>,<span class="number">12</span>|    |           ||           |</span><br><span class="line">+----+----++----+----+     +------+------++------+------+    +-----------++-----------+</span><br></pre></td></tr></table></figure></p>
<p>invocation 索引为 id，所属分组编号计算方式如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">teamID = (id &amp; <span class="number">0x00F0</span>) &gt;&gt; <span class="number">4</span>;</span><br></pre></td></tr></table></figure>
<p>当前分组在输入 tile 中的偏移量为 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">tx = (teamID &amp; <span class="number">2</span>) &lt;&lt; <span class="number">2</span>;</span><br><span class="line">ty = (teamID &amp; <span class="number">1</span>) &lt;&lt; <span class="number">3</span>;</span><br></pre></td></tr></table></figure></p></li>
<li><p>当前 invocation 在其所属分组的偏移量： <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">x = (id &amp; <span class="number">2</span>) | (id &amp; <span class="number">8</span>) &gt;&gt; <span class="number">1</span>;</span><br><span class="line">y = (id &amp; <span class="number">1</span>) &lt;&lt; <span class="number">1</span> | (id &amp; <span class="number">4</span>);</span><br></pre></td></tr></table></figure></p></li>
<li><p>最终得到 <span class="math inline">\(L+1\)</span> 层样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始偏移量：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xBlockOffset = xOffset + tx + x;</span><br><span class="line">yBlockOffset = yOffset + ty + y;</span><br></pre></td></tr></table></figure></p></li>
</ul>
<p>得到 (xBlockOffset, yBlockOffset) 后即可在每个分组内分别执行 3-levels
算法。每个分组都会生成一个 <span class="math inline">\(L+3\)</span>
层级样本，即 workgroup 生成了 4 个 <span class="math inline">\(L+3\)</span> 层级样本。在生成最终的 <span class="math inline">\(L+4\)</span> 层样本时，不能再使用
<code>subgroupShuffleXor</code> 来得到其他分组生成的 <span class="math inline">\(L+3\)</span> 层级样本，因为 subgroup size 为
32，其他分组不一定位于同一个 subgroup 内，因此这时需要使用 shared
memory。</p>
<p>每个分组的第一个线程不仅需要生成一个 <span class="math inline">\(L+3\)</span> 层级样本，还需要将该样本写入 shared
memory，组内的第一个线程为
<code>(id &amp; 0x00F0)==id</code>，写入位置为 teamID。加一个 barrier
等待 shared memory 的写操作完成。最后，workgroup 的第一个线程负责生成
<span class="math inline">\(L+4\)</span> 层样本。</p>
<h3 id="levels-4">5-levels</h3>
<p>对于 5 levels 算法，输入层级划分为 <span class="math inline">\(32\times 32\)</span> tiles，local workgroup
大小使用 256，划分为 16 组，每组 16 个线程。分组如下所示，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> workgroup的每个分组编号      workgroup内每组的invocation起始编号    </span><br><span class="line">+----+----++----+----+       +----+----++----+----+</span><br><span class="line">|  <span class="number">0</span> |  <span class="number">1</span> ||  <span class="number">4</span> |  <span class="number">5</span> |       | <span class="number">0</span>  | <span class="number">16</span> || <span class="number">64</span> | <span class="number">80</span> |</span><br><span class="line">+----+----++----+----+       +----+----++----+----+</span><br><span class="line">|  <span class="number">2</span> |  <span class="number">3</span> ||  <span class="number">6</span> |  <span class="number">7</span> |       | <span class="number">32</span> | <span class="number">48</span> || <span class="number">96</span> | <span class="number">112</span>|</span><br><span class="line">+====+====++====+====+       +====+====++====+====+</span><br><span class="line">|  <span class="number">8</span> |  <span class="number">9</span> || <span class="number">12</span> | <span class="number">13</span> |       |<span class="number">128</span> | <span class="number">144</span>||<span class="number">192</span> | <span class="number">208</span>| </span><br><span class="line">+----+----++----+----+       +----+----++----+----+</span><br><span class="line">| <span class="number">10</span> | <span class="number">11</span> || <span class="number">14</span> | <span class="number">15</span> |       |<span class="number">160</span> | <span class="number">176</span>||<span class="number">224</span> | <span class="number">240</span>|</span><br><span class="line">+----+----++----+----+       +----+----++----+----+ </span><br></pre></td></tr></table></figure>
<p>与 4 levels 算法相似，每组分别执行 3 levels
算法，并且每组的第一个线程不仅生成 <span class="math inline">\(L+3\)</span> 层级的一个样本，还要写入 shared
memory 中。在 barrier 之后，shared memory 中存在 16 组线程分别写入的一个
<span class="math inline">\(L+3\)</span> 层级样本，即 <span class="math inline">\(L+3\)</span> 层级的一个 <span class="math inline">\(4\times 4\)</span> tile。最后使用 4 个线程执行一次
2 levels 算法，最终得到 4 个 <span class="math inline">\(L+4\)</span>
层级样本与 1 个 <span class="math inline">\(L+5\)</span>
层级样本。过程如下所示，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">  <span class="built_in">L</span>(<span class="number">32</span>x32)                 L+<span class="number">3</span> (+ copy in smem)            L+<span class="number">4</span>(<span class="number">2</span>x2)                  L+<span class="number">5</span></span><br><span class="line">+---+---++---+---+             +---+---++---+---+        +-------++-------+      +---------------+</span><br><span class="line">|<span class="number">8</span>x8|<span class="number">8</span>x8||<span class="number">8</span>x8|<span class="number">8</span>x8|             |  <span class="number">0</span>| <span class="number">16</span>|| <span class="number">64</span>| <span class="number">80</span>|        |       ||       |      |               |</span><br><span class="line">+---+---++---+---+             +---+---++---+---+        |   <span class="number">0</span>   ||   <span class="number">1</span>   |      |               |</span><br><span class="line">|<span class="number">8</span>x8|<span class="number">8</span>x8||<span class="number">8</span>x8|<span class="number">8</span>x8|             | <span class="number">32</span>| <span class="number">48</span>|| <span class="number">96</span>|<span class="number">112</span>|        |       ||       |      |               |</span><br><span class="line">+===+===++===+===+ =&gt;omitted=&gt; +===+===++===+===+ ==|==&gt; +=======++=======+ ===&gt; |       <span class="number">0</span>       |</span><br><span class="line">|<span class="number">8</span>x8|<span class="number">8</span>x8||<span class="number">8</span>x8|<span class="number">8</span>x8|       |     |<span class="number">128</span>|<span class="number">144</span>||<span class="number">192</span>|<span class="number">208</span>|   |    |       ||       |      |               |</span><br><span class="line">+---+---++---+---+       |     +---+---++---+---+   |    |   <span class="number">2</span>   ||   <span class="number">3</span>   |      |               |</span><br><span class="line">|<span class="number">8</span>x8|<span class="number">8</span>x8||<span class="number">8</span>x8|<span class="number">8</span>x8|       |     |<span class="number">160</span>|<span class="number">176</span>||<span class="number">224</span>|<span class="number">240</span>|   |    |       ||       |      |               | </span><br><span class="line">+---+---++---+---+       |     +---+---++---+---+   |    +-------++-------+      +---------------+</span><br><span class="line">                         |                          |</span><br><span class="line">   <span class="number">16</span>-thread team handles each <span class="number">8</span>x8 sub-<span class="built_in">tile</span>      (barrier)</span><br></pre></td></tr></table></figure>
<p>首先确定 <span class="math inline">\(L+1\)</span> 层每个样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始位置，</p>
<ul>
<li><p>输入 tile 在 <span class="math inline">\(L\)</span>
层级的起始位置：<span class="math inline">\(M=5\)</span> 代入 (xOffset,
yOffset) 的计算</p></li>
<li><p>local workgroup 内线程的分组编号：local workgroup size 为
256，分为 16 个组，分组如下 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">        分组编号                每组的起始编号十六进制            起始编号在输入 tile 中对应的偏移量</span><br><span class="line">+----+----++----+----+       +----+----++----+----+        +-------+-------++-------+-------+</span><br><span class="line">|  <span class="number">0</span> |  <span class="number">1</span> ||  <span class="number">4</span> |  <span class="number">5</span> |       |<span class="number">0000</span>|<span class="number">0010</span>||<span class="number">0040</span>|<span class="number">0050</span>|        | (<span class="number">0</span>,<span class="number">0</span>) | (<span class="number">0</span>,<span class="number">8</span>) ||(<span class="number">0</span>,<span class="number">16</span>) | (<span class="number">0</span>,<span class="number">24</span>)|</span><br><span class="line">+----+----++----+----+       +----+----++----+----+        +-------+-------++-------+-------+</span><br><span class="line">|  <span class="number">2</span> |  <span class="number">3</span> ||  <span class="number">6</span> |  <span class="number">7</span> |       |<span class="number">0020</span>|<span class="number">0030</span>||<span class="number">0060</span>|<span class="number">0070</span>|        | (<span class="number">8</span>,<span class="number">0</span>) | (<span class="number">8</span>,<span class="number">8</span>) ||(<span class="number">8</span>,<span class="number">16</span>) | (<span class="number">8</span>,<span class="number">24</span>)|</span><br><span class="line">+====+====++====+====+       +====+====++====+====+        +=======+=======++=======+=======+</span><br><span class="line">|  <span class="number">8</span> |  <span class="number">9</span> || <span class="number">12</span> | <span class="number">13</span> |       |<span class="number">0080</span>|<span class="number">0090</span>||<span class="number">00</span>C0|<span class="number">00</span>D0|        |(<span class="number">16</span>,<span class="number">0</span>) | (<span class="number">16</span>,<span class="number">8</span>)||(<span class="number">16</span>,<span class="number">16</span>)|(<span class="number">16</span>,<span class="number">24</span>)|</span><br><span class="line">+----+----++----+----+       +----+----++----+----+        +-------+-------++-------+-------+</span><br><span class="line">| <span class="number">10</span> | <span class="number">11</span> || <span class="number">14</span> | <span class="number">15</span> |       |<span class="number">00</span>A0|<span class="number">00B</span>0||<span class="number">00E0</span>|<span class="number">00F</span>0|        |(<span class="number">24</span>,<span class="number">0</span>) | (<span class="number">24</span>,<span class="number">8</span>)||(<span class="number">24</span>,<span class="number">16</span>)|(<span class="number">24</span>,<span class="number">24</span>)|</span><br><span class="line">+----+----++----+----+       +----+----++----+----+        +-------+-------++-------+-------+</span><br></pre></td></tr></table></figure></p>
<p>invocation 索引为 id，所属分组编号计算方式如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">teamID = (id &amp; <span class="number">0x00F0</span>) &gt;&gt; <span class="number">4</span>;</span><br></pre></td></tr></table></figure>
<p>当前分组在输入 tile 中的偏移量为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">tx = (teamID &amp; <span class="number">2</span>) &lt;&lt; <span class="number">2</span> | (teamID &amp; <span class="number">8</span>) &lt;&lt; <span class="number">1</span>;</span><br><span class="line">ty = (teamID &amp; <span class="number">1</span>) &lt;&lt; <span class="number">3</span> | (teamID &amp; <span class="number">4</span>) &lt;&lt; <span class="number">2</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>当前 invocation 在其所属分组的偏移量：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">x = (id &amp; <span class="number">2</span>) | (id &amp; <span class="number">8</span>) &gt;&gt; <span class="number">1</span>;</span><br><span class="line">y = (id &amp; <span class="number">1</span>) &lt;&lt; <span class="number">1</span> | (id &amp; <span class="number">4</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>最终得到 <span class="math inline">\(L+1\)</span> 层样本在 <span class="math inline">\(L\)</span> 层对应的 <span class="math inline">\(2\times 2\)</span> block 的起始偏移量：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xBlockOffset = xOffset + tx + x;</span><br><span class="line">yBlockOffset = yOffset + ty + y;</span><br></pre></td></tr></table></figure></p></li>
</ul>
<p>得到 (xBlockOffset, yBlockOffset) 后，与 4-levels
算法类似，在每个分组内分别执行 3-levels 算法。每组生成一个 <span class="math inline">\(L+3\)</span> 层级样本，且组内第一个线程
<code>(id &amp; 0x00F0)==id</code> 负责写入 shared memory 中索引 teamID
的位置。workgroup 共生成 16 个 <span class="math inline">\(L+3\)</span>
层级样本，得到一个 4x4 大小的 shared memory。</p>
<p>为了能够继续使用 subgroup 特性，使用一个 subgroup
来生成最后两层。假设使用 ID 为 0~15 的 invocation，ID 用来索引 shared
memory 中的样本。shared memory
中的数组分为四部分，0<sub>3、4</sub>7、8<sub>11、12</sub>15，每个部分包含
4 个 <span class="math inline">\(L+3\)</span> 层级样本。
<code>id == id &amp; 0x000C</code> 的 invocation 负责收集 4 个 <span class="math inline">\(L+3\)</span> 层级样本，生成 1 个 <span class="math inline">\(L+4\)</span> 层级样本。最后 0 号 invocation 通过
<code>subgroupShuffleXor</code> 收集其他 3 个 <span class="math inline">\(L+4\)</span> 层级样本，生成最终的 1 个 L+5
层级样本。</p>
<h1 id="宽高为非-2-的次幂">宽高为非 2 的次幂</h1>
<p>对于图像宽高不是 2 的次幂的情况，<span class="math inline">\(L+1\)</span> 层级的样本在 <span class="math inline">\(L\)</span> 层级对应的 block 可能是 <span class="math inline">\(1\times 1\)</span>、<span class="math inline">\(1\times 2\)</span>、<span class="math inline">\(2\times 1\)</span>、$2$2、<span class="math inline">\(2\times 3\)</span>、<span class="math inline">\(3\times 2\)</span>、<span class="math inline">\(3\times 3\)</span> 等。在划分为 tile
计算之后层级样本时，某些样本会用到多次，如下述 <span class="math inline">\(5 \times 5\)</span> 大小的 mip level 生成 <span class="math inline">\(2\times 2\)</span> 大小的 mip
level，其中数字表示对应样本被使用到的次数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">+---+---+---+---+---+        +---------+---------+</span><br><span class="line">|   |   | <span class="number">2</span> |   |   |        |         |         |</span><br><span class="line">+---+---+---+---+---+        |         |         |</span><br><span class="line">|   |   | <span class="number">2</span> |   |   |        |         |         |</span><br><span class="line">+---+---+---+---+---+        |         |         |</span><br><span class="line">| <span class="number">2</span> | <span class="number">2</span> | <span class="number">4</span> | <span class="number">2</span> | <span class="number">2</span> | =====&gt; +---------+---------+ (each sample generated with <span class="number">3</span>x3 kernel)</span><br><span class="line">+---+---+---+---+---+        |         |         |</span><br><span class="line">|   |   | <span class="number">2</span> |   |   |        |         |         |</span><br><span class="line">+---+---+---+---+---+        |         |         |</span><br><span class="line">|   |   | <span class="number">2</span> |   |   |        |         |         |</span><br><span class="line">+---+---+---+---+---+        +---------+---------+</span><br></pre></td></tr></table></figure>
<p>这意味着，无法像前述 2 的次幂的算法那样，每个 workgroup 负责的输入
tile，不会和其他 workgroup 有任何交集。这种由非 2
的次幂引入的多次引用，会导致一些 workgroup 生成下一层级样本时，
也可能使用到其他 workgroup 生成的样本。为了提高并行性，去除不同
workgroup 之间的同步，可以推算出生成 <span class="math inline">\(M\)</span> 层级所需的输入层级的大小范围。在划分
workgroup 时，不同 workgroup 之间的输入 tile 允许有重叠部分。</p>
]]></content>
      <categories>
        <category>Vulkan Basics</category>
      </categories>
  </entry>
  <entry>
    <title>Compute Shader 基础</title>
    <url>/Vulkan-Basics/Compute-Shader-%E5%9F%BA%E7%A1%80.html</url>
    <content><![CDATA[<h1 id="基础概念">基础概念</h1>
<h2 id="gpu-中的概念">1. GPU 中的概念</h2>
<h3 id="处理器核心的层级概念">1.1 处理器核心的层级概念</h3>
<p>GPU 具有非常多的处理器核心，在 Nvidia 中称为 Streaming
Multiprocessor(SM)，AMD 称为 Compute Unit(CU)，以下使用 SM。在 SM
内部包含最基本的处理单元 lane，lane 可以近似理解为一个线程。SM
进行调度执行任务时，最小单位并不是 lane，而是 Warp(Nvidia 中的称呼，AMD
称为 wavefront)。一个 Warp 包含一定数量的 lane，在 Nvidia 中是 32，AMD
是 64。不同的架构，一个 SM 中包含的 warp 数量不同，目前的 Nvidia GPU
中一个 SM 包含 4 个 warp，即 128 个 lane(shader core、cuda core等)。GPU
显卡型号不同，GPU 包含的 SM 数量也不同，3080Ti 包含 80 个 SM。</p>
<p>在同一个 Warp 中的所有 lane
执行相同的指令，但可以处理不同的数据，这样就构成了现代 GPU 的 SIMD
机制。但一个 Warp 中的所有 lane 并不总是同时处于运行状态，即 active
lane；也并不总是同时处于非运行状态，即 inactive lane。例如：</p>
<ul>
<li><p>下发的任务组包含的任务数量较少或者不是 warp
大小的整数倍，不足以填满整个 warp 的 lane，那么未分配任务的 lane 则处于
inactive 状态。下发的任务组在 vulkan 中称为 local workgroup(D3D 中的
thread group) 大小。</p></li>
<li><p>Dynamic Branching 会导致 lane
的执行路径不止一条，即不止一套指令，这一定程度上破坏了 SIMD
机制。例如，代码中包含 if-else 语句，不满足 if 条件的 lane 由于 lockstep
运行方式需要等待其它所有满足 if 条件的 lane 执行 if 部分，等待中的 lane
则处于 inactive 状态。反之，满足 if 条件的 lane 也会等待不满足 if 条件的
lane 执行 else 部分。 但 if-else 并不总是导致这种指令分歧，对于非常简单
if-else，GPU 能够使用同一套指令实现，例如如下代码</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">uint</span> d = buff_array[...]; </span><br><span class="line"><span class="type">uint</span> c;</span><br><span class="line"><span class="keyword">if</span>(d &lt; some_value)&#123;</span><br><span class="line">   c = <span class="number">0</span>;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">   c = <span class="number">1</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可能会被转换为下述伪代码描述的指令，注意
<code>cmp_and_choose</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">register_a = 0;</span><br><span class="line">register_b = 1; </span><br><span class="line">register_d = load_from(buff_ptr_offset);</span><br><span class="line">register_some_value = ... ;</span><br><span class="line">register_c = cmp_and_choose(register_d, register_some_value, register_a, register_b);</span><br></pre></td></tr></table></figure></li>
</ul>
<p>因此，在执行之前，先将每个 local workgroup 中的线程划分为以 warp
为单位的子组，之后 GPU 以 warp 为单位进行调度执行。</p>
<h3 id="存储类型与数据同步">1.2 存储类型与数据同步</h3>
<p>以上描述的 SM、warp、lane
具有不同类型的存储，下面根据速度由快到慢依次说明，这里的参数数据以
3080Ti (Ampere-GA102架构)为例 <a href="#%5B1%5D">[1]</a></p>
<ul>
<li><p>register：每个 SM 具有256KB 大小的寄存器区。</p></li>
<li><p>shared memory/L1 cache：一个 SM 中的所有线程可以访问该 SM
具有有限大小的 shared memory。shared memory
本质上是一个较小的读写缓存区，GA102 架构中为
128KB，但其中只有一部分可被程序员使用，其他用作缓存或其他用途，例如 48KB
用作 shared memory。</p></li>
<li><p>global memory(VRAM)：global memory 即 GPU
显存。如果寄存器存储或者shared memory
的使用大小溢出，则会将其中的数据写入显存中，这一点和 CPU
的高速缓存-内存机制类似。</p></li>
</ul>
<p>这三种类型的存储除了存取速度差距大之外，还有工作机制上的区别：</p>
<ul>
<li>首先，一个 SM 的 shared memory 对另一个 SM 上的线程是不可见的，即
shared memory 只用于同一 SM 的线程的数据同步与共享。</li>
<li>其次，shared memory 与 register 虽然都属于 SM，但只有 shared memory
中的数据可以在 SM 中的 lane 之间共享。register
一般用于暂存指令执行过程的输入输出，lane/线程执行中用到的 register
相当于是私有的，对于其他线程是不可见的。因此，一般情况下，同一 SM
中的线程共享数据需要额外花费 register 到缓存(shared memory)
的传输时间，以及占用了带宽。</li>
<li>最后，对于同一 SM 中正在执行的线程之间，GPU
提供了直接通过寄存器共享数据的机制。缓存与寄存器的存取速度相差很多，因此这会带来更高效的数据同步。各大图形
SDK 也对此实现了相应 API，如后续描述的 vulkan 中的 subgroup 概念。</li>
</ul>
<h2 id="vulkan-中的概念">2. Vulkan 中的概念</h2>
<p>以上讲述了，一些硬件层面的概念，包括
SM(处理器核心)、Warp(处理器核心中同时执行的线程组)、Lane(相当于线程)。在图形
SDK 中，有相应的软件层面的概念。</p>
<ul>
<li><p>invocation：一个下发的并行任务，对应一个线程。</p></li>
<li><p>local workgroup：在同一 SM 上执行的 invocation 组，面向一个
SM。声明在 compute shader 中，如</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">layout</span>(<span class="keyword">local_size_x</span> = X, <span class="keyword">local_size_y</span> = Y, <span class="keyword">local_size_z</span> = Z) <span class="keyword">in</span>;</span><br></pre></td></tr></table></figure>
<p>上述声明表示，当前 compute shader 描述的 invocation 的 local
workgroup 大小为 <span class="math inline">\(X\times Y \times
Z\)</span></p></li>
<li><p>subgroup：local workgroup 中同时并行执行的 invocation
组，对应一个 Warp。如上述 Warp，subgroup 中的 invocation 同样有 active
和 inactive 两种状态，且具有相同的机制。</p></li>
<li><p>global workgroup：一次下发并行任务的 API 调用生成的所有
invocation，面向整个 GPU。通过 <code>vkCmdDispatch</code> 指定 local
workgroup 的维度与对应维度大小，如下 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">vkCmdDispatch</span><span class="params">(VkCommandBuffer cmdBuf, <span class="type">uint32_t</span> groupCountX, <span class="type">uint32_t</span> groupCountY, <span class="type">uint32_t</span> groupCountZ)</span></span>;</span><br></pre></td></tr></table></figure></p>
<p>上述调用表面 global workgroup 中具有 (groupCountX x groupCountY x
groupCountZ) 个 local workgroup，因此共有 (groupCountX x groupCountY x
groupCountZ) x <span class="math inline">\(X\times Y \times Z\)</span>
个 invocation/线程。</p></li>
</ul>
<p>Compute shader 的执行过程可以简要描述为：<code>vkCmdDispatch</code>
命令生成了一批 local workgroup，GPU 会将这批 local workgroup 划分到某些
SM 上执行，GPU 上的所有 SM 以并行形式执行。在 local workgroup
执行期间，一般不会离开其执行于的 SM。local workgroup 包含多个
invocation，GPU 的最小调度单位是 Warp，因此一个 local workgroup
会先被划分为一个或多个 subgroup(s)，每个 subgroup 的 invocation
可以并行执行。</p>
<p>global workgroup 中 local workgroup 的数量可以超过 GPU 中 SM
的数量，因此会有多个 local workgroup 被分配到同一个 SM 上执行。同一 SM
上的 subgroup 会在执行过程中发生切换，例如，在一个 subgroup
等待比较耗时内存访问操作完成时，为了隐藏延迟，会切换执行另一个
subgroup，这种切换也可能会发生在同一 SM 上的不同 local workgroup 的
subgroup 之间。假设 invocation 使用寄存器资源或 shared memory
过多，不足以满足切换的 subgroup，那么会有寄存器数据备份至缓存或 shared
memory 备份至显存的操作，这样会带来性能上的影响。同样，local workgroup
的 subgroup 数量也可以超过一个 SM 中的 Warp 数量。</p>
<h2 id="compute-shader-中的内置变量">3. Compute Shader 中的内置变量</h2>
<p>Compute Shader 没有 in/out 参数，只能使用 SSBO/Texture
资源进行读写。对于 Texture，compute shader 只能使用 image
类型，不支持采样且一个 image 类型参数只能使用一个 level。在访问 image
时，只能使用整数索引。</p>
<p>Compute Shader
经常用于处理高维数据，如贴图。有一些内置变量可以作为高维数据的索引，</p>
<ul>
<li><p><code>uvec3 gl_NumWorkGroups</code>：传递给 dispatch 函数的 group
数量，即 (groupCountX, groupCountY, groupCountZ)</p></li>
<li><p><code>uvec3 gl_WorkGroupID</code>：当前 invocation 所属的 local
workgroup 的编号，范围 [0, gl_NumWorkGroups.XYZ)</p></li>
<li><p><code>uvec3 gl_LocalInvocationID</code>：当前 invocation 在其
local workgroup 内的编号，范围是 [0, gl_WorkGroupSize.XYZ)，其中
gl_WorkGroupSize=(local_size_x, local_size_y, local_size_z)</p></li>
<li><p><code>uvec3 gl_GlobalInvocationID</code>：当前 invocation 在
global workgroup 中的编号，等于</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="built_in">gl_WorkGroupID</span> * <span class="built_in">gl_WorkGroupSize</span> + <span class="built_in">gl_LocalInvocationID</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>uint gl_LocalInvocationIndex</code>：相当于
gl_LocalInvocationID 的一维形式，等于</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="built_in">gl_LocalInvocationIndex</span> =</span><br><span class="line">          <span class="built_in">gl_LocalInvocationID</span>.z * <span class="built_in">gl_WorkGroupSize</span>.x * <span class="built_in">gl_WorkGroupSize</span>.y +</span><br><span class="line">          <span class="built_in">gl_LocalInvocationID</span>.y * <span class="built_in">gl_WorkGroupSize</span>.x + </span><br><span class="line">          <span class="built_in">gl_LocalInvocationID</span>.x;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>与 subgroup 相关的内置变量：</p>
<ul>
<li><code>gl_NumSubgroups</code>：local workgroup 包含的 subgroup
的数量</li>
<li><code>gl_SubgroupID</code>：当前 subgroup 在 local workgroup
中的编号，范围是 [0, gl_NumSubgroups)</li>
<li><code>gl_SubgroupSize</code>：subgroup 的大小</li>
<li><code>gl_SubgroupInvocationID</code>：当前 invocation 在 subgroup 中
ID，范围是 [0, gl_SubgroupSize)</li>
<li><code>gl_SubgroupEqMask</code>, <code>gl_SubgroupGeMask</code>,
<code>gl_SubgroupGtMask</code>, <code>gl_SubgroupLeMask</code>,<br>
<code>gl_SubgroupLtMask</code>：subgroupBallot 相关</li>
</ul>
<p>有关 gl_LocalInvocationID 与 gl_SubgroupInvocationID
的关系，没有找到有文档说明，但我相信应该与
gl_GlobalInvocationID、gl_LocalInvocationID之间的关系类似。因为 GPU
的最小调度单位是 subgroup/Warp 而不是 invocation/线程。在调度之前 local
workgroup 就应该已经被划分为 subgroup。此时，subgroup 中的所有
invocation 都对于 GPU 而言是相同的指令序列。subgroup
内的分歧应该发生在实际执行过程中。因此，gl_LocalInvocationID 和
gl_SubgroupInvocationID
之间具有事先确定的关系应该不会对后续调度产生任何影响。但是如果没有确定的关系，那么subgroup
功能的使用将会受到很大的限制。经过在 RTX 3080ti
上测试，gl_LocalInvocationID 与 gl_SubgroupInvocationID
有如下数值关系，</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line">gl_SubgroupID * gl_SubgroupSize + gl_SubgroupInvocationID == <span class="built_in">gl_LocalInvocationIndex</span> </span><br></pre></td></tr></table></figure>
<p>或者</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="built_in">gl_LocalInvocationIndex</span> % gl_SubgroupSize == gl_SubgroupInvocationID </span><br></pre></td></tr></table></figure>
<h3 id="内存模型术语">内存模型术语</h3>
<h4 id="coherent-or-incoherent-memory-access-2">1. Coherent or
Incoherent Memory Access <a href="#%5B2%5D">[2]</a></h4>
<ul>
<li>Coherent 内存访问</li>
</ul>
<p>以最简单的例子——单处理器系统来说，对于同一内存区域同时只会有一个线程访问。因此，当一个处理元件对某一内存区域写，然后另一处理元件对同一内存区域读时，总能读到更新后的值。这是我们想要的结果，但得到这一正确结果并不只是多线程读写同步。因为，在硬件层有高速缓存-内存机制，处理器的读写往往不直接针对内存，而是针对缓存。缓存中的数据是内存中数据的拷贝或者是写操作得到的更新数据，因此需要确保写操作之后的读操作能够读到新数据，而非旧数据，这就是
<strong>Coherent
内存访问</strong>。单处理器系统的内存操作工作于同一套高速缓存-内存机制中，保证
Coherent 内存访问较容易。</p>
<ul>
<li>Incoherent 内存访问</li>
</ul>
<p>但对于多处理器系统，每个处理器都有其本地缓存，当多个处理器同时访问同一内存区域时，同一内存区域的数据在这些处理器的缓存中都有一个备份。此时可能存在处理器读到的时本地缓存中的旧数据，而不是其他处理器写操作生成的新数据，即出现
<strong>Incoherent
内存访问</strong>。为了避免这种情况，多处理器系统的设计需要采用存储器一致性协议，粗略地说，当一个处理器对某一内存区域更新时，对该内存区域存在缓存备份的其他处理器也要对其本地缓存的相应位置进行更新。</p>
<h4 id="visibility-3">2. Visibility <a href="#%5B3%5D">[3]</a></h4>
<p>GPU 的核心数量远高于 CPU，在 GPU
的执行过程中，不同处理器同时读写同一内存区域时，也会出现上述 Incoherent
内存访问情况。而 <strong>Visibility</strong> 术语就是指一个 shader
invocation 可以安全地读其他 shader invocation 的 Incoherent
写数据，也就是说，不会读取到缓存中旧的备份，或者其他 invocation
写数据对其是可见的。</p>
<p>对于多个 shader invocation 同时读写不同的区域，不会出现上述
incoherent 访问情况。此外，对于在同一处理器执行的 shader
invocation，不会出现 incoherent
情况，因为使用的是同一套缓存-内存机制，例如 compute shader 中的同一
local workgroup 中的 invocation。特别注意，这里不会出现 incoherent
访问问题，并不是指读写同步问题，读写同步同样需要额外处理。当不同处理器上的
shader invocation 对同一内存区域进行读写时，则会出现 incoherent
内存访问。</p>
<p><a href="#%5B5%5D">[5]</a> 提到 incoherent 的内存访问操作有：</p>
<ul>
<li>对 image 变量的 imageLoad/imageStore</li>
<li>对 buffer 变量的读写操作</li>
<li><a href="https://www.khronos.org/opengl/wiki/Atomic_Counter">Atomic
Counters</a></li>
<li>对 compute shader 中的 shared 变量的读写操作。这个无法理解，shared
变量存储在缓存中，不应该会有多个备份的情况，为什么还是 incoherent？</li>
</ul>
<p>这时需要考虑如何避免 incoherent 内存访问，即确保 visibility
性质。这里有两种情况：</p>
<h5 id="internal-visibility一个绘制指令执行内部一部分写而另一部分读">2.1
Internal Visibility：一个绘制指令执行内部，一部分写、而另一部分读。</h5>
<ul>
<li>读写次序控制、内存控制</li>
</ul>
<p>想要使得一个 shader invocation 能够读取另一 shader invocation
写数据，要先保证写操作在读操作之前确实发生，也就是读写同步问题。例如
compute shader 中 <code>barrier</code> 函数，可以确保 local
workgroup(执行在同一处理器) 中的所有 invocation 都执行到 barrier
同步点后，才开始执行之后的代码。</p>
<p>注意 barrier 函数只是对 local workgroup 中的 invocation
的执行过程进行了控制，这种控制只发生在同一个处理器上。对于 shared memory
缓存只会在同一个处理器上共享，因此 barrier 同时也能够做到 shared memory
的读写次序控制。在 compute shader 中 shared 变量即位于 shared memory
中，由于本身就是缓存，不会出现多个备份情况，因此 shared 变量是隐含的
coherent 访问。这一点在 <a href="#%5B6%5D">[6]</a> 的 1.1.2
小节也有说明</p>
<blockquote>
<ul>
<li>Private GLSL issue #24: Clarify that <strong>barrier</strong>() by
itself is enough to synchronize both control flow and memory accesses to
<strong>shared</strong> variables and tessellation control output
variables. For other memory accesses an additional memory barrier is
still required.</li>
</ul>
</blockquote>
<ul>
<li>内存访问控制 <a href="#%5B4%5D">[4]</a></li>
</ul>
<p>确保读写次序，相当于确保了在读操作之前，写操作一定已经发生。对于
coherent 内存访问而言，这已经确保了写数据的可见性；但对于 incoherent
内存访问，写操作发生不代表写数据对其他 invocation 可见，这会导致一个
invocation 的内存读写操作的相对次序对于另一个 invocation
而言是不确定的状态，换句话说，写操作的数据对其他 invocation
可见的次序不确定。例如一个 invocation 中执行两次写操作，而另一个
invocation 可能会先看到第二次写的数据，后看到第一次写的数据。</p>
<p>这时需要使用到 memory barrier 来控制读写操作，使得其他 invocation
看到写数据的次序与写操作执行的次序一致。先对可能被多个处理器访问到的
image 或 buffer 类型变量进行 <code>coherent</code> 修饰，声明该变量为
coherent 访问机制。该访问机制使得相应的 memoryBarrier
可以控制对被修饰变量的读写操作。调用 memoryBarrier 等函数的 invocation
会等待之前的所有读写操作完成，当该函数返回后，写数据对之后的访问处于可见状态。例如一个
invocation 执行两次写操作，每次写操作后都加上一个 memory
barrier，那么其他 invocation
就不可能先看到第二次写的数据，而后看到第一次写的数据。memory barrier
有多种类型：</p>
<ul>
<li><code>memoryBarrier</code>：控制所有类型变量的内存访问，render
command 内作用于全局。</li>
<li><code>memoryBarrierAtomicCounter</code>：控制 atomic-counter
变量的访问，render command 内作用于全局。</li>
<li><code>memoryBarrierBuffer</code>：控制 buffer 变量的内存访问，render
command 内作用于全局。</li>
<li><code>memoryBarrierImage</code>：控制 image 变量的内存访问，render
command 内作用于全局。</li>
<li><code>memoryBarrierShared</code>：控制 shared
变量的内存访问，作用于同一 workgroup。</li>
<li><code>groupMemoryBarrier</code>：控制所有类型变量的内存访问，作用于同一
workgroup。</li>
</ul>
<h5 id="external-visibility">2.2 External Visibility</h5>
<p>一个 render command 内部的 visibility 是 shader invocation
之间的读写操作。对于 render command 之间的 visibility 使用 barrier
命令进行同步。例如 vulkan 中的 buffer barrier、image layout 等</p>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a>
https://images.nvidia.com/aem-dam/en-zz/Solutions/geforce/ampere/pdf/NVIDIA-ampere-GA102-GPU-Architecture-Whitepaper-V1.pdf</p>
<p><a name="[2]">[2]</a>
https://en.wikipedia.org/wiki/Memory_coherence</p>
<p><a name="[3]">[3]</a>
https://www.khronos.org/opengl/wiki/Memory_Model#Ensuring_visibility</p>
<p><a name="[4]">[4]</a>https://www.khronos.org/registry/OpenGL/specs/gl/GLSLangSpec.4.60.html#shader-memory-control-functions</p>
<p><a name="[5]">[5]</a>
https://www.khronos.org/opengl/wiki/Memory_Model#Incoherent_memory_access</p>
<p><a name="[6]">[6]</a>
https://registry.khronos.org/OpenGL/specs/gl/GLSLangSpec.4.60.html#changes</p>
]]></content>
      <categories>
        <category>Vulkan Basics</category>
      </categories>
  </entry>
  <entry>
    <title>Real-Time Global Illumination using Precomputed Light Field Probes</title>
    <url>/Paper-Notes/GI/Real-Time-Global-Illumination-using-Precomputed-Light-Field-Probes.html</url>
    <content><![CDATA[<h2 id="summary">1 Summary</h2>
<p>这篇论文提出了一种基于light
probe的全局光照方法，其中包括预计算过程中probe数据的生成方式、基于probe数据的光线追踪算法以及实时渲染中的着色过程。</p>
<p>在预计算过程中，对场景的每个离散采样点生成以该位置为中心的probe数据，每个probe包含了球面分布的两组数据：probe中心收到每一方向上最近点的radiance、该最近点的法线以及该最近距离(radial
distance)；为了处理diffuse间接光，对probe中心收到的radiance进行prefilter得到的irradiance、最近距离及其平方。</p>
<p>同时，probe数据采用了具有分段线性投影特性的octahedral
map存储，作者提出了在基于此map的具有2-level层级加速的高效光线追踪算法。为了得到确定的求交结果，需要在着色点周围的八个probe中选择进行求交测试，为此，作者提出最小化octahedral
map访问次数的选取策略。</p>
<p>在实时渲染的着色过程中，直接光照选择采用传统的deferred
shading。对于间接光照，作者将BRDF拆分为Lambertian项和glossy项，并为每一项采样一条光线计算间接光照：前者由于其在分布空间变化平缓的特性，而选择采样irradiance
map，并可以使用较大的filter进行降噪；而后者在其分空间变化强烈，采样radiance
map，并使用较小的filter降噪。而对于降噪filter过程，除了应用传统的空间与时序的降噪，作者还提出使用probe中的几何信息来构建geometry-aware的权重，来避免light/shadow-leaking问题。</p>
<p>作者源码 <a href="#%5B3%5D">[3]</a></p>
<h2 id="light-field-probe-structure">2 Light Field Probe Structure</h2>
<p>Light Field(光场)描述了空间中一点的radiance在出射方向上的分布，假设点
<span class="math inline">\(\mathbf{x}\in
\mathcal{R}^3\)</span>，以及该点的一个出射方向 <span class="math inline">\(\omega\in \mathcal{S}^2\)</span>
(应该指上半球)，那么Light Field可使用 <span class="math inline">\(\mathcal{L}(\mathbf{x},\omega)\)</span>
表示。不可能对场景中每一点都定义Light
Field，作者通过将场景的包围盒均分为grid来将空间离散化，对每个grid的一个采样点定义一个light
field。</p>
<p>在每个离散采样点 <span class="math inline">\(\mathbf{x}&#39;\)</span>（每个grid的一个采样点）上存储一个球形light
field probe数据，用以记录点 <span class="math inline">\(\mathbf{x&#39;}\)</span> 的每个方向 <span class="math inline">\(\omega\)</span> 的数据，包括：</p>
<ul>
<li><span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>：点
<span class="math inline">\(\mathbf{x}&#39;\)</span> 在方向 <span class="math inline">\(\omega\)</span> 的入射radiance。light
field是在球面上的定义，而probe像素有一定大小，可以看作在球面上一片的radiance，因此文中称为spherical
light field slice radiance。</li>
<li>在方向 <span class="math inline">\(\omega\)</span> 上距点 <span class="math inline">\(\mathbf{x&#39;}\)</span> 最近的点 <span class="math inline">\(\mathbf{x&#39;&#39;}\)</span> 的表面法线：<span class="math inline">\(\vec{\mathbf{n_{x&#39;&#39;}}}\)</span></li>
<li><span class="math inline">\(\mathbf{x&#39;}\)</span> 与 <span class="math inline">\(\mathbf{x&#39;&#39;}\)</span> 之间的radial
distance <span class="math inline">\(r_{\mathbf{x&#39;}\leftrightarrow\mathbf{x&#39;&#39;}}\)</span></li>
</ul>
<h3 id="probe-数据生成方式">2.1 Probe 数据生成方式</h3>
<p>上述Probe数据中的几何信息，例如 <span class="math inline">\(\vec{\mathbf{n_{x&#39;&#39;}}}\)</span>、<span class="math inline">\(r_{\mathbf{x}&#39;\rightarrow\mathbf{x&#39;&#39;}}\)</span>，作者使用光栅化管线生成。而spherical
light field slice radiance <span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>
需要光线模拟过程来生成：可以通过光栅化过程现有的着色技术(deferred
renderer with shadow map)来计算；或者使用离线path
tracer；作者选择迭代应用本文提出的着色算法来生成，具体做法：对于一次反射的直接光照
<span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>
只包含场景中光源的发光，重复应用着色算法得到具有额外反射的 <span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>。</p>
<h3 id="实现细节">2.2 实现细节</h3>
<p>一个probe本质上是以其中心点 <span class="math inline">\(\mathbf{x&#39;}\)</span>
为球心的球面上所有方向的信息，类似于全向相机的渲染（Omnidirectional），其数据组织为cube
map的形式，每个像素的数据为其对应方向 <span class="math inline">\(\omega\)</span>
的probe数据。本文提出的算法中不直接使用cube
map，而是在生成probe数据过程中，将cube map重采样到octahedral
map(oc-map)中 <a href="#%5B1%5D">[1]</a> <a href="#%5B2%5D">[2]</a> <a href="#5.1">[5.1]</a>，之后的算法就不再需要cube
map。八面体参数化是将球面映射到一个方形平面上，相比于cube
map，oc-map具有优势：失真度更低；同等质量水平下，存储开销与带宽降低4倍；分段线性的投影能够应用于高效的ray
marching。</p>
<p>每个probe的oc-map数据作为2D texture
array的一个layer存储，一个oc-map像素大小为32字节，其存储格式为：</p>
<ul>
<li>HDR radiance <span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>：R11G11B10F
(32 bits/pixel)</li>
<li>法线 <span class="math inline">\(\vec{\mathbf{n_{x&#39;&#39;}}}\)</span>：RG8 (16
bits/pixel)</li>
<li>radial distance <span class="math inline">\(r_{\mathbf{x}&#39;\rightarrow\mathbf{x&#39;&#39;}}\)</span>：R16F
(16 bits/pixel)</li>
</ul>
<p>irradiance probe：很多着色过程得益于prefilter light field radiance
<span class="math inline">\(\mathcal{L}(\mathbf{x&#39;},\omega)\)</span>，因此将prefilter结果预存储到一个cosine-weighted
incident irradiance probe <span class="math inline">\(\mathbf{E}(\mathbf{x&#39;},\vec{\mathbf{n}})\)</span>，即点
<span class="math inline">\(\mathbf{x&#39;}\)</span> 处的法线方向 <span class="math inline">\(\vec{\mathbf{n}}\)</span>
上收到的radiance积分。例如，Lambertian项描述的漫反射项。作者发现irradiance
probe的oc-map的分辨率高于 <span class="math inline">\(128\times
128\)</span> 后就不会再有精确度方面的收益了。irradiance
probe的数据存储格式和上述基本相同，只是将radiance替换为irradiance，法线替换为radial
distance的平方，之后用于降噪过程的切比雪夫测试。</p>
<p>radiance
probe需要的分辨率取决于渲染分辨率和场景中的最低粗糙度，避免完美镜面反射的走样。为此需要满足八面体纹素对着的立体角不大于probe中心位置的像素对着的立体角。文中建议1024x1024的分辨率。</p>
<blockquote>
<p>场景中不同区域在屏幕上的像素比例不同，例如离相机远的区域占用像素更少、粗糙度越低对应的立体角越小。如果probe分辨率不足，会导致八面体纹素对应的立体角大于像素对应的立体角，像素采样到的radiance信息超出了像素范围，即radiance采样率不足。</p>
</blockquote>
<h2 id="light-field-probe-ray-tracing">3 Light Field Probe Ray
Tracing</h2>
<p>已知光线方程 <span class="math inline">\(\mathbf{p}(t)=\mathbf{r}_o+t\cdot\mathbf{r}_d\)</span>，求解最近交点
<span class="math inline">\(\mathbf{p}(t&#39;)\equiv\mathbf{p}&#39;\)</span>。Light
field probe中的几何信息radial
distance和法线用于解决incoherent世界空间求交，radiance probe和irradiance
probe包含了所需的辐射量，交点处的出射radiance <span class="math inline">\(\mathcal{L}(\mathbf{p}&#39;,-\mathbf{r}_d)\)</span>和入射irradiance
<span class="math inline">\(\mathcal{L}(\mathbf{p}&#39;,\vec{\mathbf{n}}\equiv\vec{\mathbf{r}_d})\)</span>。</p>
<p>作者提出的Light Field ray
trace过程如下图：先选择一个候选probe，执行single-probe ray
trace，当得到miss或hit结果时，则求交结束。当无法得到求交确定结果时，选择下一个候选probe继续执行single-probe
ray trace。</p>
<blockquote>
<p>如果得到不确定的结果，则表示本次选择的probe缺少光线传播区域的信息，例如从probe视角来看，光线传播区域被遮挡，因此无法确定是miss或hit。</p>
</blockquote>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230201162511726.png" alt="image-20230201162511726" style="zoom: 67%;"></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230202104825030.png" alt="image-20230202104825030" style="zoom:67%;"></p>
<p>作者提出的 light field ray
tracing算法主要包括两方面：选择候选probe的启发式策略；probe的oc-map中的光线求交。</p>
<h3 id="single-probe光线求交">3.1 Single-Probe光线求交</h3>
<p>球面的八面体投影性质：将三维空间的线段映射为oc-map中的多段线(最多4段线)，其中每段线分布在oc-map的不同面；probe的三维球体被以probe中心为原点的坐标系分割成8个平面，分别对应了oc-map的8个面，而每段线的端点为三维空间的线段与三个坐标轴对齐面的交点在oc-map的投影。</p>
<p>probe的光线求交：首先计算出光线在oc-map投影的几组端点；然后沿着线段进行ray-march，使用当前位置与probe中心的radial
distance <span class="math inline">\(t_0\)</span>
以及当前位置在oc-map中的对应纹素存储的radial distance <span class="math inline">\(t_1\)</span> 做求交测试，</p>
<ul>
<li>如果 <span class="math inline">\(t_0&gt;t_1\)</span>
表示光线交于一个表面(hit)或者从一个表面后面通过(unknown，光线行进区域在probe中被遮挡)</li>
<li>否则miss</li>
</ul>
<blockquote>
<p>probe的光线求交与SSR思想类似，不同的是：</p>
<ul>
<li>光线在depth
buffer中的投影是一条直线，而在oc-map中的投影是几段线</li>
<li>对于光线行进位置处的求交测试：SSR使用相机视角的depth
buffer在行进位置处的深度值与相机到行进位置距离，而本文的probe求交则使用oc-map在行进位置处的radial
distance与probe中心到行进位置的radial distance</li>
</ul>
</blockquote>
<p>对于oc-map的一个面内ray-march可以使用hierarchy算法加速 <a href="#%5B4%5D">[4]</a>。上述求交测试中，当 <span class="math inline">\(t_0 &lt; t_1\)</span> 可以确定miss，但 <span class="math inline">\(t_0 &gt; t_1\)</span>
有可能hit，也有可能从后面通过(unknown)。为了得到确定结果，需要确定求交平面即oc-map的对应texel在三维空间的范围。oc-map的每个纹素描述的是距离probe中心
<span class="math inline">\(r_{\mathbf{p}&#39;\leftrightarrow
\mathbf{x}&#39;&#39;}\)</span> 并且法线为 <span class="math inline">\(\vec{\mathbf{n_{x}}&#39;&#39;}\)</span>
的一个小patch(surfel)。基于法线和表示精度，作者使用一个球形体素包围该patch。只有当前行进点与该体素相交时，才表示与光线与patch表面相交。法线也可以用作排除掉相对于光线方向的背面的交点。下图为光线求交过程的可视化，光线在如图oc-map投影得到三个线段</p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230202213146200.png" alt="image-20230202213146200" style="zoom:67%;"></p>
<center>
two-level层级结构的求交过程：黄色与绿色分别为粗粒度与细粒度的ray-march测试
</center>
<h3 id="probe选取策略">3.2 Probe选取策略</h3>
<p>对于任意光线起点 <span class="math inline">\(\mathbf{r}_o\)</span>
都会被其周围8个probe所描述立方体包裹，如下图中编号所示。light field ray
tracing首先选择中心距光线最近的probe进行求交。根据空间局部性原理，这8个probe往往具有相似的visibility信息，这一启发策略在深度高复杂高和probe密度低的情况下会失效。而首先选择最近的probe是因为光线在该probe的oc-map中的投影最短，这样会带来ray-march过程中访问texture的次数较少；同时，最近的probe更可能观察到光线行进区域而不被遮挡。</p>
<p><a name="Fig 3.2"></a></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230203133555805.png" alt="image-20230203133555805" style="zoom: 67%;"></p>
<center>
图3.2 光线起点周围的8个probe
</center>
<p>当single-probe hierarchy ray
march不能确定求交结果时，需要再选择一个probe执行求交过程。首先将光线起点移动到上一个确定求交结果的行进位置，<span class="math inline">\(\mathbf{r}&#39;_{d}=\mathbf{r}_o+t&#39;\mathbf{r}_d\)</span>
。如果新的光线起点进入了一个新的立方体范围，那么再次选择一个距离光线最近的probe。如果新的光线起点还在原立方体范围内，从未进行求交测试的probe中选择一个距离上一个probe最远的probe，这种策略同样是基于空间局部性原理。为此，作者设计了简单高效位运算，假设light
field ray tracing始于距光线最近的probe <span class="math inline">\(i\)</span>，那么下一次选择 <span class="math inline">\(i&#39;=(i+3)\space mod\space
8\)</span>，选择顺序如上图的有向曲线所示。</p>
<p><a href="#Fig%203.3">图3.3</a>使用像素颜色表示了该像素得到求交结果的probe编号，同样的颜色表示同一个probe。可以看出越近的像素，其遮挡信息越相近。注意：probe的选取策略只会影响性能，不会影响求交结果。</p>
<p><a name="Fig 3.3"></a></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230203182557809.png" alt="image-20230203182557809" style="zoom: 50%;"></p>
<center>
图3.3 probe选取可视化。像素颜色表示该像素发出的glossy
ray在颜色所表示编号的probe得到求交结果
</center>
<h3 id="性能说明">3.3 性能说明</h3>
<p>由于文中提出的probe选取策略会得到光线在所选probe的oc-map中的投影线段最短，而投影线段的缩短减少光线行进的步数，也就是减少oc-map的访问以及求交测试。这可以带来便利的内存-性能权衡：增大probe密度不仅能够提高准确度，也会由于缩短光线在oc-map的投影长度而提高性能。</p>
<p>在实现上，作者通过重新组织glsl中的branch，降低线程动态分支的开销。light
field ray tracing使用two-level
mipmap实现的层级加速，第2层分辨率是上一层的 <span class="math inline">\(1/16^2\)</span>。</p>
<h2 id="shading-with-light-field-probes">4 Shading with Light Field
Probes</h2>
<p><strong>原始的着色算法</strong>：光栅化直接可见表面后(deferred
shading)在pixel shader中使用shadow map计算直接光照；然后在同一pixel
shader中进行蒙特卡洛积分来估计间接光：基于重要性采样，发出 <span class="math inline">\(n\)</span> 个随机光线，每条光线对light
field进行采样得到对应入射方向上的incident
radiance。最后乘上cosine-weighted BRDF并使用采样分布和采样数 <span class="math inline">\(n\)</span> 归一化后得到积分估计。</p>
<p>上述原始算法可以捕获到静态和动态物体的间接效果，同时可以使用该算法<strong>再次渲染probe来逐步填充light
field
probe的在任意次反射之后的radiance分布</strong>。但该方法有两个局限：</p>
<ul>
<li><p>更高阶间接光照效果的视点偏差：当从light field
probe中采样radiance时，得到的是采样点到probe中心的radiance，与着色点是有一定差距的。</p>
<blockquote>
<p>在使用上一章节描述的光追算法，得到采样光线的反射点后，可将probe中心点到反射点的方向进行转换得到对应的oc-map的采样坐标，采样radiance
map得到的是probe中心收到反射点的入射radiance。</p>
</blockquote></li>
<li><p>性能：原始算法中需要很多光线数量来降低噪声，无法应用到实时渲染中</p></li>
</ul>
<p>但更高阶的光照效果的失真影响远远小于第一次反射，作者更关注性能问题。作者对原始着色算法进行近似与优化，使其更适用于现代实时渲染管线。</p>
<h3 id="spatial-temporal-radiance-denoising">4.1 Spatial-Temporal
Radiance Denoising</h3>
<p>实时渲染下能够支持的光线数量非常有限，应对噪声的通常做法是使用时序-空间上的降噪，即SVGF的思想。针对BRDF，作者将其分解为两项：描述diffuse的Lambertian项和描述specular的glossy项。间接光照的render
pass为每一项采样(cosine-weighting)一条光线，并将结果写入带噪声的Lambertian和glossy入射radiance
buffers。之后再在单独的denoise
pass进行双边过滤(cross-bilateral)和时序重投影降噪。</p>
<p>对于Lambertian项，均匀分布在法线方向的上半球范围，同样采样率水平下，噪声更大，但变化较小。因此可以直接采用存储积分后的irradiance，同时由于其变化较为平缓的特点，可以使用更大的filter，文中采用半径为12像素、步长为2像素的空间滤波与98%置信度的时序滤波。glossy项虽然噪声较小，但变化较为剧烈，因此使用更小的filter，采用半径为3像素的空间滤波与75%置信度的时序滤波，如
<a href="#Fig%204.1">图4.1</a> 所示。</p>
<p><a name="Fig 4.1"></a></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230213234756136.png" alt="image-20230213234756136" style="zoom:80%;"></p>
<center>
图4.1 使用一个采样光线的glossy indirect
buffer。左边为降噪前，右边为降噪后
</center>
<h3 id="irradiance-with-pre-filtered-visibility">4.2 Irradiance with
(Pre-)Filtered Visibility</h3>
<p>前文所述的预计算过程，通过蒙特卡洛计算radiance积分估计，得到probe中心位置的irradiance
oc-map(128X128)中，采样坐标为法线方向转换得到oc-map坐标。在运行时，作者通过在如<a href="#Fig%203.2">图3.2</a>的八个probe之间使用三线性插值来估计着色点处收到的irradiance。但该方法存在的缺陷：插值得到的irradiance会透过irradiance
probe立方体网格体积内的物体，从而造成漏光或者漏阴影，如<a href="#Fig%204.3">图4.3</a>所示。具体而言，在立方体网格体积内的着色点与一个或多个probe之间的可见性假设是无效的。</p>
<p>为此作者提出了一种geometry-aware方法来避免这些不合理现象。在预计算cosine-filtered
irradiance map时，采用cosine-power lobe的滤波方式，对radial
distance和其二次方进行滤波得到的filter
map，这中滤波方式避免了距离的离散值带来的锯齿，同时又不造成过度模糊，如<a href="#Fig%204.2">图4.2</a>所示。在运行时，结合基础的三线性插值的权重与额外的两项得到geometry-aware权重：</p>
<ul>
<li>着色点表面法线与其到probe的方向之间的clamp dot</li>
<li>local radial distance分布的切比雪夫可见性检验，方法与VSM<a href="#%5B5%5D">[5]</a>原理类似，分布方差为 <span class="math inline">\(\sigma^2=E[r]^2-E[r^2]\)</span>。</li>
</ul>
<p>最后累加结果使用8个probe权重之和归一化。</p>
<p><a name="Fig 4.2"></a></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230214001658030.png" alt="image-20230214001658030" style="zoom:67%;"></p>
<center>
图4.2 每一列为irradiance
probe的一组数据。行从上到下依次为：irradiance；过滤的radial
distance；过滤的二次距离
</center>
<p><a name="Fig 4.3"></a></p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230214233524659.png" alt="image-20230214233524659"></p>
<center>
图4.3 传统irradiance map的漏光缺陷，文中提出的irradiance map解决后的效果
</center>
<h2 id="appendix">5 Appendix</h2>
<h3 id="八面体参数化">5.1 <a name="5.1">八面体参数化</a></h3>
<p>cube map的八面体参数化过程：先将球面投影到正八面体上，然后再投影到
<span class="math inline">\(z = 0\)</span> 平面上，并将 <span class="math inline">\(z &lt; 0\)</span>
的部分进行翻转，最终形成一个二维正方形。如下图所示</p>
<p><img src="/images/Paper Notes/GI/Real-Time%20Global%20Illumination%20using%20Precomputed%20Light%20Field%20Probes.assets/image-20230301210633868.png" alt="image-20230301210633868" style="zoom:67%;"></p>
<center>
八面体参数化过程
</center>
<h2 id="reference">Reference</h2>
<p><a name="[1]">[1]</a> Cigolle, Zina H., et al. "A survey of efficient
representations for independent unit vectors." <em>Journal of Computer
Graphics Techniques</em> 3.2 (2014).</p>
<p><a name="[2]">[2]</a> https://zhuanlan.zhihu.com/p/384232048</p>
<p><a name="[3]">[3]</a>
https://github.com/Global-Illuminati/Precomputed-Light-Field-Probes</p>
<p><a name="[4]">[4]</a> McGuire M, Mara M. Efficient GPU screen-space
ray tracing[J]. Journal of Computer Graphics Techniques (JCGT), 2014,
3(4): 73-85.</p>
<p><a name="[5]">[5]</a> William Donnelly and Andrew Lauritzen. 2006.
Variance shadow maps. In Proceedings of the 2006 symposium on
Interactive 3D graphics and games (I3D '06). Association for Computing
Machinery, New York, NY, USA, 161–165.</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>GI</category>
        <category>Light Probe</category>
      </categories>
  </entry>
  <entry>
    <title>Dynamic Diffuse Global Illumination with Ray-Traced Irradiance Fields</title>
    <url>/Paper-Notes/GI/Dynamic-Diffuse-Global-Illumination-with-Ray-Traced-Irradiance-Fields.html</url>
    <content><![CDATA[<h2 id="summary">1 Summary</h2>
<p>本文提出了一种基于light probe的实时动态diffuse GI(DDGI)
算法。该算法的probe在世界空间下摆放，其数据完全实时生成，不需要预计算，因此支持具有动态光源与动态几何的场景。</p>
<p>DDGI算法主要包含了probe数据更新与probe-based shading：</p>
<ol type="1">
<li><p>probe数据更新：为场景中的每个probe生成 <span class="math inline">\(n\)</span>
条probe-update光线，光线与表面相交记为surfel，最终每个probe可以得到存储求交信息的surfel
buffer。使用probe-base shading方法进行surfel
shading，包含了sufel上的直接光照与从surfel周围probes中得到的间接光照。将surfel
shading以给定滞后系数blend到probe中。</p></li>
<li><p>probe-base shading：作者为surfel
shading与最终着色提出了统一的probe-base
shading，一共包含了两部分：直接光照pass生成的直接光照信息，注意作者为了简化面光源的模拟过程，使用一次反射的间接光作为面光源的直接光照，而其在直接光照过程中只会生成自发光信息；从probe数据中采样得到的间接光照信息。</p></li>
</ol>
<p>本文提出的probe-based
shading可以使用上一帧的光照信息与probe数据，实现了将多次反射的开销分摊到多帧。但这也会在visibility信息快速变化时，带来time-lag的异常现象。</p>
<h2 id="ddgi-probe-overview">2 DDGI Probe Overview</h2>
<p>和 <a href="#%5B1%5D">[1]</a>
类似，probe中同样存储球面分布的数据，包括几何信息和辐射度量数据，存储在两张oc-map中：</p>
<ul>
<li>距最近几何的距离及其平方，存储在16x16
oc-map中，数据格式为<code>RG16F</code></li>
<li>diffuse irradiance存储在8x8
oc-map中，数据格式为<code>R11G11B10F</code></li>
</ul>
<p>不同的是，在 <a href="#%5B1%5D">[1]</a> 中使用texture
array打包所有probe的oc-map；而本文采用将所有probe的每一张oc-map打包进一张贴图集，并使用一个像素的边界隔开，同时增加额外的padding来确保4x4对齐。其次本文的probe数据是实时计算得到的，因此能够实现真正动态全局光照。在每一帧，除了插值probe深度信息以适应场景几何的变化外，还能够高效地将更新的光线追踪照明混合到probe地图集。</p>
<h2 id="ddgi-probe更新">3 DDGI probe更新</h2>
<p>每一帧都按照如下几步更新probe数据：</p>
<ol type="1">
<li>为场景中的 <span class="math inline">\(m\)</span> 个active
probe都生成并发出 <span class="math inline">\(n\)</span> 条primary
rays，并将光线追踪得到的 <span class="math inline">\(n\times m\)</span>
求交信息存储到surfel buffer中</li>
<li>使用统一的着色方法对surfel buffer计算直接光照与间接光照</li>
<li>使用每个相交surfel得到的shading、距离及其平方结果以blending方式更新到
<span class="math inline">\(m\)</span> 个active probe的oc-map texel</li>
</ol>
<p>与probe相交的surfel着色计算依赖于前一帧的光照与probe数据，这样做有两个目的：这种方式可以将多次反射的计算分摊到多帧；以blending方式更新probe数据可以使得几何上与辐射度量上在随着时间的尖锐过渡变得平滑。相反，这种方式会在visibility剧烈变化的情况下，由于间接光照更新的延迟，表现出明显的变化。</p>
<h3 id="放置probe">3.1 放置probe</h3>
<p>probe在场景中的放置方式与 <a href="#%5B1%5D">[1]</a>
相同，在轴向均匀的三维grid的每个顶点上放置一个probe，对于每轴需要不同空间离散化的场景，我们可以设置scale参数来独立地缩放网格在一个轴上的间距。空间中每一点都与一个包含该点的grid相关联，即对应了一个带有8个probe的cage，如下图所示。而着色过程则需要在cage的probe中进行查询。为了能够充分采样局部光照的变化，需要尽量满足每个点都能有cage的8个probe，避免某些probe位于墙内而无效。为此，作者建议使用grid分辨率以及缩放参数来使得每个类似房间的空间中产生至少一个完整的cage。文中给出，在人类尺度的场景中，1~2米的grid间隔效果最好。</p>
<p><img src="/images/Paper Notes/GI/Dynamic%20Diffuse%20Global%20Illumination%20with%20Ray-Traced%20Irradiance%20Fields.assets/image-20230224010007700.png" alt="image-20230224010007700" style="zoom: 67%;"></p>
<center>
cage的8个probe [1]
</center>
<h3 id="生成与追踪probe-update光线">3.2 生成与追踪Probe-Update光线</h3>
<p>为了捕获场景中几何与光照的变化，作者选择 <span class="math inline">\(m\)</span> 个active probes，对每个active
probe均匀采样 <span class="math inline">\(n\)</span>
个球面方向，采样方式采用stochastically-rotated Fibonacci spiral
pattern与 <a href="#%5B1%5D">[1]</a> 类似。一个probe中心与这 <span class="math inline">\(n\)</span> 个方向可以生成 <span class="math inline">\(n\)</span> 条光线，作者将 <span class="math inline">\(m\)</span>
个probe的光线以thread-coherent方式排布，在一个batch内发出。经测试，这种组织方式要优于直接在Vulkan或DirectX的实时光追管线中的primary
shader发出光线。</p>
<blockquote>
<p>球面方向采样方式stochastically-rotated Fibonacci spiral pattern <a href="#%5B3%5D">[3]</a>以及光线组织方式需要看实现代码</p>
</blockquote>
<p>此外，作者还测试了active
probes选取场景中部分probe(相机周围给定半径的范围内)以及光线采样数量随相机距离增大而减少，但实验结果表明，虽然这些优化会带来性能收益，但优化参数依赖于场景，不同场景需要配置不同的参数，否则会带来artifact。在本文的实验中，作者统一采用了每帧更新probe、每个probe都为active以及为所有probe采样相同数量的光线。</p>
<h3 id="secondary-surfel-shading">3.3 Secondary Surfel Shading</h3>
<p>对于probe更新和最终渲染中的间接光，作者采用统一的着色模型。具体来说，计算全局光照的两个阶段：更新
<span class="math inline">\(m\times n\)</span>
Probe-Update光线采样的surfels着色；最终输出图像中相机下的像素着色。这两个阶段使用相同的着色例程，结合直接光照pass和使用probe数据的间接光照pass。也就是说，surfel的shading包含其直接光照与间接光照：</p>
<ul>
<li>surfel的直接光照：从直接光照pass生成直接光照数据中获取，当然surfel不一定可见</li>
<li>surfel的间接光照：从surfel附近的probes中进行采样插值得到间接光照信息，即probe-based
shading</li>
</ul>
<p>而probe更新与最终渲染之间的差异在于shading
queries，着色例程需要着色位置、法线以及观察方向作为输入。对于基于probe光追的surfel
shading更新，需要光线求交得到的surfel位置与法线，以及probe中心到surfel的观察方向。</p>
<h3 id="probe-surfel-upates">3.4 Probe Surfel Upates</h3>
<p>在surfel shading之后，可以得到 <span class="math inline">\(m\times
n\)</span> surfel
points中的每一个surfel的更新着色值、probe中心到surfel的距离及其平方，这些值需要更新到probe数据中。作者给出了alpha-blending的更新方式，设置
<span class="math inline">\(\alpha\)</span>
为滞后参数，用来控制更新至覆盖前一帧值得速度，那么更新速度为 <span class="math inline">\(1-\alpha\)</span>，文中采用 <span class="math inline">\(\alpha\in[0.85,0.98]\)</span>，有以下更新过程，
<span class="math display">\[
newIrradiance[texelDir]=lerp(oldIrradiance[texelDir], \\
\sum_{ProbeRays}(\max(0,texelDir\cdot rayDir)*rayRadiance),\alpha)
\]</span> 上式中可以看出，filtered irradiance是采用moment-based filtered
shadow query:confused:
直接计算得到的，避免了prefilter高分辨的入射radiance
map。这种平滑的入射irradiance被用于计算diffuse间接光照，同时，也可以选择维护一个更高频的irradiance
map用于glossy和specular的间接光。</p>
<ul class="task-list">
<li><label><input type="checkbox">技术细节：</label></li>
</ul>
<h2 id="shading-with-ddgi-probes">4 Shading with DDGI Probes</h2>
<h3 id="直接光照">4.1 直接光照</h3>
<p>点光源与方向光的直接光照采用variance shadow map(VSM)的deferred render
<a href="#%5B2%5D">[2]</a> 来计算。</p>
<p>对于面积光源的直接光照，作者使用一次反射的间接光照管线计算。间接光照管线中，一次反射的间接光是计算场景中直接光的一次反射，多次反射的间接光则是基于前一次反射的间接光计算得到。而对于面积光源的一次反射间接光是基于区域的自发光，而不是面积光源下的直接光照，因此计算得到的是面积光源的直接光照。<a href="#Fig%204.1">图4.1</a>
的最后一行给出了面积光源特殊处理的直观感受，直接光照结果只有面积光源区域的自发光，而没有照明场景中其他物体。</p>
<p>面积光源的特殊处理避免了对面积光源的直接光照的传统近似方式，例如在面积光源上采样多个点光源计算来近似面积光源的直接光照。除此之外，对面积光源的计算统一为本文提出的probe-base
shading技术，可以得到平滑的面积光阴影与反射。</p>
<p><a name="Fig 4.1"></a></p>
<p><img src="/images/Paper%20Notes/GI/Dynamic%20Diffuse%20Global%20Illumination%20with%20Ray-Traced%20Irradiance%20Fields.assets/image-20230226115136810.png" alt="image-20230226115136810"> <img src="/images/Paper%20Notes/GI/Dynamic%20Diffuse%20Global%20Illumination%20with%20Ray-Traced%20Irradiance%20Fields.assets/image-20230226115205828.png" alt="image-20230226115205828"></p>
<center>
图 4.1 第一列只有直接光照，第二列有完整的全局光照
</center>
<h3 id="diffuse间接光">4.2 Diffuse间接光</h3>
<p>为了弥补probe位置的入射irradiance没有考虑着色点周围的局部遮挡，作者使用SSAO得到的遮挡系数调制漫反射的出射radiance。但在更新probe过程中不会包含局部遮挡，间接光忽略该项的影响要远远小于直接可见表面的光照。</p>
<p>本文对间接漫反射的插值和采样技术结合了光线追踪与shadow
map的想法，提高对动态几何和光源的鲁棒性，与 <a href="#%5B1%5D">[1]</a>
中不同。具体来说，在得到包含着色点的cage上的8个probe的索引后，为每个irradiance
probe根据其相对于着色点的位置与方向计算插值权重。如<a href="#Fig%204.2">图4.2</a>中surfel X的着色过程的权重包括：使用表面法线
n 采样eight-probe cage的每个probe；使用X到P的方向 dir，为每个probe
P施加back-face权重；P中存储的(进行过filter)平均距离
r；为了避免visibility边界采样问题，作者会将X朝着法线方向施加一个偏移量。</p>
<p><a name="Fig 4.2"></a></p>
<p><img src="/images/Paper Notes/GI/Dynamic%20Diffuse%20Global%20Illumination%20with%20Ray-Traced%20Irradiance%20Fields.assets/image-20230226143129489.png" alt="image-20230226143129489" style="zoom:50%;"></p>
<center>
图4.2 surfel X的着色。
</center>
<p><a href="#Fig%204.3">图4.3</a>说明了这些加权阶段对最终渲染结果的影响，从最初的传统probe渲染开始，逐步加入权重项，强调每个权重如何消除artifacts：</p>
<ul>
<li>背面剔除位于着色点切平面下方的probe，设置一个soft
threshold，当着色点法线与其到probe的方向之间的点乘小于该阈值时，则剔除。</li>
<li>人类视觉系统对于黑暗区域的低强度照明的敏感性，作者降低了少于5%
irradiance范围的贡献</li>
<li>VSM中描述的mean- and variance-biased Chebyshev interpolants
:confused:</li>
<li>对着色点应用与着色点法线和其到probe方向成正比的bias偏移，避免visibility函数边界处不连续阴影的问题</li>
<li>使用上述权重与bias，根据着色点与probe中心的距离，执行三线性插值</li>
</ul>
<p><a name="Fig 4.3"></a></p>
<p><img src="/images/Paper Notes/GI/Dynamic%20Diffuse%20Global%20Illumination%20with%20Ray-Traced%20Irradiance%20Fields.assets/image-20230226145015593.png" alt="image-20230226145015593" style="zoom: 67%;"></p>
<center>
图4.3 封闭房间场景下的irradiance插值与采样。d~f逐渐加入权重
</center>
<p>技术细节：之前的工作使用 <span class="math inline">\(128\times 128
\times 6\)</span> 的高精度cube
map存储深度信息，而在应用本文的权重准则后，可以在不引入数值误差问题的情况下，降低至
<span class="math inline">\(16 \times 16\)</span> 的medium精度。</p>
<h3 id="多次反射的全局光照">4.3 多次反射的全局光照</h3>
<p>多次反射的计算是一个递归过程，每一次反射的间接光都是基于前一次反射的radiance
buffer。本文的间接光计算是分销在多帧的，因此会带来time-lag
artifact，这一点对于静态相机下的静态场景最明显，而对于视角、光源和几何是动态的场景，这种延迟在间接光反射是不易察觉的。</p>
<h2 id="reference">Reference</h2>
<p><a name="[1]">[1]</a> Morgan McGuire, Mike Mara, Derek
Nowrouzezahrai, and David Luebke. 2017. Real-time global illumination
using precomputed light field probes. In Proceedings of the 21st ACM
SIGGRAPH Symposium on Interactive 3D Graphics and Games (I3D '17).
Association for Computing Machinery, New York, NY, USA, Article 2, 1–11.
https://doi.org/10.1145/3023368.3023378</p>
<p><a name="[2]">[2]</a> Thaler, Jonathan. (2011). Deferred Rendering.
URL:
https://www.researchgate.net/profile/Jonathan_Thaler2/publication/323357208_Deferred_Rendering/links/5a8fce31aca272140560aaad/Deferred-Rendering.pdf</p>
<p><a name="[3]">[3]</a> Ricardo Marques, Christian Bouville, Mickaël
Ribardière, Luis Paulo Santos, Kadi Bouatouch. Spherical Fibonacci Point
Sets for Illumination Integrals. Computer Graphics Forum, 2013, 32 (8),
pp.134-143. ⟨10.1111/cgf.12190⟩. ⟨hal-01143347⟩</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>GI</category>
        <category>Light Probe</category>
      </categories>
  </entry>
  <entry>
    <title>Glossy Probe Reprojection for Interactive Global Illumination</title>
    <url>/Paper-Notes/GI/Glossy-Probe-Reprojection-for-Interactive-Global-Illumination.html</url>
    <content><![CDATA[<h2 id="summary">1 Summary</h2>
<p><code>Glossy Probe Reprojection for Interactive Global Illumination</code>
<a href="#%5B8%5D">[8]</a></p>
<p>这篇论文提出了一种基于 light probe
思想的实时全局光照算法，高效解决带有 glossy 路径的全局光照问题。light
probe 思想在场景中放置多个 light probe，每个 light probe
包含了该位置的间接光照信息，这些信息通过预计算过程存入 light
probe，并用于在实时渲染过程中计算着色点的全局光照。本论文提出自适应的
probe
参数化，通过评估不同角度、不同位置的几何信息，自适应不同情况所需要的
probe 分辨率，用以缓解 glossy
全局光照带来的高内存消耗问题。在实时渲染中提出基于 specular path
perturbation 的间接光反射点估计方法，高效 reproject glossy path
信息到实时的 novel view
space，进而收集到实时全局光照信息。同时论文改善了 occlusion boundaries
附近出现的硬边界问题，通过预计算过程中降低几何 roughness 来预计算 probe
data，以收集到更大范围的全局光照信息，在实时渲染中设计 filter 重构原
roughness，达到硬边界的平滑过渡。</p>
<h2 id="background">2 Background</h2>
<h3 id="light-probe">2.1 Light Probe</h3>
<ul class="task-list">
<li><p><label><input type="checkbox">light probe
算法思想</label></p></li>
<li><p><label><input type="checkbox">基于 3D-grid 的 light probe <a href="#%5B1%5D">[1]</a></label></p></li>
</ul>
<h2 id="approach">3 Approach</h2>
<h3 id="probe-预计算">3.1 Probe 预计算</h3>
<p>Probe 中存储实时渲染过程中需要使用的全局光照信息，通过 path-tracer
计算得到。glossy BRDF
需要较多的采样才能得到平滑的着色，如果在整个场景中的每个 probe
的所有方向都收集同样多样本的信息，会使得 probe
数据存储消耗过大。因此作者提出 adaptive probe
resolution，即根据不同位置、不同视角观察到的几何信息来评估该位置需要的
probe
resolution，高分辨率对应高样本数，低分辨率对应低样本数。先介绍一下本论文使用的
probe data 存储的数据</p>
<h4 id="per-probe-data">3.1.1 Per Probe Data</h4>
<p>预计算过程中，为每个 Probe 生成数据，包含三张贴图，如下图所示：</p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214054549.png" alt="image-20220717214054549" style="zoom:50%;"><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214112108.png" alt="image-20220717214112108" style="zoom:50%;"><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214125605.png" alt="image-20220717214125605" style="zoom:50%;"></p>
<ul>
<li><ol type="a">
<li>可见的三角形信息(三角形 ID、重心坐标)</li>
</ol></li>
<li><ol start="2" type="a">
<li>probe 空间的环境贴图(只存储 glossy BRDF 计算的颜色值)</li>
</ol></li>
<li><ol start="3" type="a">
<li>将每个面视为镜面，记录完美反射过程中的几何信息(反射点、及其材质
ID)</li>
</ol></li>
</ul>
<p>这三张贴图都是 360 度贴图，将 probe 作为相机，贴图中每个 texel
存储的是不同角度看向场景得到的信息，即 probe view 下的渲染信息。</p>
<h4 id="probe-parameterization">3.1.2 Probe Parameterization</h4>
<p>对于有限的内存消耗，probe 数量是有限的，这篇论文提出的 adaptive probe
resolution 是为了将更多地 probe resolution
分配到需要高采样的区域(因为同样的 fov 下，probe
数据的分辨率越高，包含采样的样本越多)。作者设计了一种 probe
需求的评估策略，评估结果为一个方向看到的场景所需的 probe resolution
参数。根据评估得来的 probe resolution 不断调整 probe 对应 3D grid
的等划分网格，最终达到网格大小与 probe resolution 相匹配，即 probe
resolution 越大，网格越大。对网格中像素对应的场景角度，使用 path tracer
计算 probe 离线数据。</p>
<p><strong>如何确定什么情况需要较高的 probe resolution 或较低的 probe
resolution？</strong></p>
<ul>
<li><ol type="a">
<li>因为光滑表面频率信息较高，因此越光滑的表面需要越高的分辨率，反之粗糙的材质频率较低，需要较少的样本</li>
</ol></li>
<li><ol start="2" type="a">
<li>远距离表面或者 grazing 角度看到的表面需要较高的分辨率</li>
</ol></li>
<li><ol start="3" type="a">
<li>高频几何(法线)信息在 reflected radiance
上表现出很大的变化，因此需要较高的分辨率。</li>
</ol></li>
</ul>
<p>根据上述要求，论文使用 lat-long (相当于球坐标) 对 probe
分辨率参数化。在 probe view space 中，计算每个角度对应的 probe
resolution 参数，并存储到 adaptive resolution map 中。lat-long
参数化易于确定 adaptive resolution map 的每个像素对应的 ray
direction，方便 probe data预计算过程的 ray-tracing。</p>
<h5 id="adaptive-resolution-map-计算">3.1.2.1 Adaptive Resolution Map
计算</h5>
<p>一个 Adaptive resolution map 的元素表示 probe 每个方向对应的 probe
resolution 参数。计算该 Map 前先使用以 probe 出发的 ray casting 构建 4
个 256x128 的 buffers，(i) 材质 smoothness <span class="math inline">\(\large m_{mat}\)</span>，(ii) 深度值 <span class="math inline">\(\large m_{depth}\)</span>，(iii) 法线 <span class="math inline">\(\large m_{norm}\)</span>，(iv) facing angle <span class="math inline">\(\large m_{face}\)</span>
(入射光方向与表面法线点乘)。</p>
<p>Adaptive resolution map 的元素计算：</p>
<p><span class="math display">\[
m=m_{mat}(m_{size}+m_{complexity})
\]</span> 可以看出</p>
<ul>
<li><p><span class="math inline">\(m_{mat}\)</span>​ 满足了 a)，越光滑
<span class="math inline">\(m_{mat}\)</span>​ 越大，得到的分辨率参数
<span class="math inline">\(m\)</span>​ 也越大；</p></li>
<li><p><span class="math inline">\(m_{size}\)</span>​​​​ 考虑了 b)
中距离与角度的要求，其计算为 <span class="math display">\[
                m_{size}=\frac{m^2_{depth}}{m_{face}}\cos(\theta_{long})
\]</span></p>
<blockquote>
<p>depth 越大，表面面积相对越小，对应较大的 <span class="math inline">\(m\)</span>​；<span class="math inline">\(m_{face}\)</span>​​ 越小，表示入射光与 normal
夹角越大，越 grazing，对应较大的 <span class="math inline">\(m\)</span></p>
</blockquote>
<ul class="task-list">
<li><label><input type="checkbox">不明白：This term converts probe
area to actual object size, compensating for perspective and angular
foreshortening akin to a form factor. <span class="math inline">\(\theta_{long}\)</span> is the longitude angle,
which compensates for size variations induced by the lat-long base
parameterization.</label></li>
</ul></li>
<li><p>要求 c) 需要对局部几何复杂度分析，作者使用频率分析代替： <span class="math display">\[
   m_{complexity}=\frac{1}{N^2}\sum\limits^{N-1}_{p=0}\sum\limits^{N-1}_{q=0}w_{p,q}||\mathbf{b}_{p,q}*m_{norm}||_1
   \]</span></p>
<ul class="task-list">
<li><label><input type="checkbox">2D Discrete consine transform <a href="#%5B2%5D">[2]</a>: <span class="math inline">\(\mathbf{b}\)</span>​
是 basis function，basis function 与 法线的卷积用来分析 local
neighborhood 的频率信息</label></li>
</ul>
<p><span class="math inline">\(w_{p,q} =||[p,q]||^k\)</span>
权重用来确保高频对几何复杂度贡献更高，论文中取 <span class="math inline">\(N=16,k=5\)</span>。</p></li>
</ul>
<p><span class="math inline">\(m_{size}\)</span> 和 <span class="math inline">\(m_{complexity}\)</span> 使用均值经过归一化。</p>
<p>上述 Adaptive Resolution Map 只是计算了 probe
在不同方向上的分辨率参数，但这个参数只是相对分辨率，反应不同方向上分辨率的大小关系，而不是真正的分辨率。接下来需要根据这个相对分辨率参数，进行对
probe 真正分辨率的参数化。</p>
<h5 id="adaptive-parameterization">3.1.2.2 Adaptive Parameterization
<a name="1.2.2"></a></h5>
<p>本小节，作者使用 tensorial quasi-harmonic maps <a href="#%5B3%5D">[3]</a>，将 adaptive resolution map 转为 adapted
resolution。转换过程如下：</p>
<ul class="task-list">
<li><label><input type="checkbox">需要理解 <a href="#%5B3%5D">[3]</a>
的数学原理，描述下面过程的细节</label></li>
</ul>
<ol type="1">
<li>将每个 proble 对应的 3D grid 进行离散等划分，得到均分的四边形网格
quad mesh (应该是 3D grid 的六面划分成 四边形网格)，并且 adaptive
resolution map 中每一个元素对应一个 quad mesh。</li>
<li>根据 adaptive resolution map ，求解 quasi-harmonic equation ，得到
forward flow map。</li>
<li>forward flow map 指示如何调整 quad mesh 的顶点，不断对 quad mesh
变形，最终达到与 apaptive resolution map 相一致的参数化。probe 对应的 3D
grid 最终变形为如图 <a href="#Fig.%201.2.2">Fig. 1.2.2(h)</a>
所示。</li>
</ol>
<p>至此，变形的 quad mesh 达到与 adaptive resolution map 描述一致的
adaptive resolution，即 adaptive resolution map
较大的元素对应较大的网格，较小的元素对应较小的网格。下面就需要计算不同分辨率的
probe data，即对网格的每个像素进行 path-tracing 计算 probe
data。但由于变形，这里的像素位置已经不能确定 path-tracing
的光线方向，即像素的 view vector。作者将 forward flow map 进行求逆，得到
inverse flow map，这样就可以确定像素真正的 view vector。</p>
<p><a name="Fig. 1.2.2"></a></p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214426137.png" alt="image-20220717214426137" style="zoom: 67%;"></p>
<center>
Fig. 1.2.2
</center>
<p>全局光照包含两个组成成分 glossy 和 diffuse：glossy 成分存储在 probe
数据中，在计算 probe 时，path-tracer 计算 glossy BRDF；Diffuse 成分与
view 视角无关(view-independent)，因此存储在传统的 lightmap 中。</p>
<h4 id="geometric-information">3.1.3 Geometric Information</h4>
<p>上一小节完成了 probe 中包含的全局光照信息的预计算，但 probe
贴图数据中包含的是 360°
视角的全局信息，在实时渲染中，需要确定当前着色点需要哪些 probe
数据的样本。对于此，作者使用 specular path perturbation
的方法，这个在之后章节阐述，但该方法需要几何面的导数信息。</p>
<p>因此本小节预计算场景几何信息，估计场景中每个顶点的主曲率，以此将 mesh
近似为具有解析表达式形式的抛物面，这样的抛物面可以高效计算出其高阶导，用于实时渲染中收集
probe 样本的 glossy light 信息。过程如下：</p>
<ol type="1">
<li><ul class="task-list">
<li><label><input type="checkbox">对于每个顶点，使用 <a href="#%5B4%5D">[4]</a> 技术估计其主曲率与对应方向 <a href="#Fig%201.3">Fig
1.3(a)</a>。该步骤在每个物体上分别进行，并且为了更好的结果，对较大表面，先进行细分
<a href="#Fig%201.3">Fig 1.3(b d)</a>。</label></li>
</ul></li>
<li>计算来的主曲率可以以曲率方向为坐标轴将物体表面局部表示为具有解析表达式的抛物面
<a href="#Fig%201.3">Fig
1.3(c)</a>，该解析表达式方便后续高效计算其高阶导。</li>
</ol>
<p><a name="Fig 1.3"></a></p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214455780.png" alt="image-20220717214455780" style="zoom:67%;"><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214514069.png" alt="image-20220717214514069" style="zoom:67%;"></p>
<center>
Fig 1.3
</center>
<p>至此，预计算部分已经结束，下面开始实时渲染。</p>
<h3 id="rendering-global-illumination">3.2 Rendering Global
Illumination</h3>
<p>上述预计算的数据都是 probe view 下的光照信息，实时渲染中则需要将
probe view 重投影为 novel view 下所需要的全局光照信息。全局光照信息包含
diffuse 与 glossy 光照信息，对于 diffuse 全局光照，直接采样预计算的
lightmap。而 glossy 成分则需要对 light probe 重投影到 novel view。</p>
<p>glossy 光线路径的全局光照渲染概要：</p>
<ol type="1">
<li><p>需要将预计算的 light probe
进行重投影(reprojection)。为此，先光栅化一个 G-buffer，其中包含
position、normal、表面曲率和材质(ID和roughness)。</p></li>
<li><p>使用 ray-caster，发射光线到 specular pixels(应该指把像素看作
specular)，再经过完美镜面反射。ray data 中存储反射光的 hit position
和对应的材质 IDs。</p></li>
<li><p>之后基于像素附近收集的 probes 信息来计算像素的 glossy
光照信息。</p>
<ul>
<li><p>选择距离着色点最近的 probe(最终方案为最近的 8 个)，确定 probe
data 中与当前像素关联的 sample：计算 probe 经过镜面反射最终到达 ray data
中的 hit position 的反射点，probe data 中该反射点对应的 sample
即包含了着色像素的 glossy GI 信息。(因为 camera -&gt; shading point
-&gt; hit position -&gt; specular reflected position -&gt; probe
正好形成了一条带有间接光照的光线路径，前三个是一个镜面反射路径(从 ray
data 中获取)，后三个是一个镜面反射路径(由选定的 probe
位置计算得到))。</p>
<blockquote>
<p>如图 <a href="#Fig%202">Fig 2(a)</a> 所示，光线路径为 <span class="math inline">\(\mathbf{p}\rightarrow \mathbf{x} \rightarrow
\mathbf{q} \rightarrow \mathbf{x&#39;} \rightarrow
\mathbf{p&#39;}\)</span>​，其中 <span class="math inline">\(\mathbf{x}\)</span> 为像素对应的着色点，<span class="math inline">\(\mathbf{q}\)</span> 为经过镜面反射的 hit
position(存储在 ray data 中)，<span class="math inline">\(\mathbf{p&#39;}\)</span> 为​挑选的 probe，<span class="math inline">\(\mathbf{x&#39;}\)</span> 为由 <span class="math inline">\(\mathbf{p&#39;}\)</span> 与 <span class="math inline">\(\mathbf{q}\)</span>​
作为镜面反射路径端点计算得到。</p>
</blockquote></li>
<li><p>基于一种设计的评估策略，在反射点对应的 probe sample
附近搜索选择最优的 probe sample，即为最终用来计算着色点 GI 的
sample。</p></li>
</ul></li>
</ol>
<p><a name="Fig 2"></a></p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214548051.png" alt="image-20220717214548051" style="zoom:67%;"></p>
<center>
Fig 2
</center>
<p>由上述可知，想要收集 probe samples 信息，必须先知道反射点 <span class="math inline">\(\mathbf{x&#39;}\)</span> 的位置。虽然可以再次使用
ray-caster，但为了高效，作者引入 specular path perturbation 方法。</p>
<h4 id="on-the-fly-reflection-position-estimation">3.2.1 On-the-fly
Reflection Position Estimation</h4>
<p>当相机由 <span class="math inline">\(\mathbf{p}\)</span>​​​ 移动到 <span class="math inline">\(\mathbf{p&#39;}\)</span>​​​ 时，作者基于 specular
path perturbation 理论来确定 <span class="math inline">\(\mathbf{x}\)</span>​​​ 移动到的位置 <span class="math inline">\(\mathbf{x&#39;}\)</span>​​​。这样的移动描述的其实是镜面反射路径<span class="math inline">\(\mathcal{S}_1\)</span>​​​(<span class="math inline">\(p\rightarrow x\rightarrow q\)</span>​​​) 到 <span class="math inline">\(\mathcal{S}_2\)</span>​​​(<span class="math inline">\(p&#39;\rightarrow x&#39;\rightarrow q\)</span>​​​​) 的
specular motion。其中反射路径 <span class="math inline">\(\mathcal{S}_1\)</span>​​​​ 在 G-buffer
光栅化后已经确定，那么只有 <span class="math inline">\(\mathbf{x&#39;}\)</span>​​​ 是未知的。</p>
<ul class="task-list">
<li><label><input type="checkbox">基于 specular path perturbation <a href="#%5B5%5D">[5]</a> 理论，此时问题转化为：已知 <span class="math inline">\(\Delta p=p&#39;-p\)</span>​​，近似 <span class="math inline">\(\Delta x=x-x&#39;\)</span>​​，从而得到 <span class="math inline">\(x&#39;=x+\Delta x\)</span>​​. <span class="math display">\[
\Delta x=J\Delta p+[\Delta p]^TH[\Delta p]
\]</span> 其中，<span class="math inline">\(J(p,q,x,f)\)</span>​​ 是一个
3X3 Jacobian 矩阵，<span class="math inline">\(H(p,q,x,f)\)</span>​​
是一个 3x3x3 tensor，<span class="math inline">\([\Delta p]\)</span>​​
是重复 <span class="math inline">\(\Delta p\)</span>​​ 三次的 1x1x3
tensor。由于 specular path perturbation 需要使用到 <span class="math inline">\(f\)</span>​​
的一阶、二阶、三阶导数，这里就使用到了预计算中近似的抛物面。</label></li>
</ul>
<p>至此，已经计算出 <span class="math inline">\(p\rightarrow x
\rightarrow q \rightarrow x&#39; \rightarrow p&#39;\)</span> 光线路径中
<span class="math inline">\(x&#39;\)</span> 的位置，作者对于得到的 <span class="math inline">\(x&#39;\)</span> 做了额外的处理，只保留和 <span class="math inline">\(x\)</span> 具有相同材质的 <span class="math inline">\(x&#39;\)</span> 点。下面可以使用 <span class="math inline">\(x&#39;\)</span> 来收集 probe data 中的 probe
samples，从而得到 probe data 中存储的 glossy GI 信息。</p>
<h4 id="gathering-view-dependent-color">3.2.2 Gathering View-dependent
Color</h4>
<p>结合 specular path perturbation
和预计算中表面曲率估计的方法，只是得到了 novel view 到 probe view 的
specular motion。为了得到更加鲁棒的结果，需要在 <span class="math inline">\(x&#39;\)</span> 对应的 probe sample
的区域中搜索，选取最优的 probe sample。</p>
<blockquote>
<p>在 <span class="math inline">\(p\rightarrow x \rightarrow q
\rightarrow x&#39; \rightarrow p&#39;\)</span> 光线路径中，<span class="math inline">\(p&#39;\rightarrow x&#39;\rightarrow q\)</span>
为镜面反射的路径，但 probe data 中存储的是 glossy GI，glossy BRDF 是一个
lobe，光线路径对应的是一个区域，只要最终能到达 <span class="math inline">\(q\)</span> 即为所找 probe sample。因此，<span class="math inline">\(x&#39;\)</span>
对应的样本附近区域也属于该镜面反射对应的 glossy
路径。因此可以在这个区域的 probe samples 中选取最优的
sample。注意：probe data 中的每个 sample 携带的都是其 probe 对应的 3D
grid 区域的 glossy BRDF
的全局光照信息，若要使得像素的全局光照信息更加平滑，应收集不同区域
(即不同 probe) 的全局光照信息。</p>
</blockquote>
<p><strong>probe sample 搜索算法</strong>：使用 two-level 的 grid
搜索算法，coarse-level 在较大的区域内使用较大的 step-size 进行搜索，得到
coarse-level 的最优 sample。然后以这个最优 sample 为中心，使用较小的
step-size 进行 fine-level 搜索。</p>
<p><strong>评估 probe sample 的优劣</strong>：接下来使用 <a href="#Fig%202">Fig 2(b)</a> 中的符号，probe view 下的 reflector
position <span class="math inline">\(r_p\)</span>、normal <span class="math inline">\(n_p\)</span> 和 reflected position <span class="math inline">\(R_p\)</span>；novel view 下对应的 <span class="math inline">\(r_v\)</span>、<span class="math inline">\(n_v\)</span> 和 <span class="math inline">\(R_v\)</span>​ 。设计 energy function 来评估 probe
sample 优劣的四个标准：</p>
<ul>
<li><p>novel view sample(<span class="math inline">\(R_v\)</span>) 和
probe view samples(<span class="math inline">\(R_p\)</span>)
更倾向于位于同一表面。如果二者的材质 ID 不同，则惩罚总 energy，即乘上
<span class="math inline">\(\large s_a=10\)</span>.
因此只有在其他样本不可用时，才会选用材质不匹配的样本。</p></li>
<li><p><span class="math inline">\(R_v\)</span> 与 <span class="math inline">\(R_p\)</span> 距离近更好，对此引入 <span class="math inline">\(s_b=||R_v-R_p||\)</span>.</p></li>
<li><p>相近的表面法线 <span class="math inline">\(n_v\)</span>​ 和 <span class="math inline">\(n_p\)</span>​ 可以确保一致的光照。对此引入 <span class="math inline">\(s_c=1-(n_v\cdot n_p)\)</span>​</p></li>
<li><p>样本应该具有相近的 reflected ray，对此引入： <span class="math display">\[
             s_d=1-\frac{(R_v-r_v)\cdot
(R_p-r_p)}{||R_v-r_v||\cdot||R_p-r_p||}
             \]</span></p></li>
<li></li>
</ul>
<p>结合以上标准，设计 energy function 为：</p>
<p><span class="math display">\[
\varepsilon=s_a\cdot \big(min(s_b,1)+min(s_c,1)+min(s_d,1)\big) \tag{1}
\label{energy function}
\]</span> 由于预计算 probe data 过程中应用了 apaptive
parameterization，因此在搜索 sample 邻域中不能按常规使用固定大小的
grid(即搜索算法中的步长大小单位不应该固定)，因为这样会在压缩区域中丢失细节，或在放大区域中不充分搜索。对于此，作者对
step size 进行一次 scale，即步长(grid 数量)要乘以</p>
<p><span class="math display">\[
|\frac{\partial f^{-1}_h}{\partial x}|^{-1}
\]</span> 其中 <span class="math inline">\(\large f^{-1}_h\)</span>​​
是预计算过程中生成的 <a href="#1.2.2">inverse flow
field</a>。在作者实验中，coarse-level 搜索区域大小为 7X7 samples，步长为
4 texels；fine-level 搜索区域大小为 3X3 samples，步长为 2
texels。(步长都是 scale 前的)。</p>
<p>至此已经完成在 probe data 中选取最优 probe sample 的方法。下面将
probe sample 中的 glossy GI 信息加入到像素的最终着色中。</p>
<p>如果简单的只收集一个 probe 的 probe sample 中的 glossy
GI，相机移动过程中会出现 pop-up 不平滑现象。作者使用距离 novel view
最近（即距相机最近）的 8 个 probe，每个 probe 选取一个最优 probe
sample，最终以以下方式整合： <span class="math display">\[
C=\frac{1}{Z}\sum\limits^8_{i=1} t_i\cdot exp(-\phi\varepsilon_i)\cdot
c_i \tag{2} \label{the sum of neighboor probes}
\]</span> 其中 <span class="math inline">\(t_i\)</span>​ 是 trilinear
weight；<span class="math inline">\(\phi\)</span> 是 falloff
因子，论文中设为 8；<span class="math inline">\(Z\)</span>
是归一化系数，确保加权和为一个 unity；<span class="math inline">\(c_i\)</span> 从 probe data 中得到，当 sample 没有
color-blend 时，在加载 <span class="math inline">\(c_i\)</span>​​
过程中使用 bilinear 插值。对于没有找到有效 sample 的像素，对上一帧的
probe 进行 reprojection。</p>
<h3 id="two-step-convolution-for-accurate-warping-of-glossy-probes">3.3
Two-step Convolution For Accurate Warping of Glossy Probes</h3>
<p>上述描述的都是完美镜面反射的着色点，即 <span class="math inline">\(p\rightarrow x\rightarrow q\)</span>
为镜面反射路径，但实际中着色点更多的还是 glossy 材质。对于 glossy
材质的着色点，其对应的 hit position <span class="math inline">\(q\)</span> 会变为一个区域，即 glossy lobe
对应的区域。任何可以到达该区域的光线都可以复用该区域内对应的 probe
samples。</p>
<p>对于一种特殊情况，两个相邻着色点的 BRDF lobe 的所有 probe samples
正好分别落在了 occlusion 边界两侧，这样就形成了明显的硬边界。图 <a href="#Fig%203">Fig 3</a> 形象描述了出现这种情况的原理，<a href="#Fig%203">Fig 3(e)</a> 中两个像素对应的着色点的 BRDF lobe
包含了黄色和蓝色两个平面的信息，这是 ground truth <a href="#Fig%203">Fig
3(a)</a> 对应的 BRDF lobe 积分情况；简单的 reprojection 收集的 probe
samples 正好落在了遮挡的一侧 <a href="#Fig%203">Fig
3(f)</a>，使得模糊的边缘变得 sharpen <a href="#Fig%203">Fig
3(d)</a>；如果仅进行一次简单的 post-process filter，会导致
overblur，使得表面视觉 roughness 增大 <a href="#Fig%203">Fig
3(g)</a>，注意着色像素的颜色(roughness
增大，导致像素包含了更大区域的信息，最终颜色过于平滑)。</p>
<p><a name="Fig 3"></a></p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214753044.png" alt="image-20220717214753044" style="zoom: 67%;"></p>
<center>
Fig 3
</center>
<p>本章节设计新的 filter，减小 Naive post-process filter 造成的
overblur。作者使用 Gaussian 分布拟合 BRDF，利用 Gaussian
分布卷积中的可分离特性，将 BRDF
包含的卷积进行分离为两步。在预计算过程中，减小表面 roughness，设计实时
image filter 重构原 roughness</p>
<blockquote>
<p>这种聚集 occlusion
边界一侧的现象可以理解为采样不足导致的，本章节提出在不提高采样数量的情况下，在
probe 预计算阶段降低几何表面的
roughness，这样会使得反射的光线更发散，probe sampe
更容易收集到周围着色点信息。在实时渲染过程中，再使用设计的 filter 重构原
roughness，得到原 roughness 下的周围着色点信息。</p>
</blockquote>
<h4 id="filter-footprint-estimation">3.3.1 Filter Footprint
Estimation</h4>
<p>为了使用 Gaussian 分布可分离特性，首先需要使用 Gaussian 分布拟合
BRDF，为此需要估计其 footprint（指不同参数变化下 BRDF
的变化足迹）。为了估计 BRDF footprint，作者使用了一个简单的设定，如 <a href="#Fig%203.1">Fig 3.1(a)</a> 所示：相机通过看向各向同性均匀
roughness 的反射平面(<span class="math inline">\(\mathbf{x}\)</span>​​​​​​​​)来观察 reflected
point，其中反射平面法向量与 view direction 平行，并且与相机和 reflected
point 的共线并行。基于这种设定，以固定的 fov 评估每一个点的 GGX
BRDF(微表面法线模型) <a href="#%5B6%5D">[6]</a>，如 <a href="#Fig%203.1">Fig 3.1(b)</a>
所示，中间区域最亮应该是直射区域，越往外侧越暗，GGX BRDF 的 specular
lobe 反射到相机的越来越少。</p>
<p>影响 BRDF footprint 的参数有评估点的 roughness <span class="math inline">\(\rho\)</span>​ 、相机到反射平面的距离 <span class="math inline">\(d_c\)</span>​、反射平面到反射点的距离 <span class="math inline">\(d_r\)</span>​​，给定这些参数，可以拟合一个协方差矩阵为
<span class="math inline">\(\sum_{\rho}\)</span>​ 的 Gaussian 分布 <span class="math inline">\(\mathcal{G}_{\rho}(\mathbf{x}:\rho,d_c,d_r)\)</span>​
。协方差矩阵 <span class="math inline">\(\sum_{\rho}\)</span>​
可以通过选定一组 <span class="math inline">\([\rho, d_c,d_r]\)</span>​
参数，采样 BRDF footprint 来确定。​例如 <a href="#Fig%203.1">Fig
3.1(c)</a> 展示的是一条扫描线(相机移动轨迹)下的 BRDF 随 <span class="math inline">\(\mathbf{x}\)</span>​
变化的曲线。作者通过预计算不同参数组合下的协方差表格，供实时渲染时查表。该制表包含
32x32x32 bins，roughness 由 <span class="math inline">\(0\)</span> 到
<span class="math inline">\(0.5\)</span> 、距离由 <span class="math inline">\(0.01\)</span> 到 <span class="math inline">\(10m\)</span>。并且采样过程选取 power sampling，在
<span class="math inline">\(d_r&lt;0.5m\)</span>​
降低协方差，防止这种情况下的拟合 overestimated 协方差。</p>
<p><a name="Fig 3.1"></a></p>
<p><img src="/images/Paper Notes/GI/Glossy Probe Reprojection for Interactive Global Illumination.assets/image-20220717214823052.png" alt="image-20220717214823052" style="zoom:67%;"></p>
<center>
Fig 3.1
</center>
<p>上述简单设定是 view direction
与反射平面垂直的情况，当二者不垂直时，即反射平面为一个倾斜平面，会使得
BRDF footprint
会由于远小近大而沿着倾斜方向缩小。在渲染时，通过将协方差乘上 view
direction 和 surface normal 的点乘来进行补偿。</p>
<blockquote>
<p>使用 <a href="#Fig%203.1">Fig 3.1(a)</a> 中的设置在预计算过程中拟合出
<span class="math inline">\([\rho, d_c,d_r]\)</span> 参数不同组合下的
BRDF footprint 的 Gaussian 分布，然后在实时渲染中，根据当前着色点的
<span class="math inline">\([\rho, d_c,d_r]\)</span> 查表获取该点的 BRDF
footprint。但实时渲染中着色点、相机、反射平面多数不是 <a href="#Fig%203.1">Fig 3.1(a)</a> 的简单设定，需要通过对协方差参数进行
scale 补偿，scale 系数为 view direction 和 surface normal 的点乘。</p>
</blockquote>
<p>假设 <span class="math inline">\(\mathcal{G}_{\rho}\)</span>
是反射平面的 BRDF，在 glossy light probes 预计算中获取；<span class="math inline">\(\mathcal{G}_I\)</span> 是 runtime image
filter。我们可以在预计算中降低反射平面的 roughness，得到具有对应新
roughness <span class="math inline">\(\rho&#39;\)</span> 的协方差矩阵
<span class="math inline">\(\sum_{\rho&#39;}\)</span> 的 <span class="math inline">\(\mathcal{G}_{\rho&#39;}\)</span>. 使用 Gaussian
分布的性质：</p>
<p>​ <span class="math display">\[\sum(\mathcal{G}_1*\mathcal{G}_2)=\sum(\mathcal{G}_1)+\sum(\mathcal{G}_2)\]</span>​​,
其中 <span class="math inline">\(\sum\)</span>
是卷积的求和操作，其余为协方差矩阵</p>
<p>现在需要求得一个 image filter <span class="math inline">\(\mathcal{G}_I\)</span> 将降低过 roughness <span class="math inline">\(\rho &#39;\)</span> 的 <span class="math inline">\(\mathcal{G}_{\rho&#39;}\)</span> 重构为原
roughness 的 <span class="math inline">\(\mathcal{G}_{\rho}\)</span>​.
利用上述性质，可以得到</p>
<p><span class="math display">\[
\sum_I=\sum_{\rho}-\sum_{\rho&#39;}
\]</span> 从而得到 <span class="math inline">\(\mathcal{G}_I\)</span>
（因为这里所有的 Gaussian 分布都采用均值为 0）。在实现中，作者使用 <span class="math inline">\(\rho &#39;=\rho/2\)</span>​
作为预计算中光线路径中的第一个 glossy 顶点的 roughness 参数。</p>
<p>至此，已经完成对 image-space filter footprint 的估计。下面是如何应用
image filter。</p>
<h4 id="gloss-filtering">3.3.2 Gloss Filtering</h4>
<p>使用几何数据来估计公式 <span class="math inline">\(\eqref{the sum of
neighboor probes}\)</span> 中的 <span class="math inline">\(C\)</span>
， <span class="math display">\[
\hat{C}(\mathbf{x})=\frac{1}{Z(\mathbf{x})}\sum\limits_{\mathbf{x}_i\in\mathcal{N}(\mathbf{x})}\mathcal{G}_I(\mathbf{x}_i-\mathbf{x})w_r(\mathbf{x,x}_i)\space\varepsilon^{-1}(\mathbf{x}_i)C(\mathbf{x}_i)
\tag{3} \label{glossy filtering}
\]</span> 其中，<span class="math inline">\(\mathcal{N}(\mathbf{x})\)</span>​​​​ 表示 <span class="math inline">\(\mathbf{x}\)</span>​​​​ 处的 filter footprint；<span class="math inline">\(\large \varepsilon\)</span>​​​​ 是 energy function
<span class="math inline">\(\eqref{energy
function}\)</span>​​​​，其逆函数作为置信度来限制向匹配良好的像素的传播；range
weight：</p>
<p><span class="math display">\[
w_r(\mathbf{x,x}_i)=\mathbb{1}_{\mathbf{n(x)\cdot
n}(\mathbf{x}_i)&gt;\alpha_\mathbf{n}}\cdot
\mathbb{1}_{|d(\mathbf{x})-d(\mathbf{x}_i)|&lt;\alpha_d}\cdot
\mathbb{1}_{m(\mathbf{x})=m(\mathbf{x}_i)}
\]</span> 作为 cross-bilateral 项，防止不连续的 normal 和 depth
或者不同反射材质 IDs 之间的 filtering. 实现中，作者设置 <span class="math inline">\(\alpha_{\mathbf{n}}=0.8\)</span>​ 和 <span class="math inline">\(\alpha_d=0.2\)</span>​；<span class="math inline">\(Z\)</span>​​ is the normalizing partition function,
ensuring filter weights sum to unity。</p>
<blockquote>
<p>求和的区域应该是 glossy BRDF lobe 对应的区域，对这个区域进行
filter</p>
</blockquote>
<h4 id="efficient-filter-approximation">3.3.3 Efficient Filter
Approximation</h4>
<ul class="task-list">
<li><label><input type="checkbox"><span class="math inline">\(\eqref{glossy filtering}\)</span>​​​
式的计算开销太大，作者使用 two passes, effectively using four 1D
separable filters 进行高效近似计算 <a href="#%5B7%5D">[7]</a></label></li>
</ul>
<h2 id="remain-questions">4 Remain Questions</h2>
<ul class="task-list">
<li><p><label><input type="checkbox">不明白：This term converts probe
area to actual object size, compensating for perspective and angular
foreshortening akin to a form factor. <span class="math inline">\(\large
\theta_{long}\)</span> is the longitude angle, which compensates for
size variations induced by the lat-long base
parameterization.</label></p></li>
<li><p><label><input type="checkbox">2D Discrete consine transform <a href="#%5B2%5D">[2]</a>: <span class="math inline">\(\mathbf{b}\)</span>​
是 basis function，basis function 与 法线的卷积用来分析 local
neighborhood 的频率信息</label></p></li>
<li><p><label><input type="checkbox">需要理解 <a href="#%5B3%5D">[3]</a> 的数学原理，描述下面过程的细节</label></p></li>
<li><p><label><input type="checkbox">对于每个顶点，使用 <a href="#%5B4%5D">[4]</a> 技术估计其主曲率与对应方向 <a href="#Fig%201.3">Fig
1.3(a)</a>。该步骤在每个物体上分别进行，并且为了更好的结果，对较大表面，先进行细分
<a href="#Fig%201.3">Fig 1.3(b d)</a>。</label></p></li>
<li><p><label><input type="checkbox">基于 specular path perturbation
<a href="#%5B5%5D">[5]</a> 理论，此时问题转化为：已知 <span class="math inline">\(\Delta p=p&#39;-p\)</span>，近似 <span class="math inline">\(\Delta x=x-x&#39;\)</span>，从而得到 <span class="math inline">\(x&#39;=x+\Delta x\)</span>. <span class="math display">\[
\Delta x=J\Delta p+[\Delta p]^TH[\Delta p]
\]</span> 其中，<span class="math inline">\(J(p,q,x,f)\)</span> 是一个
3X3 Jacobian 矩阵，<span class="math inline">\(H(p,q,x,f)\)</span>
是一个 3x3x3 tensor，<span class="math inline">\([\Delta p]\)</span>
是重复 <span class="math inline">\(\Delta p\)</span> 三次的 1x1x3
tensor。由于 specular path perturbation 需要使用到 <span class="math inline">\(f\)</span>​​
的一阶、二阶、三阶导数，这里就使用到了预计算中近似的抛物面。</label></p></li>
<li><p><label><input type="checkbox">cross-bilateral
是什么</label></p></li>
<li><p><label><input type="checkbox"><span class="math inline">\(\eqref{glossy filtering}\)</span>
式的计算开销太大，作者使用 two passes, effectively using four 1D
separable filters 进行高效近似计算 <a href="#%5B7%5D">[7]</a></label></p></li>
</ul>
<h2 id="reference">Reference</h2>
<p><a name="[1]">[1]</a> Morgan McGuire, Mike Mara, Derek
Nowrouzezahrai, and David Luebke. 2017b. Realtime global illumination
using precomputed light field probes. In Proceedings of the Symposium on
Interactive 3D Graphics and Games. ACM, 2</p>
<p><a name="[2]">[2]</a> Nasir Ahmed, T Raj Natarajan, and Kamisetty R
Rao. 1974. Discrete cosine transform. IEEE Trans. Comput. 100, 1 (1974),
90–93.</p>
<p><a name="[3]">[3]</a> Rhaleb Zayer, Christian Rossl, and H-P Seidel.
2005. Discrete tensorial quasi-harmonic maps. In International
Conference on Shape Modeling and Applications. IEEE, 276–285.</p>
<p><a name="[4]">[4]</a> Mark Meyer, Mathieu Desbrun, Peter Schröder,
and Alan H Barr. 2003. Discrete differential-geometry operators for
triangulated 2-manifolds. In Visualization and Mathematics III.
Springer, 35–57.</p>
<p><a name="[5]">[5]</a> Perturbation methods for interactive specular
reflections. IEEE Transactions on Visualization and Computer Graphics 6,
3 (2000), 253–264.</p>
<p><a name="[6]">[6]</a> Bruce Walter, Stephen R Marschner, Hongsong Li,
and Kenneth E Torrance. 2007. Microfacet Models for Refraction through
Rough Surfaces. Rendering Techniques 2007 2007, 18th.</p>
<p><a name="[7]">[7]</a> Eduardo SL Gastal and Manuel M Oliveira. 2011.
Domain transform for edge-aware image and video processing. ACM
Transactions on Graphics (TOG) (2011), 1–12.</p>
<p><a name="[8]">[8]</a> <em>Simon Rodriguez, Thomas Leimkühler,
Siddhant Prakash, Chris Wyman, Peter Shirley, and George Drettakis.
2020. Glossy probe reprojection for interactive global
illumination.</em> <em>ACM Trans. Graph.</em> <em>39, 6, Article 237
(December 2020), 16 pages.
DOI:https://doi.org/10.1145/3414685.3417823</em></p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>GI</category>
        <category>Light Probe</category>
      </categories>
  </entry>
  <entry>
    <title></title>
    <url>/C++/Effective-Modern-C++/Chapter-1%EF%BC%9A%E7%B1%BB%E5%9E%8B%E6%8E%A8%E5%AF%BC.html</url>
    <content><![CDATA[<h2 id="条款一模板类型推导">条款一：模板类型推导</h2>
<p>对于下面一段模板伪代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(ParamType param)</span></span>;	<span class="comment">// ParamType</span></span><br></pre></td></tr></table></figure>
<p><code>ParamType</code>
是模板形参类型，常带有修辞词(const)或引用声明。接下来将要谈论的几种情况：</p>
<ul>
<li>值传递：<code>ParamType</code>为<code>T</code></li>
<li>引用传递：
<ul>
<li>左值引用：<code>ParamType</code>为<code>T&amp;</code>或<code>T const&amp;</code></li>
<li>万能引用：<code>ParamType</code>为<code>T&amp;&amp;</code></li>
</ul></li>
<li>指针传递：<code>ParamType</code>为<code>T*</code></li>
</ul>
<h3 id="类型推导规则">类型推导规则</h3>
<ol type="1">
<li><p>无论是上述哪种情况，引用类型的实参都会被当作非引用类型处理</p>
<p>这一点与普通函数调用类似，实参是否为引用类型不会影响到形参是否为引用</p></li>
<li><p>值传递模板类型：实参的 const/volatile 会被去掉</p>
<p>这一点与普通函数调用类似，值传递形参只是实参的副本，const/volatile
修饰对于值传递没有意义。</p></li>
<li><p>引用传递模板类型：</p>
<ul>
<li><ol type="a">
<li>左值引用：遵循第1点，实参自带的引用类型会被忽略。</li>
</ol>
<ul>
<li>1 const引用模板类型：实参的const会被忽略</li>
<li>2
非const引用模板类型：实参的const会被模板类型保留，这一点与普通函数调用不同</li>
</ul></li>
<li><ol start="2" type="a">
<li>万能引用：会根据实参属于左值还是右值推导模板类型</li>
</ol>
<ul>
<li>1 左值实参：模板类型会被解释为左值引用，剩下的推导过程与 3.a.1
相同</li>
<li>2
右值实参：模板类型会被解释为右值引用。右值实参没有const而言了（因为是右值）</li>
</ul></li>
</ul></li>
<li><p>指针模板类型：与左值引用推导规则类型</p>
<ol type="1">
<li>会取消掉实参的指针类型</li>
</ol></li>
</ol>
<h3 id="示例">示例</h3>
]]></content>
  </entry>
  <entry>
    <title>Effect-based Multi-viewer Caching for Cloud-native Rendering</title>
    <url>/Paper-Notes/Cloud-Rendering/Effect-based-Multi-viewer-Caching-for-Cloud-native-Rendering.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p>本文基于object-space/texture-space
shading管线，提出一种面向multi-viewers的云渲染的cache系统设计。对于effect中与视角无关的部分，通过转换为texture-space下的渲染任务，并且设计effect-based
cache系统，使得不同viewers共享同一份effect中间数据。提出的effect-based
cache系统共包含定义在物体表面的On-Surface
Cache(OSC)与定义在grid上的World-Space Cache(WSC)两种策略，统一使用hash
table的形式进行存储管理，但二者具有不同的hash
key设计，包含实际数据的cache
entry由具体的effect定义。作者为两种cache策略分别给出相应的具体effect应用，同时提出通过为不同的effect使用不同的bias配置来为低频effect选择低分辨率，减少内存开销。</p>
<h1 id="background">2 Background</h1>
<h1 id="pipeline设计">3 Pipeline设计</h1>
<p>管线设计如图 <a href="#Fig-1">Fig-1</a>，首先为每个viewer执行visibility
pass来决定那些cache entries会被使用到；然后对所有viewers的cache
request进行整合更新，这一步可以执行在不同的GPU上；之后viewers再通过采样cache来着色帧；最后是后处理，将生成的图像插入到output
stream。</p>
<p><a name="Fig-1"></a> <img src="/images/Paper%20Notes/Cloud%20Rendering/Effect-based%20Multi-viewer%20Caching%20for%20Cloud-native%20Rendering.assets/image-20230703130901243.png" alt="image-20230703130901243"></p>
<center>
Fig-1 Cloud Native Rendering Pipeline
</center>
<p>想要被多个viewer复用，被cache的计算只能是视角无关的，同时作者提出基于effect的cache设计，可以为不同类型的渲染计算进行调整。经过对现代游戏引擎中的效果分析，作者提出两种类型的cache：on-surface
cache，共享表面点上的计算；world-space cache，共享世界空间数据。</p>
<h2 id="on-surface-caches">3.1 On-Surface Caches</h2>
<p>On-Surface Cache(OSC)采用类似于<a href="#%5B1%5D">[1]</a>的管线。首先预处理阶段需要为mesh生成唯一表示的纹理空间。作者通过将一定数量的相连接的面片划分成island单位，每个island有独立的texture
space，可以生成triangle lookup texture，即每个texel的triangle id。</p>
<p>对于cache texture的实现，尽管目前硬件支持virtual
texture，但64KB的page大小对于本系统过大，因此作者采用软件实现的cache系统。本文采用的cache
entity大小为8x8 texel
block，texel的实际大小由具体effect定义。cache的访问采用hash形式，为此作者提出设计用于访问所有virtual
textures的单个hash map。hash map中存储的是指向cache
entity(实际数据)的hash entity，由如下部分组成：</p>
<ul>
<li>island index：32bit，用来标识island，island下是独立的texture
space</li>
<li>resolution：4bit，应该是mipmap level</li>
<li>texel block coordinates：2x12bit，island下的8x8 texel
block的纹理坐标。 因此可以最多表示 32kx32k的纹理空间（<span class="math inline">\(2^{12}*8=2^{15}=32k\)</span>）</li>
</ul>
<p>OSC pipeline主要由如图<a href="#%5BFig-2%5D">Fig-2</a>中的五个阶段组成：</p>
<ol type="1">
<li>Visibility：决定哪些cache entity会被用到以及去重cache request</li>
<li>Cache Update Queuing：填充着色任务队列</li>
<li>Triangle Lookup Rendering：</li>
<li>Effect Update：cache更新</li>
<li>Compositing：整合cache数据以及剩余的渲染计算生成最终渲染结果</li>
</ol>
<p><a name="Fig-2"></a></p>
<figure>
<img src="/images/Paper%20Notes/Cloud%20Rendering/Effect-based%20Multi-viewer%20Caching%20for%20Cloud-native%20Rendering.assets/image-20230704143603816.png" alt="image-20230704143603816">
<figcaption aria-hidden="true">image-20230704143603816</figcaption>
</figure>
<center>
Fig-2 OSC Pipeline
</center>
<h3 id="visibility">3.1.1 Visibility</h3>
<p>为每个viewer执行visibility pass生成所请求的cache
entity，此外可以根据屏幕空间梯度以及每个effect的先验知识来调整cache
entity的bias，提高目标mip
level。例如空间低频的effect可以渲染的目标分辨率更低。</p>
<p>为每个texel block配置一个64bit
mask，来标识block内哪些像素需要更新。对于每个surface
point可能需要1、4、8个texel
blocks，分别用于point、bilinear、trilinear采样。对于每个texel
block都会查询hash
table来判断是否已经map，如果没有map，则map一个空block。同时，每帧重置texel
mask。通过这种方式，可以去除重复请求。</p>
<blockquote>
<p>不是很清楚mip level如何工作的？似乎是降低的是texture
space的分辨率，例如level 0的分辨率32kx32k，level
1的分辨率则为16kx16k。</p>
<p>unbiased情况是通过屏幕空间梯度计算的mip
level达到pixel与texel一一对应；biased情况是手动为某些低频effect增大mip
level，多个pixel对应一个texel。</p>
</blockquote>
<h3 id="cache-update-queuing">3.1.2 Cache Update Queuing</h3>
<p>本阶段的更新以texel block为单位进行，为每个效果创建一个cache update
queue，用于存储texel block的更新请求。</p>
<h2 id="world-space-caches">3.2 World-Space Caches</h2>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a> K. E. Hillesland and J. C. Yang. 2016. Texel
Shading. In Proceedings of the 37th Annual Conference of the European
Association for Computer Graphics: Short Papers (Lisbon, Portugal) (EG
’16). Eurographics Association, Goslar, DEU, 73–76.</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>Cloud Rendering</category>
      </categories>
  </entry>
  <entry>
    <title>Forward Light Cuts A Scalable Approach to Real-Time Global Illumination</title>
    <url>/Paper-Notes/GI/Forward-Light-Cuts-A-Scalable-Approach-to-Real-Time-Global-Illumination.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p><code>Forward Light Cuts: A Scalable Approach to Real-Time Global Illumination</code>
<a href="#%5B1%5D">[1]</a></p>
<p>这篇论文假设了次级光源为 diffuse
材质的场景，并且不考虑次级光照的阴影，提出了一种能够很好结合
tessellation 和 geometry
阶段的并行特性的高效实时全局光照近似计算算法。该算法基于 many-light
框架，将场景中的三角形类比为虚拟点光源，首先通过将场景中三角形以一种概率分布划分为
multi-scale radiance
子集，在每个子集中的每个三角形上随机采样生成一个扰动的
VPL，这样就组成了多尺度的 VPL 集合。之后求解该 VPL 集合形成的 many-light
问题来近似全局光照。该算法既模拟了传统层级的光源数量的随尺度增加的几何级下降，又具有可高度并行的线性设计，在场景完全动态并且涉及大量物体时，相比于之前算法，画面质量无明显损失并且具有较大的性能提升。</p>
<h1 id="motivation">2 Motivation</h1>
<p>实时全局光照有很多基于 radiance caching
的方法，之前这些方法构建出场景中几何的层级结构（多为树形结构），使用这种层级结构来构建
multi-scale radiance
function，之后通过查询该层级结构对像素进行照亮。</p>
<p>对于这种策略的方法，在场景完全动态并且涉及大量物体情况下，至少有两种局限，</p>
<ul>
<li>层级缓存结构每一帧都要重复计算，这个缺点带来的开销在现代图形学的并行结构下更为明显</li>
<li>对于实时渲染，初始 VPL 集合过大，带来的开销难以分摊</li>
</ul>
<h1 id="approach">3 Approach</h1>
<h2 id="vpls-generation">3.1 VPLs Generation</h2>
<h3 id="区分-divergent-和-regular-三角形">3.1.1 区分 Divergent 和
Regular 三角形</h3>
<p>设置阈值 <span class="math inline">\(S_0\)</span> ，面积大于 <span class="math inline">\(S_0\)</span> 的三角形为 Divergent
三角形，这类三角形需要经过一次 tessellation 特殊处理，之后描述。</p>
<p><span class="math display">\[
S_0=4\pi \frac{D^2_{near}}{N_{avg}}
\]</span> 这个公式表达的是启发式地照亮一个像素，该像素周围有 <span class="math inline">\(N_{avg}\)</span> 个距离为至少 <span class="math inline">\(D_{near}\)</span> 的 VPLs 。参数配置：假设 <span class="math inline">\(R_{scene}\)</span> 为场景的半径长度，设置 <span class="math inline">\(D_{near}=0.2\times R_{scene}\)</span>，<span class="math inline">\(N_{avg}\)</span> 在区间 <span class="math inline">\([64,1024]\)</span> 内进行 quality-speed
tradeoff。</p>
<blockquote>
<p><code>个人理解见</code>：<a href="#附录1">附录 1. 阈值 <span class="math inline">\(S_0\)</span> 的启发式的理解</a></p>
</blockquote>
<h3 id="三角形抽取过程">3.1.2 三角形抽取过程</h3>
<p>抽取操作目的是丢弃小三角形，因为这类三角形引入的 diffuse indirect
lighting
对最终的渲染贡献很小，丢弃会加快速度，但如果全部无差别丢弃，则会丢失那些多个小三角形一起作用而带来的明显间接光照效果，因此提取操作中引入一种随机过程，即<strong>随机抽取过程</strong>：</p>
<p>对于每一个三角形计算一个 <span class="math inline">\([0,1]\)</span>
区间均匀分布的随机数 <span class="math inline">\(u_{t_i}\)</span>，如果三角形面积 <span class="math inline">\(\mathcal{A}(t_i)&gt;u_{t_i}S_0\)</span>，则保留该三角形，否则丢弃。三角形保留的概率分布为</p>
<p><span class="math display">\[
\forall\space t_i\in \mathcal{L}, \quad P(t_i\in
\mathcal{L^*})=\frac{\mathcal{A}(t_i)}{S_0}
\]</span> 其中 <span class="math inline">\(\mathcal{L}\)</span> 是全体
regular 三角形的集合，<span class="math inline">\(\mathcal{L^*}\)</span>
是保留的三角形集合。由此可以看出，<strong>三角形面积越小，被丢弃的概率越大。</strong>对于整个场景的面能够保留下来的数量的期望为
<span class="math inline">\(\mathbb{E}(N_{sample})=\frac{\mathcal{A}_{scene}}{S_0}\)</span>。</p>
<h4 id="基于随机提取过程的三角形多尺度划分">基于随机提取过程的三角形多尺度划分</h4>
<p>将场景中三角形划分为 <span class="math inline">\((\mathcal{L}^0,...,\mathcal{L}^N)\)</span> 共
<span class="math inline">\(N+1\)</span> 个子集，子集索引由 <span class="math inline">\(0\)</span> 到 <span class="math inline">\(N\)</span>
递增，影响距离递减（即尺度递减），子集包含的三角形数量递增。为了实现这样的划分，引入
<span class="math inline">\(N+1\)</span> 长度的递增序列 <span class="math inline">\(\{S_0&lt;...&lt;S_N\}\)</span>。扩展随机提取过程到多尺度划分过程，三角形
<span class="math inline">\(\large t_i\)</span> 划分为子集 <span class="math inline">\(\large \mathcal{L}^k\)</span> 的概率为</p>
<p><span class="math display">\[
\forall k\in [0...N],\quad P(t_i\in
\mathcal{L}^k)=\frac{\mathcal{A}(t_i)}{S_k} \tag{1}\label{partition
probability}
\]</span> 在多尺度划分过程中，divergent
三角形的定义更改为<strong>面积大于 <span class="math inline">\(S_N\)</span> 的三角形</strong>。</p>
<p>实际划分算法又有进一步的改动，引入 <span class="math inline">\(\large\forall k\in [0...N],\quad
\tilde{S}_k=\frac{1}{\sum^k_{j=0}\frac{1}{S_j}}\)</span> ，
算法伪代码如下</p>
<p><img src="/images/Paper Notes/GI/Forward Light Cuts A Scalable Approach to Real-Time Global Illumination.assets/image-20220717013637705.png" alt="尺度等级划分算法" style="zoom: 67%;"></p>
<blockquote>
<p>可知 <span class="math inline">\(\tilde{S}_k\)</span> 单调递减，与原
<span class="math inline">\(S_k\)</span>
单调递增相反，但算法中是只有三角形没有被分在 <span class="math inline">\(k-1\)</span> 子集中，才有可能判断是否可分到 <span class="math inline">\(k\)</span>
子集，这是条件概率。粗略地看，子集索引越大，三角形分到该子集的概率越低，因为需要在前面没有分到所有子集的条件，才进行后续子集的划分。</p>
</blockquote>
<p>至此，我们已经对场景三角形进行随机划分为多个不同尺度的子集，之后其中的每个三角形都会随机生成一个
VPL，形成 VPL 集。接下来基于生成的 VPL 求解 many-light
问题，即如何使用这些 VPL 照亮像素。</p>
<h2 id="vpls-光照下的着色">3.2 VPLs 光照下的着色</h2>
<h4 id="many-light-下的-vpls-lighting-问题描述">3.2.1 many-light 下的
VPLs lighting 问题描述</h4>
<p>在 many-lights 框架中，对于 normal 为 <span class="math inline">\(\vec{n_x}\)</span> 的着色点 <span class="math inline">\(x\)</span> 的 indirect outgoing
radiance，计算方法由连续积分近似为来自一个 VPLs 集合的 radiance
离散求和。</p>
<p><span class="math display">\[
L^{ML}(x,\vec{n_x})=\sum_{t_i\in
\mathcal{L}}H(t_i,x,\vec{n_x})\mathcal{A}(t_i) \tag{2}
\]</span> <span class="math inline">\(\mathcal{L}\)</span>
表示场景中所有三角形集合，<span class="math inline">\(H(t_i,x,\vec{n_x})\)</span> 表示 normal 为 <span class="math inline">\(\vec{n_x}\)</span> 的点 <span class="math inline">\(\large x\)</span> 接收到由 <span class="math inline">\(t_i\)</span>(VPL) 出发的 radiance。对于 albedo 为
<span class="math inline">\(\rho_x\)</span> 的 diffuse receiver， <span class="math inline">\(H\)</span> 的形式如下：</p>
<p><span class="math display">\[
H(t_i,x,\vec{n_x})=L(t_i,\bar{y_ix})\frac{\rho_x}{\pi}\frac{&lt;\vec{n_x},\bar{xy_i}&gt;^+&lt;\vec{n_i},\bar{y_ix}&gt;^+}{d_i^2}
\]</span> 其中 <span class="math inline">\(\large \bar{u}\)</span> 表示
normalized 向量，<span class="math inline">\(&lt;\vec{u},\vec{v}&gt;^+=max(0,&lt;\vec{u},\vec{v}&gt;)\)</span>，<span class="math inline">\(L(t_i,\bar{y_ix})\)</span> 是离开 VPL 中心 <span class="math inline">\(\large y_i\in t_i\)</span> 朝向 <span class="math inline">\(\large \bar{y_ix}\)</span> 的 radiance， <span class="math inline">\(d_i=max(\epsilon,||\vec{xy_i}||)\)</span> 是进行过
clamp 的 <span class="math inline">\(y_i\)</span> 与 <span class="math inline">\(\large x\)</span> 之间的距离，避免奇异点。</p>
<blockquote>
<p><code>对近似的个人理解见</code>：<a href="#附录2">附录 2.
着色方程积分近似为求和的理解</a></p>
</blockquote>
<p>VPL 对直接光照的 diffuse 反射表示为下面的 VPL 出射 radiance：</p>
<p><span class="math display">\[
L(t_i,\bar{y_ix})=\rho_iE(t_i)\frac{3}{2\pi}&lt;\vec{n_i},\bar{y_ix}&gt;^+
\]</span> 其中 <span class="math inline">\(\large E_i\)</span>
是直接到达三角形 <span class="math inline">\(\large t_i\)</span> 的直接
irradiance，由于 <span class="math inline">\(&lt;\vec{n_i},\bar{y_ix}&gt;^+\)</span>
可知上式并非 perfectly Lambertian
(一种简单的漫反射：光线被均匀的反射到表面上方的半球)，而只会在几何法线方向发生完美反射。<span class="math inline">\(\large \frac{3}{2\pi}\)</span>
<em>用来保证能量守恒</em>。</p>
<blockquote>
<p>将 <span class="math inline">\(\large L(t_i,\bar{y_ix})\)</span> 代入
<span class="math inline">\(\large H(t_i,x,\vec{n_x})\)</span> 中有</p>
<p><span class="math display">\[
\begin{align}H(t_i,x,\vec{n_x})&amp;=\rho_iE(t_i)\frac{3}{2\pi}&lt;\vec{n_i},\bar{y_ix}&gt;^+\frac{\rho_x}{\pi}\frac{&lt;\vec{n_x},\bar{xy_i}&gt;^+&lt;\vec{n_i},\bar{y_ix}&gt;^+}{d_i^2}\\
&amp;=
\frac{3}{2\pi^2}\rho_i\rho_xE(t_i)\frac{&lt;\vec{n_x},\bar{xy_i}&gt;(&lt;\vec{n_i},\bar{y_ix}&gt;)^2}{d_i^2}
\end{align}\tag{2} \label{received radiance}
\]</span></p>
</blockquote>
<h4 id="多尺度划分下的-vpls-lighting-的近似">3.2.2 多尺度划分下的 VPLs
lighting 的近似</h4>
<p>上述进行的对三角形的多尺度随机划分引入了随机过程，因此 <span class="math inline">\(L^{ML}(x,\vec{n_x})\)</span>
也变成了<strong>随机量</strong>，接下来就需要对该随机量进行估计。我们定义
<span class="math inline">\(K(x,\vec{n_x})\)</span> 为 <span class="math inline">\(L^{ML}(x,\vec{n_x})\)</span>
<strong>估计量(Estimator)</strong>：</p>
<p><span class="math display">\[
K(x,\vec{n_x})=\sum\limits^N_{k=0}\sum\limits_{t_i\in\mathcal{L}^k}H(t_i,x,\vec{n_x})F^k(t_i,k)
\]</span> 其中 <span class="math inline">\(F^k(t_i,k)\)</span>
是一个未知函数，参数 <span class="math inline">\(x\)</span>
为着色点，<span class="math inline">\(t_i\)</span> 为 VPL
所在三角形，<span class="math inline">\(k\)</span> 为子集索引。</p>
<p>下面就要确定 <span class="math inline">\(F^k(t_i,k)\)</span>
的形式：</p>
<p><span class="math display">\[
\begin{align} \mathbb{E}\left[K(x,\vec{n_x})\right]&amp;=
\mathbb{E}\left[\sum\limits^N_{k=0}\sum\limits_{t_i^k\in
\mathcal{L}^k}H(t_i^k,x,\vec{n_x})F^k(t_i^k,x)\right] \\ &amp;=
\sum\limits_{t_i\in \mathcal{L}}H(t_i,x,\vec{n_x})
\mathbb{E}\left[\sum\limits^N_{k=0}F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\right] \\ &amp;= \sum\limits_{t_i\in
\mathcal{L}}H(t_i,x,\vec{n_x})\sum\limits^N_{k=0}F^k(t_i,x)P(t_i\in
\mathcal{L}^k)\end{align}\tag{3}\label{many light estimator}
\]</span> 其中 <span class="math inline">\(\mathbb{I}_{t_i\in
\mathcal{L}^k}\)</span> 为指示函数，当 <span class="math inline">\(t_i\in \mathcal{L}^k\)</span> 时值为 <span class="math inline">\(1\)</span>，否则为 0。</p>
<blockquote>
<p><code>推导见</code>：<a href="#附录3">附录 3. VPL lighting
推导</a></p>
</blockquote>
<p>我们要选取 <span class="math inline">\(F^k(t_i,x)\)</span> 使得 <span class="math inline">\(K(x,\vec{n_x})\)</span> 为 <span class="math inline">\(L^{ML}(x,\vec{n_x})\)</span> 的无偏估计，即 <span class="math inline">\(\mathbb{E}\left[K(x,\vec{n_x})\right]=L^{ML}(x,\vec{n_x})\)</span>，比较式
(2) 和 (4) 可有</p>
<p><span class="math display">\[
\forall x,\quad \sum\limits_k F^k(t_i,x)P(t_i\in
\mathcal{L}^k)=\mathcal{A}(t_i)\label{partition derivate}
\]</span> 根据划分策略，将 <span class="math inline">\(F^k\)</span>
定义为 <span class="math inline">\(F^k(t_i,x)=S_kf^k(t_i,x)\)</span></p>
<blockquote>
<p><code>推导见</code>：<a href="#附录4">附录 4.
转为整体划分问题推导</a></p>
</blockquote>
<p>这样就将一个无偏估计问题转为了寻找对一个整体的划分问题。</p>
<p><strong>整体的划分选取</strong></p>
<p><span class="math inline">\(f^k(t_i,x)\)</span> 划分函数的参数 <span class="math inline">\(t_i,x\)</span>
都为三维，论文采用一种方法将划分函数进行降维近似处理，此方法参考了论文
<em>Point-based approximate color bleeding</em> 中的 nested balls: <span class="math inline">\(\mathcal{B}_h(t_i)\)</span>，其定义如下：</p>
<p><span class="math display">\[
\forall h\in \mathbb{R}^*, \quad
\mathcal{B}_h(t_i)=\{x\in\mathbb{R}^3\space s.t. \space
\underset{\vec{n_x}}{max}\space H(t_i,x,\vec{n})\geq h\} \tag{4}
\label{partition nested ball}
\]</span></p>
<blockquote>
<p><span class="math inline">\(H\)</span> 是 <span class="math inline">\(\large x\)</span> 接收到 <span class="math inline">\(t_i\)</span> VPL 的 radiance，这个 nested ball
的含义就是定义了一个对着色点 <span class="math inline">\(x\)</span> 的
radiance 贡献较为显著的区域，这个显著程度由 <span class="math inline">\(h\)</span> 决定。</p>
</blockquote>
<p>此外，考虑 receiver 正对着 emitter 的情况，即 <span class="math inline">\(\vec{n_x}=\bar{xy_i}\)</span>，此时 <span class="math inline">\(H\)</span> 达到最大值，有：</p>
<p><span class="math display">\[
\mathcal{B}_h(t_i)=\{x\in\mathbb{R}^3\space s.t. \space
\frac{||x-y_i||}{&lt;\vec{n_i},\bar{xy_I}&gt;^+}\leq D(h)\} \tag{5}
\label{nested ball}
\]</span> 其中 <span class="math inline">\(D(h)=\frac{1}{\pi}\sqrt{\frac{3\rho_x\rho_iE(t_i)}{2h}}\)</span></p>
<blockquote>
<p><code>推导见</code>：<a href="#附录5">附录 5.
整体划分选取推导</a></p>
</blockquote>
<p><span class="math inline">\(D(h)=\frac{1}{\pi}\sqrt{\frac{3\rho_x\rho_iE(t_i)}{2h}}\)</span>
与 <span class="math inline">\(\large x\)</span> 着色点无关，因此 <span class="math inline">\(\mathcal{B}_h(t_i)\)</span> 是一个 nest
ball，其边界上有点 <span class="math inline">\(y_i\)</span>，其中心位于直线 <span class="math inline">\((y_i,\vec{n_i})\)</span> 上。如下图所示</p>
<p><img src="/images/Paper Notes/GI/Forward Light Cuts A Scalable Approach to Real-Time Global Illumination.assets/image-20220717013806858.png" alt="image-20220717013806858" style="zoom:67%;"></p>
<blockquote>
<p>上图是 <span class="math inline">\(f^k(t_i,x)\)</span>
函数的二维可视化，其值从 0(黑) 到 1(白)。</p>
</blockquote>
<p>作者做了一个假设：nest ball 边界上的三维划分 <span class="math inline">\(^k(t_i,x)\)</span> 是不变的，通过如下 <span class="math inline">\(\large \mathbb{R}^3\)</span> 到 <span class="math inline">\(\large \mathbb{R}\)</span> 的映射：</p>
<p><span class="math display">\[
\forall x\in \mathbb{R}^3, \quad
d(t_i,x)=\frac{||x-y_i||}{&lt;\vec{n_i},\bar{xy_I}&gt;^+}
\]</span> 可以将三维划分将为一维划分：</p>
<p><span class="math display">\[
\forall x\in \mathbb{R}^3, \quad f^k(t_i,x)=\tilde{f}^k(d(t_i,x))
\]</span> <span class="math inline">\(\large f^k\)</span> 将会在
rendering 中用作 splat function，因此尽可能使得 <span class="math inline">\(f^k\)</span> 易于计算且
smooth，论文定义为下面一组分段线性函数</p>
<p><span class="math display">\[
\forall d\in\mathbb{R},\quad
\tilde{f}^k(d)=\begin{cases}\begin{align}&amp;1 &amp;k=0\space and
\space d\in[0,D_1] \\ &amp;\frac{d-D_{k-1}}{D_k-D_{k-1}}
&amp;k&gt;0\space and \space d\in[D_{k-1},D_k] \\
&amp;\frac{D_{k+1}-d}{D_{k+1}-D_k} &amp;k&gt;0 \space and \space d\in
[D_k,D_{k+1}]\\&amp;0 &amp;otherwise\end{align}\end{cases}
\]</span> <span class="math inline">\(\{D_k\}\)</span> 定义了每一级 VPL
的影响距离。</p>
<p><strong>参数配置</strong></p>
<p>作者为了模仿传统的层级表示，采用了子集大小几何级下降的参数配置。从前述划分策略来看，<span class="math inline">\(S_k\)</span> 影响到划分子集的结果，此外，可以将
<span class="math inline">\(S_k\)</span> 理解为子集 <span class="math inline">\(\mathcal{L}^k\)</span>
的平均面积，应几何级增加，因此定义如下参数配置：</p>
<p><span class="math display">\[
S_k=S_0\mu^k
\]</span> 其中 <span class="math inline">\(\mu &gt;1\)</span>
是用户定义的参数，论文建议 <span class="math inline">\([1.4,5]\)</span>。作者还建议定义影响距离参数，这样每个点都只会有一个可控数量的
VPL 到达，控制计算量，如：</p>
<p><span class="math display">\[
\begin{cases}D_k=\sqrt{S_0\mu^k}\\ D_{N+1}=D_{N}\end{cases}
\]</span></p>
<blockquote>
<p>个人理解见：<a href="#附录6">附录 6.
模拟传统层级的几何级下降的理解</a></p>
</blockquote>
<h1 id="实现细节">4 实现细节</h1>
<h4 id="pipeline-描述">4.1 Pipeline 描述</h4>
<p>本论文提出的技术共用到三个 geometry pass，其中两个主要 pass 用于生成
GBuffer 和生成 Splat VPLs。第三个 pass 用于处理 Divergent
triangle。如下图所示</p>
<p><img src="/images/Paper Notes/GI/Forward Light Cuts A Scalable Approach to Real-Time Global Illumination.assets/image-20220717013853277.png" alt="image-20220717013853277" style="zoom:50%;"></p>
<h4 id="divergent-三角形处理">4.2 Divergent 三角形处理</h4>
<p>在本论文提出的 indirect lighting pipeline 中包含了两个 geometry
pass：第一个 pass 关闭 tessellation stage
处理整个场景的三角形，这个阶段检测出哪些是 Divergent
三角形，并存入单独的 buffer 中。剩下的 regular 三角形送入 regular
pipeline。</p>
<p>存储 Divergent 三角形的 buffer 直接作为后续第二个 geometry pass
的输入。tessellation stage 只在这个 pass 开启，用于细分 divergent
三角形，使得它们的面积小到能够被 regular pipeline 处理。</p>
<blockquote>
<p>Tessellation stage 相比于 geometry 是开销是非常大的，因此使用一个
geometry 阶段过滤 regular 三角形，提高了效率。</p>
</blockquote>
<p>regular pipeline 即对 regular 三角形采样分级，再进行 VPL lighting
计算。</p>
<h4 id="per-triangle-random-number-generation">4.3 Per-triangle random
number generation</h4>
<p>本论文涉及的随机过程都需要随机数来完成，对于算法 1 用到的随机数 <span class="math inline">\(\large u_{t_i}\)</span>，作者使用伪随机数。mesh
中每个顶点添加一个额外的 uint32 属性，v_rand。该属性在加载 mesh
时为每个顶点生成一个 uniform 随机数。在 regular pipeline
中，每个三角形的随机数 <span class="math inline">\(\large
u_{t_i}\)</span> 是使用其三个顶点间的 <span class="math inline">\(\large
xor\)</span> 操作得到的。</p>
<p>对于 tessellation stage
细分生成的新三角形，作者通过采样一张预计算的噪声贴图得到新顶点的随机值，采样坐标为新顶点的重心坐标，最后对新三角形顶点的随机数使用同样的
<span class="math inline">\(\large xor\)</span> 操作。</p>
<p>想要以上随机数每帧更新，以上随机数最终再与一个 uniform 的全局变量
u_rand 进行 <span class="math inline">\(\large xor\)</span> 操作，这个
uniform 全局变量大概要一帧更新一次。算法如下</p>
<p><img src="/images/Paper Notes/GI/Forward Light Cuts A Scalable Approach to Real-Time Global Illumination.assets/image-20220717013914497.png" alt="image-20220717013914497" style="zoom: 67%;"></p>
<h4 id="progressive-rendering">4.4 Progressive rendering</h4>
<p>可以按照上述使用 u_rand 的方法生成多个 independent
rendering，之后累积作平均用以产生更好的渲染结果。如，从 u_rand
中为每个三角形生成两个独立的随机数，用以扰动 VPL 中心点 <span class="math inline">\(\large y_i\)</span>。平均由多个 jittered VPLs
生成的多个 independent rendering，可以提供接近公式 (4)
真实解的结果。</p>
<h1 id="remain-question">5 Remain Question</h1>
<ul class="task-list">
<li><label><input type="checkbox">划分整体过程中的整体应该怎么理解？</label></li>
<li><label><input type="checkbox">Splatting indirect illumination
技术不了解</label></li>
</ul>
<h1 id="reference">6 Reference</h1>
<p><a name="[1]">[1]</a> Gilles LAURENT, Cyril DELALANDRE, Grégoire de
LA RIVIÈRE, and Tamy BOUBEKEUR. 2016. Forward Light Cuts: A Scalable
Approach to Real-Time Global Illumination. <i>Comput. Graph. Forum</i>
35, 4 (July 2016), 79–88.</p>
<h1 id="附录">附录</h1>
<h2 id="阈值-s_0-的启发式的理解">1. <a name="附录1">阈值 <span class="math inline">\(S_0\)</span> 的启发式的理解</a></h2>
<p>以像素为球心、半径为 <span class="math inline">\(D_{near}\)</span>
的球面面积为 <span class="math inline">\(4\pi
D^2_{near}\)</span>，假设在距离 <span class="math inline">\(D_{near}\)</span> 内的 VPL
光源才能到达该像素，并且 <span class="math inline">\(N_{avg}\)</span> 个
VPL 共同作用下才能照亮该像素。由于本文中次级光源都假设为
diffuse，因此可粗略地认为 VPL 发出的 radiance
与面积成正比。这样下来，VPL 的平均面积 <span class="math inline">\(4\pi
\frac{D^2_{near}}{N_{avg}}\)</span>，高于此平均面积的三角形成为
divergent，剩下的三角形为 regular，regular 三角形生成的 VPL
具有可控的影响距离，divergent 三角形后续会进一步细分为小三角形。</p>
<h2 id="着色方程积分近似为求和的理解">2.
<a name="附录2">着色方程积分近似为求和的理解</a></h2>
<p>首先来看精确的 rendering equation，</p>
<p><span class="math display">\[
L(x,\omega_o)=\int_{\Omega^+}\space
f_r(x,\omega_i\rightarrow\omega_o)L_i(x&#39;,\omega_i)cos\theta \space
d\omega_i
\]</span> 上式为对立体角的积分，转为对光源面积的积分为</p>
<p><span class="math display">\[
L(x,\omega_o)=\int_{A}\space
f_r(x,\omega_i\rightarrow\omega_o)L_i(x&#39;,\omega_i)\frac{cos\theta
cos\theta&#39;}{||x&#39;-x||^2} \space dA
\]</span></p>
<p><span class="math inline">\(L^{ML}\)</span>
的形式即从对光源面积的积分近似而来，diffuse 下 BRDF 是常量，即 <span class="math inline">\(H\)</span> 中的 <span class="math inline">\(\large\frac{\rho_x}{\pi}\)</span>，<span class="math inline">\(L_i\)</span> 对应 <span class="math inline">\(H\)</span> 中的 <span class="math inline">\(L\)</span>。因此，<span class="math inline">\(L^{ML}(x,\vec{n_x})\)</span> 与 <span class="math inline">\(L(x,\omega_o)\)</span>
唯一不同的是一个是对总体面积的连续积分，一个是将每个 VPL
的面积视为微元的离散求和。可知，在 VPL
面积较小时，这种近似较为接近正确。</p>
<h2 id="vpl-lighting-推导">3. <a name="附录3">VPL lighting 推导</a></h2>
<p>将划分的三角形所有子集看作一个整体求和符号有：<span class="math inline">\(\sum\limits^N_{k=0}\sum\limits_{t_i^k\in
\mathcal{L}^k}=\sum\limits_{t_i\in \mathcal{L}}\)</span></p>
<blockquote>
<p>因为 <span class="math inline">\(t_i^k\)</span> 本就代表是子集 <span class="math inline">\(\mathcal{L}^k\)</span> 中的三角形，可知 <span class="math inline">\(\large \mathbb{I}_{t_i^k\in
\mathcal{L}^k}\)</span> 恒为 <span class="math inline">\(1\)</span>，因此有</p>
</blockquote>
<p><span class="math inline">\(\mathbb{E}\left[\sum\limits^N_{k=0}\sum\limits_{t_i^k\in
\mathcal{L}^k}H(t_i^k,x,\vec{n_x})F^k(t_i^k,x)\right]=\mathbb{E}\left[\sum\limits^N_{k=0}\sum\limits_{t_i^k\in
\mathcal{L}^k}H(t_i^k,x,\vec{n_x})F^k(t_i^k,x)\mathbb{I}_{t_i^k\in
\mathcal{L}^k}\right]\)</span>，</p>
<blockquote>
<p>将和的期望转为期望的和</p>
</blockquote>
<p><span class="math inline">\(=\sum\limits^N_{k=0}\sum\limits_{t_i^k\in
\mathcal{L}^k}\mathbb{E}\left[H(t_i^k,x,\vec{n_x})F^k(t_i^k,x)\mathbb{I}_{t_i^k\in
\mathcal{L}^k}\right]\)</span>，</p>
<blockquote>
<p>此时由于指示函数的存在可将三角形子集看作一个整体求和，</p>
</blockquote>
<p><span class="math inline">\(=\sum\limits_{t_i\in
\mathcal{L}}\mathbb{E}\left[H(t_i,x,\vec{n_x})F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]\)</span>，</p>
<blockquote>
<p><span class="math inline">\(H(t_i,x,\vec{n_x})\)</span>
与随机划分无关，对于期望而言是常量，可以提出期望计算外，即</p>
</blockquote>
<p><span class="math inline">\(=\sum\limits_{t_i\in
\mathcal{L}}H(t_i,x,\vec{n_x})\mathbb{E}\left[F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]\)</span>，</p>
<blockquote>
<p>接下来进行一个恒等变换，<span class="math inline">\(F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}=\sum\limits^N_{k=0}F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\)</span>，因为对于每一个 <span class="math inline">\(t_i\)</span>，只会有一个 <span class="math inline">\(k\)</span> 值使得 <span class="math inline">\(\large \mathbb{I}_{t_i\in
\mathcal{L}^k}=1\)</span>，其余都为 <span class="math inline">\(0\)</span>。</p>
</blockquote>
<p><span class="math inline">\(=\sum\limits_{t_i\in
\mathcal{L}}H(t_i,x,\vec{n_x})\sum\limits^N_{k=0}\mathbb{E}\left[F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]\)</span>，</p>
<blockquote>
<p>目前期望计算中只有指示函数与随机划分相关，且易知 <span class="math inline">\(\mathbb{E}\left[\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]=P(t_i\in \mathcal{L^k})\)</span> ，有</p>
</blockquote>
<p><span class="math inline">\(\mathbb{E}\left[F^k(t_i,x)\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]=F^k(t_i,x)\mathbb{E}\left[\mathbb{I}_{t_i\in
\mathcal{L}^k}\right]=F^k(t_i,x)P(t_i\in \mathcal{L^k})\)</span></p>
<blockquote>
<p>最终有</p>
</blockquote>
<p>$ _{t_i}H(t_i,x,)<sup>N_{k=0}F</sup>k(t_i,x)P(t_i^k)$</p>
<h2 id="转为整体划分问题推导">4.
<a name="附录4">转为整体划分问题推导</a></h2>
<p>由 <span class="math inline">\(\eqref{partition probability}\)</span>
式代入 <span class="math inline">\(\eqref{many light estimator}\)</span>
中有</p>
<p><span class="math display">\[\forall x,\quad \sum\limits_k
F^k(t_i,x)\frac{\mathcal{A}(t_i)}{S_k}=\mathcal{A}(t_i)\]</span></p>
<p><span class="math display">\[\forall x,\quad \sum\limits_k
\frac{F^k(t_i,x)}{S_k}=1\]</span></p>
<p>假设未知函数 <span class="math inline">\(f^k(t_i,x)\)</span> 使得
<span class="math inline">\(F^k(t_i,x)=S_kf^k(t_i,x)\)</span>，那么有</p>
<p>​ <span class="math inline">\(\sum\limits_kf^k(t_i,x)=1\)</span></p>
<h2 id="整体划分选取推导">5. <a name="附录5">整体划分选取推导</a></h2>
<blockquote>
<p>当 receiver 正对着 emitter 时，将 <span class="math inline">\(\vec{n_x}=\bar{xy_i}\)</span> 代入 <span class="math inline">\(\eqref{received radiance}\)</span>，有</p>
</blockquote>
<p><span class="math inline">\(\begin{align}\underset{\vec{n_x}}{max}\space
H(t_i,x,\vec{n_x})&amp;=\frac{3}{2\pi^2}\rho_i\rho_xE(t_i)\frac{(&lt;\vec{n_i},\bar{y_ix}&gt;)^2}{d_i^2}\\&amp;=\frac{3}{2\pi^2}\rho_i\rho_xE(t_i)\left(\frac{&lt;\vec{n_i},\bar{y_ix}&gt;}{||x-y_i||}\right)^2\end{align}\)</span>​</p>
<blockquote>
<p>将 <span class="math inline">\(\large H\)</span> 代入公式 <span class="math inline">\(\eqref{partition nested ball}\)</span> 有</p>
</blockquote>
<p><span class="math display">\[\frac{3}{2\pi^2}\rho_i\rho_xE(t_i)\left(\frac{&lt;\vec{n_i},\bar{y_ix}&gt;}{||x-y_i||}\right)^2
\geq h\]</span></p>
<p><span class="math display">\[\left(\frac{&lt;\vec{n_i},\bar{y_ix}&gt;}{||x-y_i||}\right)^2
\geq \frac{2h\pi^2}{3\rho_i\rho_xE(t_i)}\]</span></p>
<p><span class="math display">\[\frac{&lt;\vec{n_i},\bar{y_ix}&gt;}{||x-y_i||}
\geq \pi \sqrt{\frac{2h}{3\rho_i\rho_xE(t_i)}}\]</span></p>
<p><span class="math display">\[\frac{||x-y_i||}{&lt;\vec{n_i},\bar{y_ix}&gt;}
\leq \frac{1}{\pi} \sqrt{\frac{3\rho_i\rho_xE(t_i)}{2h}}\]</span></p>
<h2 id="模拟传统层级的几何级下降的理解">6.
<a name="附录6">模拟传统层级的几何级下降的理解</a></h2>
<p>这里的模拟层级表示而引入几何级下降的参数配置：例如满二叉树的层级表示，根节点层(0层)节点数量为
<span class="math inline">\(S_0=1\)</span> 个，往下每层数量是前一层的 2
倍，即有 <span class="math inline">\(S_k=S_0 2^k\)</span>
个，为几何级增长。</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>GI</category>
      </categories>
  </entry>
  <entry>
    <title>Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting</title>
    <url>/Paper-Notes/Ray-Tracing/Spatiotemporal-reservoir-resampling-for-real-time-ray-tracing-with-dynamic-direct-lighting.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p><code>Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting</code>
<a href="#%5B1%5D">[1]</a></p>
<p>解决 many-lights 下的直接光照的重要性采样问题。</p>
<h1 id="background">2 Background</h1>
<p>本论文将光源视为场景中的发光几何体，因此 many-lights
下的渲染方程为：着色点 <span class="math inline">\(y\)</span> 在方向
<span class="math inline">\(\vec{\omega}\)</span> 所反射的 radiance
<span class="math inline">\(L\)</span> 是对所有发光表面 <span class="math inline">\(A\)</span> 的积分： <span class="math display">\[
L(y,\vec{\omega}) = \int_A
\rho(y,\vec{yx}\leftrightarrow\vec{\omega})L_e(x\rightarrow
y)G(x\leftrightarrow y)V(x\leftrightarrow y) \space dA_x \tag{1}
\label{render equation}
\]</span> 其中，<span class="math inline">\(\vec{\omega}\)</span>
是观察方向，与相机相关；<span class="math inline">\(\rho\)</span>
与着色点 <span class="math inline">\(y\)</span> 的材质有关，论文中指
BSDF；<span class="math inline">\(L_e\)</span> 是光源表面一点 <span class="math inline">\(x\)</span> 到着色点 <span class="math inline">\(y\)</span> 方向上发出的 radiance；<span class="math inline">\(G\)</span> 几何项，包含了 inverse squared distance
和反射方向与入射方向的余弦项。</p>
<p>对渲染方程的表示进行一些符号简化，被积函数简化为 <span class="math inline">\(f(x)\)</span>，积分微元 <span class="math inline">\(dx\)</span>，有渲染方程的简化形式： <span class="math display">\[
L=\int_A f(x)\space dx,\quad where \space f(x)\equiv\rho(x)\cdot
L_e(x)\cdot G(x)\cdot V(x) \tag{2} \label{simple render equation}
\]</span></p>
<ul>
<li><strong>Importance Sampling(IS)</strong></li>
</ul>
<p>使用蒙特卡洛重要性采样方法估计 <span class="math inline">\(\eqref{simple render equation}\)</span> 式积分，从
source PDF <span class="math inline">\(p(x_i)\)</span> 中进行 <span class="math inline">\(N\)</span> 次采样，得到 <span class="math inline">\(N\)</span> 个样本，<span class="math inline">\(\eqref{simple render equation}\)</span>
积分的估计量为： <span class="math display">\[
L \approx \langle L \rangle_{IS}^N =\frac{1}{N}\sum\limits_{i=1}^N
\frac{f(x_i)}{p(x_i)} \tag{3} \label{IS MC}
\]</span> 其中如果在 <span class="math inline">\(f(x)\)</span>
非零时，<span class="math inline">\(p(x)\)</span> 都为正，IS
则为无偏估计；并且可以通过选取与 <span class="math inline">\(f(x)\)</span> 相关的 <span class="math inline">\(p(x)\)</span> 形式，来降低方差。</p>
<ul>
<li><a name="MIS"><strong>Multiple Importance
Sampling(MIS)</strong></a></li>
</ul>
<p>由于 <span class="math inline">\(f(x)\)</span>
的复杂性（包含多项），直接采样与 <span class="math inline">\(f(x)\)</span>
成比例（相关）的分布是不可实行的，而通常采样与 <span class="math inline">\(f(x)\)</span> 中单项成比例的分布，例如 <span class="math inline">\(\rho\)</span> BSDF、<span class="math inline">\(L_e\)</span> 等。因此，定义 <span class="math inline">\(M\)</span> 个采样策略 <span class="math inline">\(p_s\)</span> (概率分布)，MIS 从每个采样策略 <span class="math inline">\(s\)</span> 中采样出 <span class="math inline">\(N_s\)</span> 个样本，再将这些样本组合到一个
weighted-estimator 中，如下： <span class="math display">\[
L\approx \langle
L\rangle_{MIS}^{M,N}=\sum\limits_{s=1}^M\frac{1}{N_s}\sum\limits_{i=1}^{N_s}w_s(x_i)\frac{f(x_i)}{p(x_i)}
\tag{4}\label{MIS MC}
\]</span> 其中，只要权重 <span class="math inline">\(w_s\)</span>
可以组成一个归一化整体，MIS 就是无偏估计，即 <span class="math inline">\(\sum^M_{s=1}w_s(x)=1\)</span>。一种流行且较好的启发式选择为
<span class="math inline">\(\Large w_s(x)=\frac{N_s p_s(x)}{\sum_j
N_jp_j(x)}\)</span>，等价于采样 <span class="math inline">\(M\)</span>
个独立采样策略的混合分布。</p>
<h2 id="resample-importance-samplingris">2.1 Resample Importance
Sampling(RIS)</h2>
<p>由 <span class="math inline">\(\eqref{MIS MC}\)</span> 可以看出，MIS
是对着色项的线性组合的采样，而另一种可选的采样策略是对其中一些项的乘积进行近似成比例地采样，这就是
RIS 实现的策略。RIS 从 source PDF <span class="math inline">\(p\)</span>
中生成 <span class="math inline">\(M\geq 1\)</span> 个候选样本 <span class="math inline">\(\boldsymbol{x}={x_1,\cdots,x_M}\)</span>，<span class="math inline">\(p\)</span> 选择次佳但易于采样地分布，如 $p L_e
$。然后从这个候选池中使用以下离散概率分布进行随机选取 <span class="math inline">\(z\in\{1,\cdots,M\}\)</span>， <span class="math display">\[
p(z\mid
\boldsymbol{x})=\frac{\mathrm{w}(x_z)}{\sum^M_{i=1}\mathrm{w}(x_i)}
\quad with \quad \mathrm{w}(x)=\frac{\hat{p}(x)}{p(x)}
\tag{5}\label{discrete PDF}
\]</span> 其中，<span class="math inline">\(p(x)\)</span> 为易于采样的
source PDF；<span class="math inline">\(\hat{p}(x)\)</span> 为期望得到的
target PDF，例如无法直接采样的分布 <span class="math inline">\(\hat{p}\propto \rho\cdot L_e \cdot
G\)</span>。对于 <strong>1-sample RIS</strong> 估计量，因此有 <span class="math inline">\(y\equiv x_z\)</span>，有 <span class="math display">\[
L\approx \langle L \rangle^{1,M}_{RIS} =
\frac{f(y)}{\hat{p}(y)}\cdot\left(\frac{1}{M}\sum\limits^M_{j=1}\mathrm{w}(x_j)\right)
\tag{6} \label{1-sample RIS}
\]</span></p>
<blockquote>
<p>对 <span class="math inline">\(\eqref{1-sample RIS}\)</span>
的理解：RIS 一次采样相当于得到一个 target PDF <span class="math inline">\(\hat{p}\)</span> 的样本 <span class="math inline">\(y\)</span>，这个分布是可以采用多项乘积的形式。回顾重要性采样
IS，要使得被积函数对应的估计量 <span class="math inline">\(\langle
L\rangle\)</span> 无偏，需要估计量的形式为 <span class="math inline">\(f(y)/p_y(y)\)</span>，注意这里的 <span class="math inline">\(p_y(y)\)</span> 是 <span class="math inline">\(y\)</span> 的真实 PDF，而不是 target
PDF。因此需要乘上括号部分，来近似 <span class="math inline">\(y\)</span>
的真实
PDF，但这种近似并不总是无偏的，会在某些情况下引入偏差，之后部分会有具体分析。</p>
</blockquote>
<p>多次执行 RIS，然后取平均，可以得到一个 N-sample RIS 估计量： <span class="math display">\[
L \approx\langle
L\rangle^{M,N}_{RIS}=\frac{1}{N}\sum\limits^N_{i=1}\left(\frac{f(y_i)}{\hat{p}(y_i)}\cdot
\left(\frac{1}{M}\sum^M_{j=1}\mathrm{w}(x_{ij})\right)\right) \tag{7}
\label{N-sample RIS}
\]</span> 在 <span class="math inline">\(M,N\geq 1\)</span> 并且 <span class="math inline">\(f\)</span> 非零时，<span class="math inline">\(p,\hat{p}\)</span> 都为正，RIS
则为无偏估计。理论上 <span class="math inline">\(M\)</span> 和 <span class="math inline">\(N\)</span>
存在与方差有关的最优比，但实际中，这个比值难以事先预知。本文中采用简单形式
<span class="math inline">\(N=1\)</span>，即 1-sample
RIS。有算法流程：</p>
<ol type="1">
<li><p>预设 source PDF <span class="math inline">\(p\)</span>，文中实验将其设为在面光源上的面积采样分布；target
PDF <span class="math inline">\(\hat{p}\)</span>，文中实验设置为
unshadowed path contribution 部分，即 <span class="math inline">\(\rho
\cdot L_e\cdot G\)</span>，不包含 visibility 项</p></li>
<li><p>设置初始为空的权重列表 <span class="math inline">\(\textup{W}\)</span>，初始为 <span class="math inline">\(0\)</span> 的权重累积和 <span class="math inline">\(\textup{w}_{sum}\)</span>，初始为空的候选样本列表
<span class="math inline">\(\boldsymbol{x}\)</span></p></li>
<li><p>对 source PDF <span class="math inline">\(p\)</span> 进行 <span class="math inline">\(M\)</span> 次采样：</p>
<ul>
<li><p>第 <span class="math inline">\(i\)</span> 次采样的随机样本为
<span class="math inline">\(x_i\)</span>，放入候选样本列表 <span class="math inline">\(\boldsymbol{x}\)</span></p></li>
<li><p>计算样本 <span class="math inline">\(x_i\)</span> 的权重 <span class="math inline">\(\mathrm{w}_i=\Large
\frac{\hat{p}(x_i)}{p(x_i)}\)</span>，并放入权重列表 <span class="math inline">\(\mathrm{W}\)</span></p></li>
<li><p>累积权重和 <span class="math inline">\(\mathrm{w}_{sum}=\mathrm{w}_{sum}+\mathrm{w}_i\)</span></p></li>
</ul></li>
<li><p>对候选样本权重进行归一化：<span class="math inline">\(\mathrm{w}_i= \Large
\frac{\mathrm{w}_i}{\mathrm{w}_{sum}}\)</span></p></li>
<li><p>将归一化权重作为候选样本的概率分布，采样候选样本列表得到最终选出的样本
<span class="math inline">\(y=x_z\)</span></p></li>
<li><p>返回选出的样本 <span class="math inline">\(y=x_z\)</span>
以及本次 RIS 的候选样本的权重累积和 <span class="math inline">\(\textup{w}_{sum}\)</span></p></li>
</ol>
<p>假设像素 <span class="math inline">\(q\)</span> 对应 target PDF <span class="math inline">\(\hat{p}(q)\)</span>，以及其渲染方程积分 <span class="math inline">\(f_q\)</span>，1-sample RIS 算法伪代码如下：</p>
<p><a name="Algo 1"></a></p>
<p><img src="/images/Paper Notes/Ray Tracing/Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting.assets/image-20220717215808514.png" alt="image-20220717215808514" style="zoom: 50%;"></p>
<center>
算法 1
</center>
<p><span class="math inline">\(\eqref{1-sample RIS}\)</span>
式的计算需要候选样本数量 <span class="math inline">\(M\)</span>，这是算法输入，是已知的；<span class="math inline">\(\mathrm{w}_{sum}\)</span>，算法输出；<span class="math inline">\(\hat{p}(y)\)</span>，<span class="math inline">\(y\)</span> 是算法选出的样本，<span class="math inline">\(\hat{p}\)</span> 是预设 target
PDF，本文使用的是代入样本到被积函数后的颜色转为亮度。</p>
<h2 id="weighted-reservoir-samplingwrs">2.2 Weighted Reservoir
Sampling(WRS)</h2>
<p>WRS 是一类从一个 stream <span class="math inline">\(\{x_1,x_2,\cdots,x_M\}\)</span>
中通过一遍操作来随机采样 <span class="math inline">\(N\)</span>
个元素的算法，该 stream 数据中每个元素 <span class="math inline">\(x_i\)</span> 都关联一个权重 <span class="math inline">\(\textup{w}(x_i)\)</span>，<span class="math inline">\(x_i\)</span> 被选中的概率为 <span class="math display">\[
P_i = \frac{\mathrm{w}(x_i)}{\sum^M_{j=1}\mathrm{w}(x_j)} \tag{8}
\label{reservoir CDF}
\]</span> Reservoir sampling 对每个元素只进行一次操作，仅需要 <span class="math inline">\(N\)</span> 个元素保留在内存中，并且 stream 的长度
<span class="math inline">\(M\)</span> 不必事先预知。Reservoir sampling
算法，分为有放回采样与无放回采样，由于蒙特卡洛积分积分的采样通常是相互独立的，因此本论文只考虑较为简单的有放回采样算法。</p>
<p>Reservoir sampling 按照 input stream
的次序来处理其中的元素，保存一个具有 <span class="math inline">\(N\)</span> 个样本的
reservoir。<strong>在任何时刻，reservoir sampling 保持采样自 desired PDF
的 reservoir 中的样本的概率分布仍然是 desired
PDF</strong>。即当处理一个来自 stream 的新元素时，更新 reservoir
来<strong>保持其中的样本分布的不变性</strong>，更新操作为：</p>
<p>在处理了 m 个样本之后，样本 <span class="math inline">\(x_i\)</span>
在 reservoir 出现的概率为 <span class="math inline">\(\textup{w}(x_i)/\sum^m_{j=1}\textup{w}(x_j)\)</span>。处理下一个新元素
<span class="math inline">\(x_{m+1}\)</span>
的更新规则为，使用下一个样本 <span class="math inline">\(x_{m+1}\)</span> 以以下概率随机替换 reservoir
中样本 <span class="math inline">\(x_i\)</span> ： <span class="math display">\[
\frac{\mathrm{w}(x_{m+1})}{\sum^{m+1}_{j=1}\mathrm{w}(x_j)}
\]</span> 因此任意之前的样本 <span class="math inline">\(x_i\)</span> 在
reservoir 内的概率(即已在 reservoir
中，且不会被下一个样本替换掉的概率)为： <span class="math display">\[
\frac{\mathrm{w}(x_i)}{\sum^m_{j=1}\mathrm{w}(x_j)}\left(1-\frac{\mathrm{w}(x_{m+1})}{\sum^{m+1}_{j=1}\mathrm{w}(x_j)}\right)=\frac{\mathrm{w}(x_i)}{\sum^{m+1}_{j=1}\mathrm{w}(x_j)}
\]</span> &gt; 可以看出 N 个样本的 reservoir
接收到一个新元素的更新规则是，逐个样本执行更新规则</p>
<p>观察 <span class="math inline">\(\eqref{reservoir CDF}\)</span>
中给出的 reservoir CDF 可知，在处理过一个新的元素后 reservoir
中元素的概率分布依然不变。N=1 时的 WRS 算法如下：</p>
<p><img src="/images/Paper Notes/Ray Tracing/Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting.assets/image-20220717215902454.png" alt="image-20220717215902454" style="zoom:50%;"></p>
<h1 id="streaming-ris-with-spatiotemporal-reuse">3 Streaming RIS With
Spatiotemporal Reuse</h1>
<p>RIS 算法对多个 source PDF
的样本进行重采样，重采样的概率分布结合了被积函数的(unshadowed) target
PDF 以及源分布，而 target PDF 可以近似为渲染方程 <span class="math inline">\(\eqref{simple render equation}\)</span>
的被积函数每一项对应分布的乘积形式，这种乘积形式要比 <a href="#MIS">MIS</a>
的线性组合形式更接近被积函数的真实分布。因此，<strong>RIS
最终选出的样本可近似看作一次 MIS
采样(包含多种采样策略)的样本，且理论上更优。</strong></p>
<p>WRS 算法能够高效地维护一个 reservoir，在保持 reservoir
中原有样本所服从分布不变的情况下，不断纳入新的随机样本，使得 reservoir
样本不断逼近目标估计量的期望值。</p>
<p>理论上，对 RIS 应用 WRS 可以得到一个使 RIS
样本不断逼近期望值的高效算法，当然由于本文的 target PDF 选取 unshadowed
path contribution，最终会由于 visibility 无法继续提高 RIS
样本。下面介绍如何应用 WRS 算法，从而得到 Streaming RIS 算法。</p>
<h2 id="streaming-ris-using-reservoir-sampling">3.1 Streaming RIS Using
Reservoir Sampling</h2>
<p>RIS 算法为每个像素 <span class="math inline">\(q\)</span>
独立地生成候选样本，再在候选样本中以 target PDF <span class="math inline">\(\hat{p}_q\)</span> 重新采样，选出最终的样本。对
RIS 可以直接应用 WRS 算法，RIS 选出的样本及其对应的权重，可以直接用于
WRS 算法更新其 reservoir。论文中，通过均匀采样 emitter
的面积，并且使用无阴影的路径的贡献 <span class="math inline">\(\hat{p}(x)=\rho(x)\cdot L_e(x)\cdot G(x)\)</span>
作为 target distribution，仅对 reservoir 中的 <span class="math inline">\(N\)</span> 个样本进行 trace shadow ray。Streaming
RIS 算法如下，</p>
<p><a name="Algo 3"></a></p>
<p><img src="/images/Paper Notes/Ray Tracing/Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting.assets/image-20220717215920480.png" alt="image-20220717215920480" style="zoom:50%;"></p>
<center>
算法 3
</center>
<p>这里每次更新 reservoir ：候选样本 <span class="math inline">\(x_i\)</span> 及其权重 <span class="math inline">\(\eqref{discrete PDF}\)</span> 中的 <span class="math inline">\(\Large
\textup{w}_{i}=\frac{\hat{p}(x_i)}{p(x_i)}\)</span>。</p>
<h2 id="spatiotemporal-reuse">3.2 Spatiotemporal Reuse</h2>
<h3 id="复用-reservoir">3.2.1 复用 reservoir</h3>
<p>Reservoir
样本累积了目前见过的所有样本信息，如果可以基于某些策略(如接下来的Spatiotemporal)，选择与当前像素<strong>样本分布相似</strong>的某些像素的
reservoir，再与当前像素的 reservoir
进行结合复用，就能大幅提高当前像素的采样率。</p>
<p>一种易想到的简单复用方式使用 2-pass 算法：第一个 pass
先为每一个像素生成其候选样本；第二个 pass
再结合自身与其周围像素的候选样本，进行重采样。但这种方式带来的内存开销非常大，需要存储每个像素的候选样本，即
input stream (如前述算法中 <span class="math inline">\(M\)</span>
个)。</p>
<p>为了规避内存开销问题，论文使用了 Reservoir sampling
的一个重要性质，该性质可以在不访问其他 reservoir 的 input stream
的条件下，结合多个 reservoir。如果多个 reservoir
中样本分布相似，同样可以将多个 reservoir 输入到一个新的 reservoir
中，这个新的 reservoir 汇集了所有输入 reservoir
的样本信息，并且由于相似的样本分布，而得到采样率/质量更高的样本。</p>
<p>以两个 reservoir 为例：每个 reservoir 当前的状态包含当前选择的样本
<span class="math inline">\(y\)</span> 以及其 sum weight <span class="math inline">\(\textup{w}_{sum}\)</span>，作为一个新的 reservoir
的输入，重采样出一个结合两个输入 reservoir
的新样本和新权重。这种方式只需要访问 reservoir 的当前状态，避免了存储
input stream 的开销，但结果在数学形式上等同于在两个 reservoir 的 input
streams 重采样。</p>
<p>复用 reservoir 只是与当前 reservoir
的样本分布相似，但不完全相同，因此需要根据样本分布调整权重。假设 <span class="math inline">\(q\)</span> 为当前像素，<span class="math inline">\(q&#39;\)</span> 为复用像素，论文中将新的 reservoir
中的样本权重使用 <span class="math inline">\(\hat{p}_q(r.y)/\hat{p}_{q&#39;}(r.y)\)</span>
进行调整，采用 <a href="#Algo%203">算法 3</a> 中的符号得到最终新的权重为
<span class="math inline">\(\hat{p}_q(r.y)\cdot r.W \cdot
r.M\)</span>。对于任意数量的 reservoir 都可以以这种方式结合，下面是
<span class="math inline">\(k\)</span> 个 reservoir 结合的算法，只有
O(k) 的复杂度：</p>
<p><a name="Algo 4"></a></p>
<p><img src="/images/Paper Notes/Ray Tracing/Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting.assets/image-20220717215937128.png" alt="image-20220717215937128" style="zoom:50%;"></p>
<center>
算法 4
</center>
<p>这里新的 reservoir 更新：输入 reservoir 样本，其对应权重根据 <span class="math inline">\(\eqref{discrete PDF}\)</span> 理论上应为 <span class="math inline">\(\Large
\frac{\hat{p}_q(r.y)}{p_q(r.y)}\)</span>，但 <span class="math inline">\(y\)</span> 的真实分布是未知的，这里使用 <span class="math inline">\(\hat{p}_q(r.y)\cdot r.\textup{W}\)</span>
来近似，<span class="math inline">\(r.M\)</span>
则是为了对不同候选样本数量的 reservoir 的加权。<strong>在复用 reservoir
样本时，新的 reservoir 的 source PDF 就是输入 reservoir 样本的分布，即
<span class="math inline">\(p_y(y)\)</span>。</strong></p>
<p>下面介绍，选用那些像素的 reservoir 来增强当前像素。</p>
<h3 id="spatial-reuse">3.2.2 Spatial Reuse</h3>
<p>空间复用基于一种观察：相邻像素的 target PDFs
通常存在显著联系，通常具有相似的 Geometry 和
BSDF。因此可以结合相邻像素的 RIS 候选样本来增加自身的 RIS
候选样本数量。</p>
<p>空间复用相邻像素的 reservoir 的步骤如下：</p>
<ol type="1">
<li>使用 <a href="#Algo%203">算法 3</a> RIS(q) 为每个像素生成 <span class="math inline">\(M\)</span> 个候选样本，并将 reservoir 的结果存储在
image-sized buffer 中。</li>
<li>每个像素选取 <span class="math inline">\(k\)</span>
个邻居像素，并使用 <a href="#Algo%204">算法 4</a> 结合 <span class="math inline">\(k\)</span> 个邻居像素的 reservoirs 以及自身的
reservoir。</li>
</ol>
<p>每像素的复杂度为 <span class="math inline">\(O(k+M)\)</span>，但产生了 <span class="math inline">\(k\cdot M\)</span> 个候选样本。Spatial Reuse
可以重复多次执行，其中每次都是上个 reuse pass 的输出。对于执行了 <span class="math inline">\(n\)</span>
次空间复用迭代，假设每次迭代都复用不同的相邻像素，那么时间复杂度为 <span class="math inline">\(O(kn+M)\)</span>，但产生了 <span class="math inline">\(k^n\cdot M\)</span> 个候选样本。</p>
<p>注意，虽然空间复用可以大幅增多候选样本，即采样率，进而提高图像质量，但这种提升不会随着迭代次数增加而一直增加下去。直至迭代次数多到相邻像素的像素信息被全部利用到，图像质量不再提升。</p>
<h3 id="temporal-reuse">3.2.3 Temporal Reuse</h3>
<p>上一帧的像素同样可以提供可复用的候选样本。当完成绘制一帧时，将当前帧的像素的
reservoir 结果存入历史帧信息中。在下一帧时利用 motion vector
找到像素的对应关系，复用对应像素的历史 reservoir。</p>
<h3 id="visibility-reuse">3.2.4 Visibility Reuse</h3>
<p>不幸的是，即使有无限数量的候选样本，RIS
也无法达到无噪声的图像质量。尽管理论上，<span class="math inline">\(M\rightarrow \infty\)</span> 时，reservoir
样本的分布趋向于 target PDF <span class="math inline">\(\hat{p}\)</span>，但 <span class="math inline">\(\hat{p}\)</span>
并没有完美采样渲染方程的被积函数。因为，在实际中，<span class="math inline">\(\hat{p}\)</span> 通常设置为了无阴影的 path
contribution，当 <span class="math inline">\(M\)</span> 不断增大时，由于
visibility 项噪声会越来越明显，并且 visibility
噪声会表现的非常严重。论文为了解决这一问题，同样对 visibility
进行复用。</p>
<p>在进行 spatial reuse 或者 temporal reuse 之前，先<strong>对每个像素的
reservoir 选择的样本 <span class="math inline">\(y\)</span> 进行
visibility 评估。如果 <span class="math inline">\(y\)</span>
被遮挡，则丢弃该
reservoir，即被遮挡的样本不会传递给其相邻像素。</strong>visibility
评估需要对当前像素向复用的 reservoir 样本发射 shadow ray。</p>
<h3 id="完整算法描述">3.2.5 完整算法描述</h3>
<p>将上述过程全部考虑进来，得到完整的算法流程：</p>
<ol type="1">
<li>首先为每个像素生成 <span class="math inline">\(M\)</span> 个 light
candidates，进行重采样，选出最终的样本</li>
<li>对选出的样本进行 visibility 测试，从中丢弃掉被遮挡的样本</li>
<li>结合每个像素的 reservoir 和上一帧的对应像素的 reservoir</li>
<li>执行 <span class="math inline">\(n\)</span> 次空间复用迭代，利用附近
<span class="math inline">\(k\)</span> 个像素的 reservoirs</li>
</ol>
<p>算法伪代码如下所示：</p>
<p><a name="Algo 5"></a></p>
<p><img src="/images/Paper Notes/Ray Tracing/Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting.assets/image-20220717215954040.png" alt="image-20220717215954040" style="zoom:50%;"></p>
<h1 id="eliminating-bias-in-multi-distribution-ris">4 Eliminating Bias
in Multi-Distribution RIS</h1>
<p>虽然上一部分引入复用样本，提升了图片质量，但由于每个像素都具有不同的积分区域和
target
PDF，复用会引入潜在的偏差，导致蒙特卡洛积分成为有偏估计。下面通过分析
RIS 权重，推导出偏差的源头。这里假设所有的候选样本都来自于同一源分布
<span class="math inline">\(p\)</span> 。</p>
<h2 id="分析-ris-权重">4.1 分析 RIS 权重</h2>
<p><span class="math inline">\(\eqref{1-sample RIS}\)</span> 式的
1-sample RIS 可以重写为： <span class="math display">\[
\langle
L\rangle^{1,M}_{RIS}=f(y)\cdot\left(\frac{1}{\hat{p}(y)}\frac{1}{M}\sum\limits^M_{j=1}\mathrm{w}(x_j)\right)=f(y)\mathrm{W}(\boldsymbol{x},z)
\tag{9}\label{regroup 1-sample RIS}
\]</span> 其中 <span class="math inline">\(\boldsymbol{x}=\{x_1,x_2,\cdots,x_M\}\)</span>，source
PDF 的 <span class="math inline">\(M\)</span> 次独立采样；<span class="math inline">\(\textup{W}\)</span> 是 RIS 选出的样本 <span class="math inline">\(y\equiv x_z\)</span>
对应的权重，挑选过程是随机，可知是个随机变量。</p>
<blockquote>
<p>理解 <span class="math inline">\(y\)</span> 与 <span class="math inline">\((\boldsymbol{x},z)\)</span> 以及 <span class="math inline">\(\textup{W}(\boldsymbol{x},z)\)</span>：首先 <span class="math inline">\((\boldsymbol{x},z)\)</span> 表示从 <span class="math inline">\(M\)</span> 次独立采样得到的 <span class="math inline">\(M\)</span> 个样本中，随机挑选出第 <span class="math inline">\(z\)</span> 个。因此 <span class="math inline">\(\boldsymbol{x}\)</span> 是 <span class="math inline">\(M\)</span> 个独立同分布于 source PDF 的 <span class="math inline">\(M\)</span> 维随机变量，有 <span class="math inline">\(p(\boldsymbol{x})=\prod\limits^M_{i=1}p_i(x_i)\)</span>；<span class="math inline">\(z\)</span> 服从一个离散分布，有 <span class="math inline">\(\eqref{discrete PDF}\)</span> 描述的条件形式。其次
<span class="math inline">\(y\)</span> 是 RIS
算法得到的样本，虽然采样过程已知，但 <span class="math inline">\(y\)</span> 的真实概率密度没有解析形式。但根据 RIS
算法，<span class="math inline">\(y\)</span> 与 <span class="math inline">\((\boldsymbol{x},z)\)</span> 有关，但对于一个样本
<span class="math inline">\(y\)</span>，会有很多种 <span class="math inline">\((\boldsymbol{x},z)\)</span> 的组合，例如 <span class="math inline">\(x_1=y\)</span>，<span class="math inline">\((x_2,\cdots,x_M)\)</span> 随机选择，同理也可以有
<span class="math inline">\(x_2=y\)</span>，因为 <span class="math inline">\(\boldsymbol{x}\)</span> 是 <span class="math inline">\(M\)</span> 次独立采样，每次采样都有可能得到样本
<span class="math inline">\(y\)</span>。</p>
<p>对于 <span class="math inline">\(\textup{W}\)</span>，虽然文中公式形式为 <span class="math inline">\(\textup{W}(\boldsymbol{x},z)\)</span>，但实际意义应该是，RIS
采样得到样本 <span class="math inline">\(y\)</span> 时对应的权重，即
<span class="math inline">\(x_z=y\)</span> 的权重。换句话说，给定样本
<span class="math inline">\(y\)</span>
时的权重，<strong>权重更精确的形式</strong>是： <span class="math display">\[
\mathop{\textup{W}}\limits_{x_z=y}(\boldsymbol{x},z)=\frac{1}{\hat{p}(y)}\frac{1}{M}\sum\limits^M_{j=1}\textup{w}(x_j)
\tag{10} \label{RIS weight}
\]</span> 即在 <span class="math inline">\(x_z=y\)</span>
的条件下的权重。</p>
</blockquote>
<p>首先猜想如何使得 RIS 估计量是个无偏估计？虽然 <span class="math inline">\(y\)</span> 的真实分布是未知的，但根据 <span class="math inline">\(\eqref{IS MC}\)</span> 式的重要性采样理论，这里的
<span class="math inline">\(\textup{W}(\boldsymbol{x},z)\)</span>
需要充当 <span class="math inline">\(1/p(y)\)</span>
的作用才能得到无偏估计，注意这里的 <span class="math inline">\(p(y)\)</span> 指的是 <span class="math inline">\(y\)</span> 的真实 PDF，而不是 source
PDF，为了区分，我们重写为 <span class="math inline">\(p_y(y)\)</span>。
如 <span class="math inline">\(\eqref{RIS weight}\)</span>
式描述，对于给定的 RIS 样本 <span class="math inline">\(y\)</span>，可能有很多种 <span class="math inline">\((\boldsymbol{x},z)\)</span>，因此 <span class="math inline">\(\textup{W}(\boldsymbol{x},z)\)</span>
是一个随机变量，要想使得 RIS 无偏，则需要 <span class="math inline">\(\textup{W}(\boldsymbol{x},z)\)</span> 的期望值等于
<span class="math inline">\(1/p_y(y)\)</span>。</p>
<p><a href="#Algo%204">算法 4</a> 中将多个 reservoir 整合为一个新的
reservoir 时，每个 reservoir 的权重根据 <span class="math inline">\(\eqref{discrete PDF}\)</span> 理论上应为 <span class="math inline">\(\Large \frac{\hat{p}(y)}{p_y(y)}\)</span>，而算法
4 使用的近似为 <span class="math inline">\(\hat{p}(y)\cdot
\textup{W}\)</span>，这里则是用了 <span class="math inline">\(\textup{W}\)</span> 作为 <span class="math inline">\(1/p_y(y)\)</span> 的估计量。额外的 <span class="math inline">\(M\)</span> 则是为了对不同候选样本数量的 reservoir
进行的加权。</p>
<h2 id="biased-ris">4.2 Biased RIS</h2>
<p>我们将第 <span class="math inline">\(i\)</span>
次采样所服从分布的概率密度写为 <span class="math inline">\(p_i(x_i)\)</span>，候选样本的联合概率密度有 <span class="math display">\[
p(\boldsymbol{x})=\prod\limits^M_{i=1}p_i(x_i)
\]</span> <span class="math inline">\(\eqref{discrete PDF}\)</span>
式中候选样本的概率密度重写为 <span class="math display">\[
p(z|\boldsymbol{x})=\frac{\textup{w}_z(x_z)}{\sum^M_{i=1}\textup{w}_i(x_i)}
\quad where \space \space
\textup{w}_i(x_i)=\frac{\hat{p}(x_i)}{p_i(x_i)}
\]</span> 候选样本与选取的样本索引 <span class="math inline">\(z\)</span> 的联合概率密度为 <span class="math display">\[
p(\boldsymbol{x},z)=p(\boldsymbol{x})\cdot p(z\mid
\boldsymbol{x})=\left(\prod\limits^M_{i=1}p_i(x_i)\right)\cdot
\frac{\mathrm{w}_z(x_z)}{\sum^M_{i=1}\mathrm{w}_i(x_i)} \tag{11}
\label{joint PDF}
\]</span> 对于给定样本值 <span class="math inline">\(y\)</span>，可能有非常多的 <span class="math inline">\((\boldsymbol{x},z)\)</span> 组合，对于采样出值为
<span class="math inline">\(y\)</span> 的样本 <span class="math inline">\(x_i\)</span> 要满足 <span class="math inline">\(p_i(y)=p(\boldsymbol{x},i)&gt;0\)</span>，因此所有可能情况有：
<span class="math display">\[
Z(y)=\{i\mid 1\leq i\leq M \wedge p_i(y)&gt;0 \} \tag{12} \label{all z}
\]</span> 因此最终输出 <span class="math inline">\(y\)</span> 的 PDF
可以由 <span class="math inline">\(\eqref{joint PDF}\)</span>
的联合概率密度和 <span class="math inline">\(\eqref{all z}\)</span>
的所有可能取值得到 <span class="math display">\[
p_y(y)=\sum\limits_{i\in Z(y)}\underbrace{\int\cdots\int}_{M-1}
p(\boldsymbol{x}^{i\rightarrow y},i) \space \underbrace{dx_1\cdots
dx_M}_{M-1} \tag{13} \label{y PDF}
\]</span> $p(^{iy},i) $ 是参照 <span class="math inline">\(\eqref{joint
PDF}\)</span> 代入 <span class="math inline">\(z=i,x_i=y\)</span> 得到的
<span class="math inline">\((x_1,\cdots,x_{i-1},y,x_{i+1},\cdots,x_M)\)</span>
的联合概率密度，对每种可能取值下的联合概率密度求 <span class="math inline">\(y\)</span> 的边缘概率密度 <span class="math inline">\(p_i(y)\)</span>，并求和得到 <span class="math inline">\(p_y(y)\)</span>。</p>
<p>验证 RIS 是否无偏，需要知道 <span class="math inline">\(\eqref{RIS
weight}\)</span> 中描述的权重的期望是否为 <span class="math inline">\(1/p_y(y)\)</span>，求解其期望如下： <span class="math display">\[
\mathop{\mathbb{E}}\limits_{x_z=y}\left[\textup{W}(\boldsymbol{x},z)\right]=\frac{1}{p_y(y)}\frac{|Z(y)|}{M}
\tag{14} \label{weight expectation}
\]</span> 由 <span class="math inline">\(\eqref{all z}\)</span>
可知，<strong>只有在所有候选样本分布都不为 0 时，上式的权重期望才为
<span class="math inline">\(1/p_y(y)\)</span>，RIS
才是无偏的</strong>。</p>
<p>如果部分积分区域上的 source PDF 为 0，则有 <span class="math inline">\(\large\frac{|Z(y)|}{M} &lt; 1\)</span>，
此时，重要性采样的 inverse PDF
会偏小，积分结果会偏小，带来能量损失。</p>
<h2 id="unbiased-ris">4.3 Unbiased RIS</h2>
<p>上一节给出了 RIS 有偏的情况，即会有部分样本的 source PDF 为
0。这是由于 visibility 项导致的，我们在复用其他像素的 reservoir
时，并没有考虑这些 reservoir 样本是否对当前像素可见。同样，在执行 <a href="#Algo%203">算法 3</a> streaming RIS 时，会根据 shadow ray
丢弃掉部分不可见候选样本，但在复用阶段，这些被丢弃的样本可能对当前像素是可见的，这就损失了部分能量。</p>
<p>因此若想得到无偏 RIS 则需要对 <span class="math inline">\(\eqref{RIS
weight}\)</span> 和 <span class="math inline">\(\eqref{weight
expectation}\)</span> 的候选样本的总数 <span class="math inline">\(M\)</span>
进行调整，调整为当前像素可见的候选样本总数，设置 <span class="math inline">\(m(x_z)=\frac{1}{M}\)</span>，新的权重及其期望形式为
<span class="math display">\[
\begin{align}
\mathop{\textup{W}}\limits_{x_z=y}(\boldsymbol{x},z)&amp;=\frac{1}{\hat{p}(x_z)}\left[m(x_z)\sum\limits^M_{j=1}\textup{w}(x_j)\right]
\\
\mathop{\mathbb{E}}\limits_{x_z=y}\left[\textup{W}(\boldsymbol{x},z)\right]&amp;=\frac{1}{p_y(y)}\sum\limits_{i\in
Z(y)}m(x_z)
\end{align}
\]</span> 因此当 <span class="math inline">\(\sum\limits_{i\in
Z(y)}m(x_z)=1\)</span> 时，RIS 为无偏估计。如何保证这一条件？</p>
<h2 id="a-practical-algorithm-for-unbiased-reuse">4.4 A Practical
Algorithm for Unbiased Reuse</h2>
<p>本文使用光源分布作为 source PDF，候选样本以及选出的 reservoir
样本都是光源面上一点。并且对于直接光照采样，只会采样着色点可见光源，即
<span class="math inline">\(\eqref{weight expectation}\)</span>
中使用的候选样本分布都不为
0，因此对于初始候选样本重采样的过程是无偏的。但对于reservoir
重用过程，新的 reservoir 的 source PDF 变成了，每个 reservoir
样本的分布，这个是未知的，因此重用过程的 source PDF
可能互不相同。重用过程引入 bias 的原因是：尽管复用 reservoir
对其像素可见，但对于当前像素可能不可见，这就导致了新 reservoir 的部分
source PDF 为 0，为 <span class="math inline">\(\eqref{weight
expectation}\)</span> 引入了 bias。</p>
<p>论文中提出一种无偏 RIS 算法，该算法基于一种观察：尽管复用 reservoir
样本 PDF (即新 reservoir 的 source
PDF)不可知，但当样本对当前像素不可见时，其 PDF 为 0。因此可以在复用
reservoir 样本之前，先进行 visibility 测试，如果不可见，设置其 PDF 为
0，即丢弃掉该样本，也就是 visibility
reuse。相比有偏算法，无偏算法开销很大，因为需要对每个复用 reservoir
样本发射一条 shadow ray。</p>
<h1 id="design-and-implementation">5 Design and Implementation</h1>
<p>作者为有偏与无偏算法选用不同的参数配置，以达到两种算法的开销近似。</p>
<p><strong><span class="math inline">\(M=32\)</span>
个初始候选样本的生成</strong>：根据三角面片发光的 power
重要性采样选择一个三角形，然后在三角形上均匀采样一个点 <span class="math inline">\(x\)</span>，即 source PDF <span class="math inline">\(p(X)\propto
L_e(x)\)</span>。如果有环境光贴图，那么 25%
的候选样本由重要性采样环境光贴图生成。重要性采样发光三角形与环境光贴图都是离散采样，采用
alias table <a href="#%5B2%5D">[2]</a>。同时，作者也测试了通过预计算为场景的发光三角形生成
VPL 集合，虽然能够提高性能，但有 visual artifact。</p>
<p><strong>Target PDF</strong>：在重采样步骤中，需要 target PDF
对选出的样本进行加权，作者使用 unshadowed path contribution 作为 target
PDF，即 <span class="math inline">\(\hat{p}\propto \rho\cdot L_e\cdot
G\)</span>。作者为场景所有几何使用统一的材质，Lambertian diffuse 和
dielectric GGX microfacet
。而对于复杂的材质模型，为每个候选样本计算会更耗时。</p>
<p><strong>Neighbor Selection</strong>：对于 Spatial Reuse，在 30-pixel
半径范围内采样 <span class="math inline">\(k\)</span>
个像素进行复用，无偏选用 <span class="math inline">\(k=3\)</span>，有偏选用 <span class="math inline">\(k=5\)</span>。对于 Temporal Reuse，则使用 motion
vector 找到历史帧对应的像素。</p>
<p><strong>Biased RIS
降低偏差</strong>：对于有偏算法，重用来自不同几何/材质的像素会增加偏差，因此作者使用一种简单的启发式策略拒绝这部分像素。比较重用像素与当前像素的到相机距离、normal
夹角，拒绝超过预设阈值的像素(10% 当前像素的深度和 25°)。</p>
<p><strong>Evaluated Sample Count</strong>：对于 <a href="#Algo%205">算法 5</a> ，每个像素存储 <span class="math inline">\(N\)</span> 个 reservoir，无偏算法采用 <span class="math inline">\(N=1\)</span>，有偏算法采用 <span class="math inline">\(N=4\)</span>。</p>
<p><strong>Reservoir storage and temporal
weighting</strong>：每个像素只存储选出的样本 <span class="math inline">\(y\)</span> 及其权重 <span class="math inline">\(W\)</span>，候选样本的数量 <span class="math inline">\(M\)</span>。对于 temporal
reuse，对当前像素产生贡献的候选样本数量会无限制增加，因为每一帧都结合了历史帧的
reservoir。在重采样过程中，这样会导致 temporal sample
加权高度不成比例，作者将历史帧的候选样本数量限制在当前帧 <span class="math inline">\(20\times M\)</span>
内，这样就限制了时序信息的影响。</p>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a> Benedikt Bitterli, Chris Wyman, Matt Pharr,
Peter Shirley, Aaron Lefohn, and Wojciech Jarosz. 2020. Spatiotemporal
reservoir resampling for real-time ray tracing with dynamic direct
lighting. <i>ACM Trans. Graph.</i> 39, 4, Article 148 (August 2020), 17
pages. DOI:https://doi.org/10.1145/3386569.3392481</p>
<p><a name="[2]">[2]</a> Alastair J Walker. 1974. New fast method for
generating discrete random numbers with arbitrary frequency
distributions. Electronics Letters 10, 8 (1974), 127–128.</p>
<h1 id="appendix">Appendix</h1>
<h2 id="重要性采样为何要到-resample-importance-sampling">1.
<a name="App 1">重要性采样为何要到 Resample Importance Sampling</a></h2>
<p>蒙特卡洛积分估计理论上是进行越多的采样次数，越接近
groud-truth，但实际中要限制开销，只能以有限的采样次数来进行积分估计。因此，采样分布如果能够更倾向于采样被积函数较重要的区域，则会更快达到收敛，这就是重要性采样
(IS) 的思想。<span class="math inline">\(\eqref{IS MC}\)</span>
式描述的重要性采样(IS)是蒙特卡洛积分的最简单的无偏估计，因为 IS
只采样了一种分布，例如 cosine-weighted sample hemisphere
就是根据渲染方程 cosine 项的特征得到的采样方法。</p>
<p>但通常渲染方程被积函数包含多项，如 <span class="math inline">\(\eqref{simple render equation}\)</span> 中的 <span class="math inline">\(\rho,L_e,G\)</span> 等。于是又有了，<span class="math inline">\(\eqref{MIS MC}\)</span>
式描述的多重重要性采样，MIS
使用了多种采样策略，每种采样策略都进行了一次重要性采样，最后将多次重要性采样结合在一起。虽说，MIS
考虑了多种分布，但其估计量形式相当于是<strong>对多种分布的线性组合</strong>，而对于渲染方程而言，其被积函数是多项的乘积形式，因此不能更好地采样被积函数的重要性区域。</p>
<p>对于多种分布的乘积形式，是无法直接采样的。但可以使用 Resample
Importance Sampling(RIS) <span class="math inline">\(\eqref{1-sample
RIS}\)</span> 来近似成比例地采样多种分布乘积形式。</p>
<h2 id="ris-权重期望">2. RIS 权重期望</h2>
<p><span class="math display">\[
\begin{align}
\mathop{\mathbb{E}}\limits_{x_z=y}\left[\textup{W}(\boldsymbol{x},z)\right]&amp;=\underbrace{\int
\cdots \int}_M\frac{ \textup{W}(\boldsymbol{x},z,x_z=y)\cdot
p(\boldsymbol{x},z,x_z=y)}{p_z(x_z=y) \space } \space dx_1\cdots dx_Mdz
\\
&amp;\overset{\eqref{y PDF}}{=}\sum\limits_{i\in
Z(y)}\underbrace{\int\cdots\int}_{M-1} \frac{
\textup{W}(\boldsymbol{x}^{i\rightarrow y},i)\cdot
p(\boldsymbol{x}^{i\rightarrow y},i)}{p_y(y) } \space dx_1\cdots dx_M \\
&amp;= \sum\limits_{i\in Z(y)} \frac{\underbrace{\int\cdots\int}_{M-1}
\textup{W}(\boldsymbol{x}^{i\rightarrow y},i)\cdot
p(\boldsymbol{x}^{i\rightarrow y},i)\space dx_1\cdots dx_M}{p_y(y)} \\
&amp;= \frac{1}{p_y(y)}\cdot \sum\limits_{i\in Z(y)}
\underbrace{\int\cdots\int}_{M-1}
\textup{W}(\boldsymbol{x}^{i\rightarrow y},i)\cdot
p(\boldsymbol{x}^{i\rightarrow y},i)\space dx_1\cdots dx_M \\
&amp;\overset{代入 \eqref{RIS weight},\eqref{joint
PDF}}{=}  \frac{1}{p_y(y)}\sum\limits_{i\in Z(y)}\int\cdots\int
\left(\frac{1}{\hat{p}(x_i)}\frac{1}{M}\sum\limits^M_{j=1}\textup{w}(x_j)\right)
\left(\prod\limits^M_{j=1}p_j(x_j)\right)
\frac{\textup{w}_i(x_i)}{\sum^M_{j=1}\textup{w}_j(x_j)} \space
dx_1\cdots dx_M \\
&amp;= \frac{1}{p_y(y)}\sum\limits_{i\in Z(y)}\int\cdots\int
\left(\frac{1}{\hat{p}(x_i)}\frac{1}{M}\right)
\left(\prod\limits^M_{j=1}p_j(x_j)\right) \textup{w}_i(x_i) \space
dx_1\cdots dx_M \\
&amp;= \frac{1}{p_y(y)}\sum\limits_{i\in
Z(y)}\left(\frac{p_i(x_i)}{\hat{p}(x_i)}\frac{\textup{w}_i(x_i)}{M}\underbrace{\int\cdots\int
\prod\limits^M_{j\neq i}p_j(x_j) \space dx_1\cdots dx_M}_1\right) \\
&amp;=\frac{1}{p_y(y)}\sum\limits_{i\in
Z(y)}\left(\frac{p_i(x_i)}{\hat{p}(x_i)}\frac{\textup{w}_i(x_i)}{M}\right)
\\
&amp;= \frac{1}{p_y(y)}\sum\limits_{i\in Z(y)}\frac{1}{M} \\
&amp;= \frac{1}{p_y(y)}\frac{|Z(y)|}{M}
\end{align}
\]</span></p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>Ray Tracing</category>
      </categories>
  </entry>
  <entry>
    <title>Texel Shading</title>
    <url>/Paper-Notes/Render-Pipeline/Texel-Shading.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p>传统的光栅化管线着色（forward/deferred）是基于屏幕空间像素的着色过程，由于光栅化流水线作业的限制，shading
rate完全由分辨率、几何复杂度、帧率控制。而本文提出的texel
shading管线将着色转化为texture space的任务，并将着色结果cache在texture
space，最终的着色通过采样cache完成。</p>
<p>基于texture space的着色将shading
rate与分辨率、几何复杂度、帧率解耦，可以每像素调整shading
rate，变得更加可控。同时texture
space下对于时空复用类算法更加友好。概况地讲，texture space
shading(TSS)的提出主要有如下动机：</p>
<ol type="1">
<li>可控的表面像素采样率。对于远处高光的小物体，相机的运动会导致非常不稳定的效果。例如，从正对着表面运动到grazing视角，表面变得更加高频，而采样率不足。TSS的着色受相机视角影响较小。</li>
<li>更加准确的帧间复用。不同的观察位置、角度下，相邻帧之间的表面像素的不匹配程度较高，例如可能处于不同的mip
level。传统光栅化管线在时序上的复用难以考虑这一点，而TSS着色结果cache在texture
space，时序连续性较高。</li>
<li>更加准确的空间复用。屏幕空间的像素之间暴露出incoherent性质，这对于空间复用很不友好。而texture
space下，相邻像素往往满足世界空间的coherent。</li>
</ol>
<h1 id="pipeline设计">2 Pipeline设计</h1>
<p>texel
shading的前提是使用某种参数化方式将场景的mesh统一到唯一的texture
space下，即每一个surface
point都具有唯一的texture坐标。这种参数化方式在TSS技术中也是一大问题，本文未做这方面的详细调研。texel
shading管线共包含三个阶段：</p>
<ol type="1">
<li><strong>Shade Queuing</strong>：使用一个geometry
pass来标记需要着色的texel tiles(8x8)，每个texel
tile作为着色任务。再新增着色任务前会先查询cache中是否已存在。</li>
<li><strong>Shade Evaluation</strong>：compute
shader中提取着色任务需要的顶点属性，并生成着色结果到cache
texture中。</li>
<li><strong>Shade Gathering</strong>：再执行一次geometry pass，从cache
texture中收集着色结果，得到最终屏幕着色。</li>
</ol>
<h2 id="预处理">2.1 预处理</h2>
<p>为了能够在compute
shader中得到几何信息，还需要在预处理阶段生成triangle index
texture，即texture space下纹理坐标到triangle ID的映射。</p>
<p><strong>实现设计</strong>：对整个texture
space分辨率进行一次光栅化即可得到triangle index texture。</p>
<h2 id="shade-queuing">2.2 Shade Queuing</h2>
<p>本阶段通过geometry
pass为每个像素生成texel单位的shade任务，并选择合适的mip
level。通过一张cache texture来缓存每个8x8
tile的状态，例如最近更新的帧数，可以控制tile的更新频率。8x8
tile的shade任务生成算法如下：</p>
<p><img src="/images/Paper Notes/Render Pipeline/Texel Shading.assets/image-20230703182339279.png" alt="image-20230703182339279" style="zoom: 50%;"></p>
<blockquote>
<p>算法：geometry pass生成的texel任务(i, j, L)转为 8x8 tile任务(i/8.
j/8, L-3)。一个L-3级别上的8x8 tile可以计算得到一个L的texel。</p>
</blockquote>
<p>这里可以看出控制shading rate的方式有：</p>
<ul>
<li>tile的更新频率阈值（多少帧更新一次）</li>
<li>texel的mip
level越大，意味着越少数量的texel，一个texel可以覆盖多个像素，这适用于低频区域。</li>
</ul>
<p><strong>实现设计</strong>：</p>
<ul>
<li>geometry
pass需要对带纹理坐标的几何进行光栅化，得到每个像素的纹理坐标、以及重心坐标。</li>
<li>cache texture：从算法实现上看，cache
texture应该是N-3层级的贴图，给定shade texel(i, j,
L)，其在cache中的坐标为(i/8, j/8, L-3)。cache
texture的texel是对应tile的状态信息：
<ul>
<li>上一次更新的帧数</li>
<li>bias配置：提高mip level来减少shade texel</li>
</ul></li>
<li>如果在本阶段的geometry pass中计算的mip level向上取整，一个8x8
tile的着色可以在Shade Gathering阶段做到三线性插值。</li>
</ul>
<h2 id="shade-evaluation">2.3 Shade Evaluation</h2>
<p>本阶段执行于compute shader，每个thread group执行一个8x8
tile。通过texel的纹理坐标从triangle index texture得到当前triangle
ID，并根据此ID得到triangle数据。使用texel的重心坐标进行插值得到texel的顶点数据。之后便可以按照传统渲染的方式对texel着色。</p>
<h2 id="shade-gathering">2.4 Shade Gathering</h2>
<p>此时，cache中已经有了屏幕着色需要的所有数据。再执行一次geometry
pass得到对应level的texel着色。</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>Render Pipeline</category>
      </categories>
  </entry>
  <entry>
    <title>Texture Space Shading</title>
    <url>/Paper-Notes/Render-Pipeline/Texture-Space-Shading.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p>传统的光栅化管线（forward或者deferred）执行于屏幕空间，表面采样率或者像素密度与相机距离、视角相关。同时，GPU光栅化管线的流水线作业使得shading
rate完全由分辨率、几何复杂度、帧率控制。而texture space
shading(TSS)提出在texture space着色，使得shading
rate变得更加可控。同时texture space下的texel着色具有更优的spatial
coherent以及temporal coherent，这对于时空复用类算法更加友好。</p>
<p>概况地讲，TSS的提出主要有如下动机：</p>
<ol type="1">
<li>可控的表面像素采样率。对于远处高光的小物体，相机的运动会导致非常不稳定的效果。例如，从正对着表面运动到grazing视角，表面变得更加高频，而采样率不足。TSS的着色受相机视角影响较小。</li>
<li>更加准确的帧间复用。不同的观察位置、角度下，相邻帧之间的表面像素的不匹配程度较高，例如可能处于不同的mip
level。传统光栅化管线在时序上的复用难以考虑这一点，而TSS着色结果cache在texture
space，时序连续性较高。</li>
<li>更加准确的空间复用。屏幕空间的像素之间暴露出incoherent性质，这对于空间复用很不友好。而texture
space下，相邻像素往往满足世界空间的coherent。</li>
</ol>
<h1 id="category">2 Category</h1>
<p>本文按照 <a href="#%5B1%5D">[1]</a>
给出的分类方法描述TSS的相关工作，以及设想中的大致实现思路。</p>
<figure>
<img src="/images/Paper%20Notes/Render%20Pipeline/Texture%20Space%20Shading.assets/image-20230628191733254.png" alt="image-20230628191733254">
<figcaption aria-hidden="true">image-20230628191733254</figcaption>
</figure>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a> Neff, T., Mueller, J.H., Steinberger, M. and
Schmalstieg, D. (2022), Meshlets and How to Shade Them: A Study on
Texture-Space Shading. Computer Graphics Forum, 41: 277-287.
https://doi.org/10.1111/cgf.14474</p>
]]></content>
      <categories>
        <category>Paper Notes</category>
        <category>Render Pipeline</category>
      </categories>
  </entry>
  <entry>
    <title>Screen Space Ray Reflection</title>
    <url>/Rendering-Blogs/Graphics-Algorithm/1.-Hierarchy-Ray-March.html</url>
    <content><![CDATA[<h1 id="summary">1 Summary</h1>
<p>本文介绍屏幕空间反射中最重要的一步——基于 hierarchy depth buffer(HZB)
的 ray march算法，主要参考了 <a href="#%5B1%5D">[1]</a> 中的实现。</p>
<p>ray
march算法的目的是为了根据屏幕空间深度得到光线在屏幕上的交点，相比于世界空间的光追，这是一种开销很低但仅局限于屏幕空间的做法，也就是若光线交于相机看不到的表面，那么屏幕空间ray
march则无法得到求交结果。而 hierarchy depth buffer
则提供了四叉树的加速结构，每一级mipmap都取上一级四个深度中的closest
depth。假如光线在 closest depth
的表面之上（距相机更近），那么可以光线也不会与更精细层级相交，因此可以快速行进不会相交的区域。</p>
<h1 id="hierarchy-depth-buffer">2 hierarchy depth buffer</h1>
<p>hierarchy depth buffer(HZB) 是构建depth
buffer的mipmap形式，但与API接口提供的mipmap构建方式（取均值）不同，hierarchy
ray march需要每一层级取前一层级四个深度中的closest
depth，具体取最小或最大由depth实现决定。本文采用 <span class="math inline">\([0,1]\)</span>
的depth，越大距相机越远，因此取最小深度。</p>
<h1 id="construct-ray">3 Construct Ray</h1>
<p>对于屏幕空间，往往已知光线起点像素坐标
<code>fragCoord</code>，以及基于需要采样的光线方向 <code>rayDir</code>
（位于世界/相机空间，本文采样相机空间）。ray march
过程在屏幕空间进行，同时为了方便在不同mip层级行进，需要得到screen uv
space(即 <span class="math inline">\([0,1]\times [0,1]\times
[0,1]\)</span>)下的光线起点，以及屏幕空间的光线方向。</p>
<p><code>(fragCoord, depth)</code> 变换到view space得到
<code>viewRayPos</code>，沿光线方向行进一个单位得到
<code>nextRayPos = viewRayPos + rayDir</code>，最后将二者都变换到screen
uv space，得到 <code>origin</code> 与
<code>nextRayScreenPos</code>，光线方向
<code>direction = nextRayScreenPos - origin</code>，注意不需要normalize，因为这并不是三维空间的方向。</p>
<h1 id="ray-march">4 Ray March</h1>
<p>给定光线起点 <code>origin</code> 与光线方向
<code>direction</code>，都位于 screen uv
space。在光线行进过程中，会反复执行一下步骤：</p>
<ul>
<li><p>先行进 linear
step，即在当前层级行进一个像素。执行基于深度的求交判断，有：</p>
<ul>
<li><p>如果光线从当前像素在表面上（更靠近相机）掠过，表示无交点，那么去更高层级（表示更大步长）。</p></li>
<li><p>如果光线与当前像素相交，表示可能相交，那么去更低层级（表示更小步长）</p></li>
</ul></li>
</ul>
<h2 id="linear-march">4.1 Linear March</h2>
<p>行进一个步长是希望沿着光线方向行进到下一个像素，同时下个像素内执行一个偏移，如图
<a href="#Fig-1">Fig-1</a> 所示。</p>
<p><a name="Fig-1"></a></p>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/1. Hierarchy Ray March.assets/image-20230812143337171.png" alt="image-20230812143337171" style="zoom: 50%;"></p>
<center>
Fig-1 一次Linear March的示意图
</center>
<p>行进步长 <code>marchStep</code> 可设置为</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">vec2</span> marchStep = <span class="built_in">mix</span>(<span class="type">vec2</span>(<span class="number">0.0</span>), <span class="type">vec2</span>(<span class="number">1.0</span>), <span class="built_in">greaterThanEqual</span>(direction.xy, <span class="type">vec2</span>(<span class="number">0.0</span>)));</span><br></pre></td></tr></table></figure>
<p>像素内部偏移量 <code>uvOffset</code> 以像素大小为单位，可设置为</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">vec2</span> uvOffset = <span class="number">0.005</span> * <span class="built_in">exp2</span>(baseLevel) * (<span class="number">1.0</span> / screenResolution);	<span class="comment">// baseLevel 表示 ray march 执行的最低层级</span></span><br><span class="line">uvOffset = <span class="built_in">mix</span>(-uvOffset, uvOffset, <span class="built_in">greaterThanEqual</span>(direction.xy, <span class="type">vec2</span>(<span class="number">0.0</span>)));</span><br></pre></td></tr></table></figure>
<p>假设当前光线位置 <code>currentRayCoord</code> 以及当前层级分辨率
<code>currentMipResolution</code>，linear march的执行如下</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">vec2</span> LinearMarch(<span class="type">vec2</span> currentRayCoord, <span class="type">vec2</span> currentMipResolutionInv)	<span class="comment">// currentMipResolutionInv 为倒数</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">vec2</span> newRayCoord = <span class="built_in">floor</span>(currentRayCoord) + marchStep;</span><br><span class="line">	newRayUV = newRayCoord * currentMipResolutionInv + uvOffset;</span><br><span class="line">    <span class="keyword">return</span> newRayUV;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在进行求交之前，光线需要先行进一步，避免与自身所在像素自相交，行进到下一个像素的boundary
plane处，有</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> InitRay(<span class="type">vec3</span> origin, <span class="type">vec3</span> direction, <span class="type">vec3</span> directionInv, <span class="type">vec2</span> currentRayCoord, </span><br><span class="line">	<span class="type">vec2</span> currentMipResolutionInv， <span class="keyword">out</span> <span class="type">vec3</span> position, <span class="keyword">out</span> <span class="type">float</span> t)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">vec2</span> xyPlane = LinearMarch(currentRayCoord, currentMipResolutionInv);</span><br><span class="line">    <span class="comment">// o + d * t = p&#x27; =&gt; t = (p&#x27; - o) * (1 / d)</span></span><br><span class="line">    <span class="type">vec2</span> tXy = xyPlane * directionInv.xy - origin.xy * directionInv.xy;</span><br><span class="line">    t = <span class="built_in">min</span>(tXy.x, tXy.y);</span><br><span class="line">    position = origin + t * direction;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="intersection">4.2 Intersection</h2>
<p>每次迭代都需要进行判断是否与将要行进到的像素有交点，以此来决定下一步应该如何执行。如何判断光线与将要行进像素是否相交？</p>
<p>假设光线当前位置为 positton，将要行进位置为 new position，如图 <a href="#Fig-1">Fig-1</a> 所示。new position 处的 (x, y, surfaceZ)
(surfaceZ为该处像素的深度) 形成了一个bounding
box的一半，即相邻的三个面组成的boundary，可以看到 <a href="#Fig-1">Fig-1</a> 中，俯视近平面所展示的 x/y plane 形成的黄色
boundary plane。从侧面看近平面如图 <a href="#Fig-2">Fig-2</a>
所示，相机位于上方，两条黄色 boundary line交点处即为 new position。</p>
<p><a name="Fig-2"></a></p>
<figure>
<img src="/images/Rendering%20Blogs/Graphics%20Algorithm/1.%20Hierarchy%20Ray%20March.assets/image-20230812194326749.png" alt="image-20230812194326749">
<figcaption aria-hidden="true">image-20230812194326749</figcaption>
</figure>
<center>
Fig-2 从侧面看近平面
</center>
<p>对于一个像素而言，其内部深度都是相同的，光线是否会与即将行进的像素相交，通过查看两个端点
position与new position即可：</p>
<ul>
<li>光线当前位置 position 与表面的关系有：position.z &lt;
surfaceZ，光线在表面之上；否则，光线在表面之下，可能与像素相交</li>
<li>求光线与new position形成的boundary plane (x, y,
surfaceZ)的最近交点，如果没有与z plane相交，则表示光线可以从new
position上方掠过</li>
</ul>
<p>因此，对于光线当前位置 position 的两种情况而言：</p>
<ul>
<li>position.z &lt;
surfaceZ：光线在表面之上，不会与position所在像素相交。
<ul>
<li><ol type="a">
<li>光线与 z plane 相交，表示光线与下一个像素相交</li>
</ol></li>
<li><ol start="2" type="a">
<li>否则，即与 x/y plane 相交，表示光线与下一个像素也不相交</li>
</ol></li>
</ul></li>
<li><ol start="3" type="a">
<li>否则，光线在表面之下，与position所在像素相交</li>
</ol></li>
</ul>
<p>为了能够使用 hierarchy depth buffer 加快速度，应该有：</p>
<ul>
<li>如果本次迭代的行进有可能相交，则去更细粒度层级，即使用更小行进步长。例如上述情况
a，光线可以行进到 z plane
交点位置；c，光线位置不变，等待更细粒度层级测试</li>
<li>如果不会相交，则去更粗粒度层级，即使用更大行进步长。例如上述情况
b，光线可以行进到 x/y plane 的交点位置。</li>
</ul>
<p>上述迭代一直进行到没有更细粒度层级可用时，表示已找到可能交点；否则，达到迭代次数上限后，表示无法找到交点。</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="comment">// surfaceZ: depth at currentRayCoord.xy</span></span><br><span class="line"><span class="type">bool</span> AdvanceRay(<span class="type">vec3</span> origin, <span class="type">vec3</span> direction, <span class="type">vec2</span> currentRayCoord, <span class="type">float</span> surfaceZ. <span class="type">vec2</span> currentMipResolutionInv, 		<span class="keyword">inout</span> <span class="type">vec3</span> position, <span class="keyword">inout</span> <span class="type">float</span> t)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// new position 处的 boundary plane</span></span><br><span class="line">	<span class="type">vec2</span> xyPlane = LinearMarch(currentRayCoord, currentMipResolutionInv);</span><br><span class="line">	<span class="type">vec3</span> boundaryPlane = <span class="type">vec3</span>(xyPlane, surfaceZ);</span><br><span class="line">	<span class="comment">// o + d * t = p&#x27; =&gt; t = (p&#x27; - o) * (1 / d)</span></span><br><span class="line">	<span class="type">vec3</span> tXyz = boundaryPlane * directionInv - origin * directionInv;</span><br><span class="line">    <span class="comment">// 特殊情况，// Prevent using z plane when shooting out of the depth buffer.</span></span><br><span class="line">    tXyz.z = direction.z &gt; <span class="number">0</span> ? tXyz.z : SSR_FLOAT_MAX;</span><br><span class="line">	<span class="type">float</span> tMin = <span class="built_in">min</span>(<span class="built_in">min</span>(tXyz.x, tXyz.y), tXyz.z);</span><br><span class="line">	<span class="type">bool</span> isAboveSurface = position.z &lt; surfaceZ;</span><br><span class="line">	<span class="type">bool</span> skipTile = tMin != tXyz.z &amp;&amp; isAboveSurface;	<span class="comment">// 是否为情况 b</span></span><br><span class="line">	t = isAboveSurface ? tMin : t;</span><br><span class="line">	position = origin + t * direction;</span><br><span class="line">    <span class="keyword">return</span> skipTile;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每次迭代后，调整层级</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> skipTile = AdvanceRay(...);</span><br><span class="line">currentLevel += skipTile ? <span class="number">1</span> : <span class="number">-1</span>;</span><br><span class="line">currentMipResolution *= skipTile ? <span class="number">0.5</span> : <span class="number">2.0</span>;</span><br><span class="line">currentMipResolutionInv *= skipTile ? <span class="number">2.0</span> : <span class="number">0.5</span>;</span><br></pre></td></tr></table></figure>
<h3 id="特殊情况-confused-question">4.2.1 特殊情况 :confused:
:question:</h3>
<p>上面描述都是以光线方向背离相机为例，当光线方向朝向相机时，需要特殊注意：</p>
<ul>
<li>position.z &gt;= surfaceZ：与前述过程没有不同</li>
<li>position.z &lt; surfaceZ：此时不能使用 z plane
的交点，因为得到的</li>
</ul>
<h3 id="线性查找">4.2.2 线性查找</h3>
<p>前面讲到，迭代到没有更细粒度层级时，表示找到交点，退出迭代。但如果是超出最大层级时呢？一种做法是，可以看作无法找到交点，但如果想要查找更充分，此时可以降为在最大层级线性查找过程，迭代后层级调整实现修改如下</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> skipTile = AdvanceRay(...);</span><br><span class="line"><span class="type">int</span> prevLevel = currentLevel;</span><br><span class="line">currentLevel += skipTile ? <span class="number">1</span> : <span class="number">-1</span>;</span><br><span class="line">currentLevel = <span class="built_in">min</span>(currentLevel, LevelCount - <span class="number">1</span>);</span><br><span class="line"><span class="keyword">if</span> (currentLevel != prevLevel) &#123;	<span class="comment">// 只有层级发生改变时</span></span><br><span class="line">    currentMipResolution *= skipTile ? <span class="number">0.5</span> : <span class="number">2.0</span>;</span><br><span class="line">	currentMipResolutionInv *= skipTile ? <span class="number">2.0</span> : <span class="number">0.5</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="交点是否有效">4.2.3 交点是否有效</h3>
<p>前述讲的求交测试都是基于深度信息进行的，如果光线走到了像素背后，则表示相交。但实际上，像素占据了表面一定面积，而该小部分表面并非与近平面平行或者并非是平坦的，因此所在表面存在一定厚度，有可能光线从表面背后穿过去，只有在像素厚度范围内的光线，才算是相交。因此，在得到交点后，还需要进行验证是否有效。</p>
<p>提取交点 <code>position</code> 处的深度值
<code>surfaceZ</code>，分别将
<code>position.z</code>与<code>surfaceZ</code>
变换到线性空间（相机/世界空间），得到 <code>rayDepth</code> 与
<code>hitDepth</code>，有</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> TestThickness(<span class="type">float</span> rayZ, <span class="type">float</span> hitZ)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">float</span> rayDepth = Linearize(rayZ);</span><br><span class="line">    <span class="type">float</span> hitDepth = Linearize(hitZ);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">abs</span>(rayDepth - hitDepth) &lt; (relativeThickness * <span class="built_in">max</span>(<span class="built_in">abs</span>(hitDepth), <span class="number">1e-5</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="put-all-together">4.3 Put All Together</h2>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">vec3</span> HierarchyRayMarch(<span class="type">vec3</span> origin, <span class="type">vec3</span> direction, <span class="type">int</span> maxIteration, </span><br><span class="line">	<span class="type">float</span> relativeThickness, <span class="keyword">out</span> <span class="type">bool</span> isValidHit)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 一些参数初始化</span></span><br><span class="line">    <span class="keyword">const</span> <span class="type">vec3</span> directionInv = <span class="number">1.0</span> / direction;</span><br><span class="line">    <span class="keyword">const</span> <span class="type">int</span> baseLevel = <span class="number">0</span>;     <span class="comment">// 0 means full resolution depth</span></span><br><span class="line">    <span class="type">int</span> currentLevel = baseLevel;</span><br><span class="line">    <span class="type">vec2</span> currentMipResolution = GetMipResolution(screenResolution, currentLevel);</span><br><span class="line">    <span class="type">vec2</span> currentMipResolutionInv = <span class="number">1.0</span> / currentMipResolution;</span><br><span class="line">    <span class="comment">// 设置 Linear March 步长</span></span><br><span class="line">    <span class="type">vec2</span> marchStep = <span class="built_in">mix</span>(<span class="type">vec2</span>(<span class="number">0.0</span>), <span class="type">vec2</span>(<span class="number">1.0</span>), <span class="built_in">greaterThanEqual</span>(direction.xy, <span class="type">vec2</span>(<span class="number">0.0</span>)));</span><br><span class="line">    <span class="type">vec2</span> uvOffset = <span class="number">0.005</span> * <span class="built_in">exp2</span>(baseLevel) * (<span class="number">1.0</span> / screenResolution);	<span class="comment">// baseLevel 表示 ray march 执行的最低层级</span></span><br><span class="line">	uvOffset = <span class="built_in">mix</span>(-uvOffset, uvOffset, <span class="built_in">greaterThanEqual</span>(direction.xy, <span class="type">vec2</span>(<span class="number">0.0</span>)));</span><br><span class="line">    <span class="type">vec3</span> position;</span><br><span class="line">    <span class="type">float</span> t;</span><br><span class="line">    <span class="comment">// 光线先行进一个单位，避免与自身像素相交</span></span><br><span class="line">    InitRay(origin, direction, currentMipResolution * origin.xy, currentMipResolutionInv, position, t);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (i &lt; maxIteration &amp;&amp; currentLevel &gt;= baseLevel &amp;&amp; InsideScreen(position.xy)) &#123;</span><br><span class="line">        <span class="type">vec2</span> currentRayCoord = currentMipResolution * position.xy;</span><br><span class="line">        <span class="type">float</span> surfaceZ = <span class="built_in">texelFetch</span>(hiDepthTexArray[currentLevel], <span class="type">ivec2</span>(currentRayCoord), <span class="number">0</span>).r;</span><br><span class="line">        <span class="type">bool</span> skipTile = AdvanceRay(origin, direction, currentRayCoord, surfaceZ, </span><br><span class="line">  							currentMipResolutionInv, position, t);</span><br><span class="line">		<span class="type">int</span> prevLevel = currentLevel;</span><br><span class="line">		currentLevel += skipTile ? <span class="number">1</span> : <span class="number">-1</span>;</span><br><span class="line">		currentLevel = <span class="built_in">min</span>(currentLevel, LevelCount - <span class="number">1</span>);</span><br><span class="line">		<span class="keyword">if</span> (currentLevel != prevLevel) &#123;	<span class="comment">// 只有层级发生改变时</span></span><br><span class="line">    		currentMipResolution *= skipTile ? <span class="number">0.5</span> : <span class="number">2.0</span>;</span><br><span class="line">			currentMipResolutionInv *= skipTile ? <span class="number">2.0</span> : <span class="number">0.5</span>;</span><br><span class="line">		&#125;</span><br><span class="line">        ++i;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 验证交点是否有效</span></span><br><span class="line">    isValidHit = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (currentLevel &lt; baseLevel &amp;&amp; InsideScreen(position.xy)) &#123;</span><br><span class="line">        <span class="type">vec2</span> currentRayCoord = screenResolution * position.xy;</span><br><span class="line">        <span class="type">float</span> hitZ = <span class="built_in">texelFetch</span>(hiDepthTexArray[<span class="number">0</span>], <span class="type">ivec2</span>(currentRayCoord), <span class="number">0</span>).r;</span><br><span class="line">        isValidHit = TestThickness(position.z, hitZ);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> position;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="reflection">5 Reflection</h1>
<p>最后简要介绍下屏幕空间反射的反射部分。屏幕空间反射希望得到金属材质的着色点的反射信息，首先需要在着色点处根据材质信息，重要性采样得到反射光线的方向。以着色点为起点、反射光线的方向组成的光线，在屏幕空间执行ray
march，找到的交点即为反射点。接下来，SSR直接将上一帧反射点的颜色（不包含反射）作为反射点向着色点发出的radiance。注意：这是一种简单的近似，并非PBR过程，因为屏幕上的颜色实际上应该是屏幕像素向相机发出的radiance，这里直接近似作为其向着色点的radiance。 </p>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a>
https://github.com/GPUOpen-Effects/FidelityFX-SSSR</p>
]]></content>
      <categories>
        <category>Rendering Blogs</category>
        <category>Graphics Alogrithm</category>
      </categories>
  </entry>
  <entry>
    <title>Reblur解析</title>
    <url>/Rendering-Blogs/Graphics-Algorithm/2.-Reblur%E8%A7%A3%E6%9E%90.html</url>
    <content><![CDATA[<h1 id="概要">1 概要</h1>
<h1 id="调用逻辑">2 调用逻辑</h1>
<p>文件 Reblur_DiffuseSpecular.hpp，在
nrd::InstanceImpl::Add_ReblurDiffuseSpecular 里创建好所有 render
pass</p>
<p>每个 pass 的名字使用宏 <code>PushPass("PostFix")</code>
设置，最后得到 pass name = <code>DENOISER_NAME - PostFix</code></p>
<p><code>AddDispatch</code>
的第一个参数指定shader文件名，而这个shader文件只是充当一个组织文件，预定义一些宏，控制降噪走的分支，
实际的代码在其 include 的 shader 文件中。对于 REBLUR_DiffuseSpecular
会走 <code>REBLUR_DIFFUSE</code> 与 <code>REBLUR_SPECULAR</code>
两个分支</p>
<p>reblur 配置 <code>nrd::ReblurSettings m_ReblurSettings</code>
，更新在 <code>InstanceImpl::Update_Reblur</code></p>
<p>shader参数：</p>
<ul>
<li><code>REBLUR_SHARED_CB_DATA</code>在
<code>InstanceImpl::AddSharedConstants_Reblur</code>中更新</li>
<li>pass参数的更新在 <code>InstanceImpl::Update_Reblur</code></li>
</ul>
<h1 id="渲染逻辑">3 渲染逻辑</h1>
<figure>
<img src="/images/Rendering%20Blogs/Graphics%20Algorithm/2.%20Reblur解析.assets/image-20230820103719276-1694926428932-2.png" alt="image-20230820103719276-1694926428932-2">
<figcaption aria-hidden="true">image-20230820103719276-1694926428932-2</figcaption>
</figure>
<h2 id="classify-tiles">3.1 Classify tiles</h2>
<p>识别需要降噪的tile</p>
<h2 id="pre-pass">3.2 Pre-pass</h2>
<p>准备 pre-blur 参数</p>
<h3 id="depth-based-bilateral-weight">3.2.1 depth-based bilateral
weight</h3>
<p><code>float2 wc</code> ：depth-based bilateral
weight，使用左右两个像素<code>viewZ0</code>、<code>viewZ1</code>与当前像素的<code>viewZ</code>的相对差异。对相对差异施加一个cut
off，限制在[0, cut_off]范围内。当相对差异超过阈值 0.03 时，权重为
0；当相对差异&lt;=0时，权重取 1；其余在 0~1 之间。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 viewZ01 = float2(viewZ0, viewZ1);</span><br><span class="line">float2 x = <span class="built_in">abs</span>(viewZ01 - viewZ) * rcp(max(<span class="built_in">abs</span>(viewZ01), <span class="built_in">abs</span>(viewZ)));	<span class="comment">// 当前像素与左右相邻像素的 viewZ 的相对差异</span></span><br><span class="line"><span class="type">float</span> cut_off = <span class="number">0.03</span>;</span><br><span class="line"><span class="comment">// 相当于把 x 限制在 [0, cut_off] 内，&gt;= cut_off 为 0，&lt;= 0 为 1</span></span><br><span class="line">wc = saturate((x - cut_off) / (-cut_off)) = saturate(<span class="number">1</span> - x / cut_off);  </span><br><span class="line">wc *= <span class="number">1.0</span> / max((wc.x + wc.y), <span class="number">1e-15</span>);</span><br></pre></td></tr></table></figure>
<h3 id="checkboard模式处理">3.2.2 Checkboard模式处理</h3>
<p>当为 <code>RESOLUTION_HALF</code> 时，checkerboard mode 为
<code>CheckerboardMode::WHITE(2)</code>、diffCheckerboard(1)、specCheckerboard(0)；否则为
OFF(0)，diffCheckerboard(2)、specCheckerboard(2)。当为半屏模式时，经过pre
pass可以得到全屏结果。</p>
<ol type="1">
<li><p><code>uint checkerboard</code> ：0/1
值，使用像素坐标与帧数得到，相邻像素交错，相邻帧交错
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">uint <span class="title function_">CheckerBoard</span><span class="params">( uint2 samplePos, uint frameIndex )</span></span><br><span class="line">&#123;</span><br><span class="line">    uint a = samplePos.x ^ samplePos.y;</span><br><span class="line">    <span class="keyword">return</span> ( a ^ frameIndex ) &amp; <span class="number">0x1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
<li><p><code>int3 checkerboardPos</code> ：1/2
屏幕坐标(横坐标缩减一半)。x、z
取当前像素的左右相邻像素横坐标，y取当前像素坐标，最后横坐标右移一位</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">int3 checkerboardPos = pixelPos.xyx + int3( <span class="number">-1</span>, <span class="number">0</span>, <span class="number">1</span> );</span><br><span class="line">checkerboardPos.xz &gt;&gt;= <span class="number">1</span>;</span><br></pre></td></tr></table></figure></li>
<li><p>checkboard 模式处理，如文件 <em>NRDSettings.h</em>
描述，当为半屏的 checkboard 模式时，noisy
input在左半部分。<code>hasData</code>表示当前像素是否有有效数据，使用交错处理。
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(gDiffCheckerboard<span class="comment">/*\gSpecCheckerboard*/</span> != <span class="number">2</span>)&#123;	<span class="comment">// 半屏的交错模式</span></span><br><span class="line">    hasData = checkerboard == gDiffCheckerboard<span class="comment">/*\gSpecCheckerboard*/</span>;  <span class="comment">// checkerboard 交错得到有效/无效数据</span></span><br><span class="line">    pos.x &gt;&gt;= <span class="number">1</span>;  <span class="comment">// 1/2 屏幕</span></span><br><span class="line">&#125;</span><br><span class="line">REBLUR_TYPE diff = gIn_Diff[pos]; <span class="comment">/*REBLUR_TYPE spec = gIn_Spec[pos];*/</span></span><br></pre></td></tr></table></figure></p></li>
</ol>
<h3 id="准备当前像素的数据">3.2.3 准备当前像素的数据</h3>
<p><code>N</code>(世界空间法线)、<code>Nv</code>(view
space下的法线)、roughness、<code>Vv</code> (View space 下的view
vector)、<code>Xv</code> (像素在view space下的position)</p>
<p><code>float4 rotator</code> ：每帧生成的向量，用于 Poisson
采样。<code>REBLUR_PRE_BLUR_ROTATOR_MODE</code> 取
<code>NRD_FRAME</code> ，这里 rotator 取的是 CPU 传来的。</p>
<h3 id="执行-pre-blur">3.2.4 执行 Pre-blur</h3>
<p>diffuse 与 specular 有各自的 spatial filter，参考下一章节。</p>
<h2 id="temporal-accumulation">3.3 Temporal accumulation</h2>
<h3 id="preload与数据准备">3.3.1 Preload与数据准备</h3>
<ol type="1">
<li><p>将 tile (GROUP_X + BORDER * 2) x (GROUP_Y + BORDER * 2) 的 normal
hitdist 数据加载到
<code>s_Normal_MinHitDist[GROUP_X+BORDER*2][GROUP_Y+BORDER*2]</code>
中，减少后续重复访问 texture。</p></li>
<li><p>在当前像素的 (BORDER * 2) x (BORDER * 2)
区域计算<code>Navg</code> (averaged normal)
与<code>hitDistForTracking</code> (取最小)。</p>
<blockquote>
<p>注意：<code>threadPos+BORDER</code> 对应了当前像素在
s_Normal_MinHitDist 中的位置，因此循环中跳过了 (i== BORDER &amp;&amp; j
== BORDER)</p>
</blockquote>
<p>但是，Navg 的计算只包含了以当前像素为右下角的四个像素，而
hitDistForTracking 则遍历了以当前像素为中心的 3x3
区域。不懂这个设计</p></li>
<li><p>准备当前像素的数据
<code>float3 N</code>(世界空间法线)，<code>float materialID</code>，<code>Xv</code>(view
space坐标)，
<code>X</code>(相机无平移变换时的世界空间坐标)，<code>roughness</code></p></li>
</ol>
<h3 id="变换">3.3.2 变换</h3>
<p>Temporal
处理时需要进行world、view、clip以及到上一帧的变换，比较特殊的一点，本阶段使用的变换将camera的平移都取消掉了。</p>
<p>常规的变换对应了 <code>CommonSettings</code> 中的
<code>viewToClipMatrix</code>()、<code>worldToViewMatrix</code>()等一系列矩阵，而传给shader的变换实际是
<code>InstanceImpl</code> 里的
<code>m_ViewToClip</code>()、<code>m_WorldToView</code>()、<code>m_ViewToWorld</code>()、<code>m_WorldToClip</code>()。将view
space的平移整体取消，即 world 与 view 之间的变换的平移。而 world
到上一帧view 以及 view 到上一帧world
之间的变换采用相机的运动矢量，即相对偏移。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">m_ViewToWorld.<span class="built_in">SetTranslation</span>(ml::float3::<span class="built_in">Zero</span>());</span><br><span class="line">ml::float3 translationDelta = cameraPositionPrev - cameraPosition;	<span class="comment">// 指向上一帧的相机偏移</span></span><br><span class="line">m_ViewToWorldPrev.<span class="built_in">SetTranslation</span>(translationDelta);</span><br></pre></td></tr></table></figure>
<p>上面一系列做法相当于永远将当前帧相机至于世界原点位置，因此 view 到
world
的变换只需要执行旋转变换。为了便于理解，后续描述也直接忽略相机平移。例如
<code>Xv</code> 是像素在view space下的坐标，而下面应用从view 到
world旋转得到的 <code>X</code> 称为像素的世界空间坐标。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">float3 X = STL::Geometry::<span class="built_in">RotateVector</span>(gViewToWorld, Xv);</span><br></pre></td></tr></table></figure>
<p><strong>因此，通过变换到三位空间得到的点，既是三维空间坐标，又是点到相机的向量。</strong></p>
<h3 id="计算视差">3.3.3 计算视差</h3>
<p>视差（parallax）是指比较两个观察方向（世界空间）的差距大小。观察方向是相机到着色点的方向，因此视差是针对某一着色点而言的。只有当相机发生了位置变化，才会产生视差。在相同的相机运动下，不同着色点具有不同的视差。因此计算视差要固定着色点，如下图所示，相机运动向量
<span class="math inline">\(\vec{c}\)</span>，运动前后的观察方向 <span class="math inline">\(\vec{v},\vec{v}_{prev}\)</span>，可以得到视差度量定义
<span class="math display">\[
parallax = \tan\big(\arccos(\vec{v} \cdot \vec{v}_{prev})\big)
\]</span>
<img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230821185416474.png" alt="image-20230821185416474" style="zoom: 50%;"></p>
<ul>
<li><p><code>Xprev</code>上一帧位置：处理animation带来的运动，计算着色点在上一帧的世界空间坐标
<code>gIn_Mv</code>应该是物体animation带来的motion
vector（mv可能是世界空间下、也可能是uv空间下），测试例子里都是 0；
<code>gMvScale</code> 取值 C++ 的
motionVectorScale，gIsWorldSpaceMotionEnabled = 0。</p></li>
<li><p>世界空间下 (gIsWorldSpaceMotionEnabled ! = 0)：Xpre += mv
得到上一帧的位置</p>
<p>motionVectorScale = (1.0, 1.0, 0.0)</p></li>
<li><p>uv空间下：mv.xy 为 uv 的运动，uv.z 为深度的运动，(此时为 25D)
motionVectorScale = (1.0/width, 1.0/height, 1.0)</p>
<p>因此在上一帧的像素uv： <code>smbPixelUv = pixelUv + mv.xy</code>
与深度 (viewZ + mv.z)</p>
<p>变换得到上一帧的世界坐标：<code>Xprev = RotateVectorInverse(...) + gCameraDelta</code>。RotateVectorInverse
只处理了<strong>相机旋转</strong>（应该是旋转矩阵的逆等于转置，避免求逆）。gCameraDelta
是指向上一帧的运动向量，而这里的变换都是以当前帧相机为世界原点，因此加上
gCameraDelta 最终得到上一帧的世界空间坐标。</p>
<blockquote>
<p>注意：Xprev 是着色点在上一帧的世界坐标，因此如果着色点没有动画，那么
Xprev 与着色点在当前帧的世界坐标应该一样</p>
</blockquote></li>
<li><p><code>smbParallaxInPixels</code>与上一帧之间的视差（像素为单位的距离）</p>
<p>ComputeParallaxInPixels(Xprev - gCameraDelta, pixelUv, gWorldToClip,
gRectSize )</p>
<ul>
<li><p><code>gCameraDelta</code> 即相机在世界空间指向上一帧的运动向量
prev - current。</p>
<p>Xprev - gCameraDelta = Xprev +
(-gCameraDelta)：相当于保持相机位置不同，向着色点施加上一帧指向当前帧的相机平移运动</p>
<p>再执行 <code>gWorldToClip</code> 变换得到当前相机下的
<code>uv</code>。</p></li>
<li><p>像素在上一帧的世界坐标 Xprev
施加了相机运动并变换到当前帧的uv，此时与 pixelUv
处于同一相机下，如上图右侧。因此可以计算二者之间的像素距离</p></li>
</ul></li>
</ul>
<h3 id="历史数据重投影">3.3.4 历史数据重投影</h3>
<p>时序累积是将当前帧与历史帧结合得到更稳定的结果。需要将当前帧重投影到上一帧，从历史数据中得到可靠的对应，并以一定权重将当前帧更新到历史帧中。为此，需要考虑：</p>
<ul>
<li>disocclusion
tracking：当前帧像素是否发生去遮挡，即新出现的像素。</li>
<li>accumulate speed：当前帧像素与历史帧像素结合的权重。</li>
</ul>
<p>reblur的重投影结合了 surface motion based 与 virtual motion
based(仅用于specular) 两种方法，下面先介绍surface motion</p>
<h4 id="surface-motion-based-reprojection">3.3.4.1 Surface motion based
reprojection</h4>
<p>（1）Disocclusion Tracking</p>
<p>前面已经通过重投影或者motion
vector找到当前帧像素<code>pixelUv</code>对应上一帧像素<code>smbPixelUv</code>，通过比较当前帧与上一帧之间view
z的差异是否超过阈值，如果超过则表示像素不匹配。此外，这个过程是基于Catmull-Rom
filter与bilinear
filter进行的，对于匹配的历史信息使用filter结果与当前帧进行混合。</p>
<ol type="1">
<li><p>disocclusion threshold：输入
<code>gDisocclusionThreshold</code>，其在 C++ 上的数值计算如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> disocclusionThresholdBonus = (<span class="number">1.0f</span> + m_JitterDelta) / <span class="built_in">float</span>(rectH);</span><br><span class="line"><span class="type">float</span> disocclusionThreshold = m_CommonSettings.disocclusionThreshold + disocclusionThresholdBonus;	<span class="comment">// 0.01 + 抖动值</span></span><br></pre></td></tr></table></figure>
<p>m_JitterDelta 是相邻两帧camera jitter的差值，camera
jitter则是每帧的halton抖动值 [-0.5,0.5]</p>
<p>先乘上 frustumSize 得到
disocclusionThresholdMulFrustumSize，基于<code>NoV</code>与视差调整阈值，视差越大，阈值越低</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> smbDisocclusionThreshold = disocclusionThresholdMulFrustumSize / lerp(<span class="number">0.05</span> + <span class="number">0.95</span> * NoV, <span class="number">1.0</span>, saturate(smbParallaxInPixels/<span class="number">30.0</span>));</span><br></pre></td></tr></table></figure>
<p>上一帧uv是否在屏幕内，以及上一帧uv是否正面朝向。若不是，则
smbDisocclusionThreshold 取 -1，那么之后的occlusion判断都不通过</p>
<ul>
<li>是否正面朝向，通过上一帧与当前帧的法线夹角判断。因为当前帧肯定是正面朝向，如果法线夹角不超过一定值则视上一帧也是正面朝向。
<ul>
<li>夹角cos阈值
<code>frontFacing</code>：lerp(cos(STL::Math::DegToRad(135.0)),
cos(STL::Math::DegToRad(91.0)), saturate(2*smbParallaxInPixels-2))</li>
<li>法线夹角采用当前帧/上一帧像素的bilinear区域法线均值：dot(prevNavg,
Navg) &gt; frontFacing;</li>
</ul></li>
</ul></li>
<li><p>在Catmull-Rom filter区域与bilinear
filter区域进行occlusion判断，并生成occlusion
weight。当catmull-rom区域不匹配时，则降为使用occlusion
weight的bilinear区域。Catmull-Rom区域为4x4，中间2x2对应了bilinear区域，定义如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">      Gather      =&gt; CatRom12    =&gt; Bilinear</span></span><br><span class="line"><span class="comment">    0x 0y 1x 1y       0y 1x</span></span><br><span class="line"><span class="comment">    0z 0w 1z 1w    0z 0w 1z 1w       0w 1z</span></span><br><span class="line"><span class="comment">    2x 2y 3x 3y    2x 2y 3x 3y       2y 3x</span></span><br><span class="line"><span class="comment">    2z 2w 3z 3w       2w 3z</span></span><br><span class="line"><span class="comment">     CatRom12     =&gt; Bilinear</span></span><br><span class="line"><span class="comment">       0x 1x</span></span><br><span class="line"><span class="comment">    0y 0z 1y 1z       0z 1y</span></span><br><span class="line"><span class="comment">    2x 2y 3x 3y       2y 3x</span></span><br><span class="line"><span class="comment">       2z 3z</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">CatmullRom</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    float2 origin;</span><br><span class="line">    float2 weights[<span class="number">4</span>];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>通过GatherRed提取上一帧像素smbPixelUv的<code>smbCatromFilter</code>区域的view
z数据。<code>smbCatromGatherUv</code> 为gather 0x的左上角，而 GatherRed
采样得到的是 bilinear 区域的4个像素，顺序如下</p>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230829165239433.png" alt="image-20230829165239433" style="zoom: 67%;"></p>
<p>通过 GatherRed 的以下偏移量取wzxy，正好对应上面 Gather
区域（注意首数字 0123 表明了屏幕坐标轴方向），有</p>
<ul>
<li><p>(1, 1): smbViewZ0 = (0x, 0y, 0z, 0w)。取yzw，prevViewZ0 = (0y,
0z, 0w)</p></li>
<li><p>(3, 1): smbViewZ1 = (1x, 1y, 1z, 1w)。取xzw，prevViewZ1 = (1x,
1z, 1w)</p></li>
<li><p>(1, 3): smbViewZ2 = (2x, 2y, 2z, 2w)。取xyw，prevViewZ2 = (2x,
2y, 2w)</p></li>
<li><p>(3, 3): smbViewZ3 = (3x, 3y, 3z, 3w)。取xyz， prevViewZ3 = (3x,
3y, 3z)</p></li>
</ul></li>
<li><p>比较view
z的差异判断Catmull-Rom区域的occlusion，并生成其中bilinear区域的occlusion
weights，以及bilinear区域的可信度(用于之后的累积速度)。</p>
<p><code>Xprev</code>变换到上一帧的view
space得到<code>Xvprev.z</code>(注意：这是当前帧像素 PixelUv
的着色点在上一帧坐标空间下的坐标)。如果差异绝对值超过
smbDisocclusionThreshold，则认为像素不匹配，取0；否则取1。最后可得到
smbOcclusion0、smbOcclusion1、smbOcclusion2、smbOcclusion3。</p>
<ul>
<li><p>计算bilinear区域的<code>smbOcclusionWeights</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float4 smbOcclusionWeights = GetBilinearCustomWeights(smbBilinearFilter, float4(smbOcclusion0.z, smbOcclusion1.y, smbOcclusion2.y, smbOcclusion3.x))</span><br></pre></td></tr></table></figure></li>
<li><p>Catmull-Rom区域是否匹配，只有匹配时后续才会对历史信息进行
Catmull-Rom filter。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> smbAllowCatRom = dot(smbOcclusion0 + smbOcclusion1 + smbOcclusion2 + smbOcclusion3, <span class="number">1.0</span>) &gt; <span class="number">11.5</span></span><br></pre></td></tr></table></figure></li>
<li><p>bilinear区域的可信度 <code>smbFootprintQuality</code>：对
occlusion 进行 smbBilinearFilter
再开方得到，用以之后调整累积速度（越大越倾向于历史信息）。应对视角变化，对于可信度进行调整。例如相机斜对着表面(NoVPrev&lt;1)变为正对着(NoV=1)，那么历史信息相对当前帧可信度下降</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> sizeQuality = (NoVprev + <span class="number">1e-3</span>) / (NoV + <span class="number">1e-3</span>);</span><br><span class="line">sizeQuality *= sizeQuality;</span><br><span class="line">sizeQuality = lerp(<span class="number">0.1</span>, <span class="number">1.0</span>, saturate(sizeQuality));</span><br><span class="line">smbFootprintQuality *= sizeQuality;</span><br></pre></td></tr></table></figure></li>
</ul></li>
</ul></li>
<li><p>处理材质变化，如果材质ID不匹配，那么视为像素不匹配</p>
<p>上一帧像素的bilinear区域得到的prevMaterialIDs，与当前像素materialID比较，得到
<code>float4 materialCmps</code>(相同为1，不同为0)，bilinear区域的occlusion乘上
materialCmps 后再计算上述occlusion值，得到</p>
<p>smbOcclusionWeightsWithMaterialID、smbAllowCatRomWithMaterialID、smbFootprintQualityWithMaterialID</p></li>
<li><p>上一帧像素 smbPixelUv 的 bilinear 区域
<code>smbBilinearFilter</code></p>
<p>使用 linear 采样法线得到上一帧
<code>prevNavg</code>，再转到当前帧世界坐标（gWorldPrevToWorld，本例为单位阵）</p>
<p>收集 smbBilinearFilter 区域4个像素的
diffAccumSpeeds、specAccumSpeeds、prevMaterialIDs。</p></li>
</ol>
<h2 id="history-fix">3.3 History fix</h2>
<h3 id="preload与数据准备-1">3.3.1 Preload与数据准备</h3>
<p>与 temporal accumulation 阶段相同，将tile (GROUP_X + BORDER * 2) x
(GROUP_Y + BORDER * 2) 加载到
<code>float2 s_FrameNum[BUFFER_Y][BUFFER_X]</code>
中，即累积的帧数(x为diffuse、y为specular)。本阶段的 BORDER 为 2。</p>
<p>提取当前像素数据：<code>N</code>(世界空间法线)、<code>roughness</code>、<code>Xv</code>(view
space坐标)、<code>Nv</code>(view space法线)</p>
<h3 id="平滑累积帧数">3.3.2 平滑累积帧数</h3>
<p>从 preload 数据中获取当前像素的累积帧数
<code>float2 frameNumUnclamped</code>，当前像素在 preload 数据中的位置
<code>int2 smemPos = threadPos + BORDER;</code></p>
<p>再使用 <code>gHistoryFixFrameNum</code> 进行归一化得到
<code>normFrameNum</code>。</p>
<p>在当前像素的 (BORDER * 2 + 1) X (BORDER * 2 + 1)
区域，对归一化累积帧数超过当前像素的样本取平均，得到
<code>normFrameNum</code>。 权重为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 w = step(c, s);	<span class="comment">// c 是当前像素归一化后的累计帧数，s是当前样本归一化后的累积帧数</span></span><br></pre></td></tr></table></figure>
<p>最终得到</p>
<ul>
<li><p>float2 scale = saturate(1.0 - normFrameNum);</p>
<p>gHistoryFixFrameNum
作为累积帧数阈值，scale相当于累积帧数不足的比例，history
fix会对scale超出一定阈值执行。</p></li>
<li><p>float2 frameNum = normFrameNum * gHistoryFixFrameNum;</p></li>
</ul>
<h1 id="diffuse-denoise-pipeline">4 Diffuse Denoise Pipeline</h1>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820112644427.png" alt="image-20230820112644427" style="zoom:67%;"></p>
<h1 id="specular-denoise-pipeline">5 Specular Denoise Pipeline</h1>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820112723742.png" alt="image-20230820112723742" style="zoom:67%;"></p>
<h2 id="pre-blur">5.1 Pre blur</h2>
<h3 id="一些默认参数">5.1.1 一些默认参数</h3>
<ol type="1">
<li>宏定义的默认参数 <code>REBLUR_SPATIAL_MODE == REBLUR_PRE_BLUR</code>
<code>REBLUR_PRE_BLUR_NON_LINEAR_ACCUM_SPEED</code> 1/9
<code>REBLUR_PRE_BLUR_FRACTION_SCALE</code> 2.0</li>
<li>CPU传入的可调参数 gSpecPrepassBlurRadius 默认 50.0 lobeAngleFraction
默认 0.15 roughnessFraction 默认 0.15 resolutionScale 默认 (1.0, 1.0)
gPlaneDistSensitivity 默认 0.005 gMinRectDimMulUnproject :
<code>(float)ml::Min(rectW, rectH) * unproject</code></li>
</ol>
<h3 id="blur策略">5.1.2 blur策略</h3>
<ol type="1">
<li><p>blur平面的选择：这里选用世界空间的blur平面，与 Reflected GGX-D
垂直，可以更多保留特征
<img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820113124323.png" alt="image-20230820113124323" style="zoom:67%;"><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820113146073.png" alt="image-20230820113146073" style="zoom: 50%;"></p></li>
<li><p>blur半径的选择：由下图可以看出，反射物体离着色点越近，特征越明显，越远越模糊；此外越粗糙，specular
lobe夹角越大，反射也会越模糊。因此反射距离越远、着色点越粗糙，对应越大的blur半径。</p>
<p>也就是说，specular
lobe与反射物体形成的锥形底面的直径越大，对应的blur半径越大。
<img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820113322228.png" alt="image-20230820113322228" style="zoom: 55%;"><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820113351881.png" alt="image-20230820113351881" style="zoom:45%;"></p></li>
</ol>
<h3 id="blur-radius计算">5.1.3 blur radius计算</h3>
<ol type="1">
<li><p><code>float hitDist</code> ：基于roughess与viewZ
进行一定缩放，roughness 越小 scale 越大。scale 定义如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> _REBLUR_GetHitDistanceNormalization(<span class="type">float</span> viewZ, float4 hitDistParams, <span class="type">float</span> roughness = <span class="number">1.0</span>)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">return</span> (hitDistParams.x + <span class="built_in">abs</span>(viewZ) * hitDistParams.y) * lerp(<span class="number">1.0</span>, hitDistParams.z, </span><br><span class="line">                                                                   saturate(exp2(hitDistParams.w * roughness * roughness)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中 gHitDistParams 定义如下 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">HitDistanceParameters</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">float</span> A = <span class="number">3.0f</span>; <span class="comment">// 来自 hitDistScale * meterToUnitsMultiplier，默认为 3.0 * 1.0。大概是米到单位值的变换</span></span><br><span class="line">    <span class="type">float</span> B = <span class="number">0.1f</span>; <span class="comment">// (&gt; 0) - viewZ based linear scale (1 m - 10 cm, 10 m - 1 m, 100 m - 10 m)</span></span><br><span class="line">    <span class="type">float</span> C = <span class="number">20.0f</span>; <span class="comment">// (&gt;= 1) - roughness based scale, use values &gt; 1 to get bigger hit distance for low roughness</span></span><br><span class="line">    <span class="type">float</span> D = <span class="number">-25.0f</span>; <span class="comment">// (&lt;= 0) - absolute value should be big enough to collapse &quot;exp2(D * roughness ^ 2)&quot; to &quot;~0&quot; for roughness = 1</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p></li>
<li><p><code>lobeRadius</code> ：反射lobe形成的锥形底面的半径</p>
<ul>
<li><p>Specular lobe的主方向 <code>float4 Dv</code>：(GGX Dominant
Direction, lerp factor) 见附录 <code>NoD</code> 法线与 Dv
的夹角余弦</p></li>
<li><p>计算lobe夹角正切 <code>float lobeTanHalfAngle</code>
：根据roughness估算specular lobe的半角正切 <a href="#%5B1%5D">[1]</a>(page 72)</p></li>
<li><p>估算lobe半径大小</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> lobeRadius = hitDist * NoD * lobeTanHalfAngle;    <span class="comment">// 没看出有什么精确的几何变换，更像是一个近似模型</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>转换为像素单位： 世界空间半径 /
一个像素对应的世界空间大小。像素对应的世界空间大小，与像素的深度有关，即投影到
viewZ 处的截面 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">minBlurRadius = lobeRaidus / PixelRadiusToWorld(gUnproject, gOrthoMode, <span class="number">1.0</span>, viewZ + hitDist * Dv.w);</span><br><span class="line"><span class="comment">// 将屏幕空间以像素为单位的半径投影回视锥体 viewZ 处的截面上的几何半径</span></span><br><span class="line"><span class="type">float</span> <span class="title function_">PixelRadiusToWorld</span><span class="params">(<span class="type">float</span> unproject, <span class="type">float</span> orthoMode, <span class="type">float</span> pixelRadius, <span class="type">float</span> viewZ)</span></span><br><span class="line">&#123;</span><br><span class="line">     <span class="keyword">return</span> pixelRadius * unproject * lerp( viewZ, <span class="number">1.0</span>, <span class="built_in">abs</span>( orthoMode ) );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li><p>gUnproject：1.0f / (0.5f * rectH * project[1]); project[1]
计算的是 <span class="math inline">\(\frac{1}{\tan\alpha_1}\)</span>
<img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230917160953598.png" alt="image-20230917160953598" style="zoom:50%;"><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230917161201304.png" alt="image-20230917161201304" style="zoom:50%;"></p>
<p>视锥体在 viewZ 处的截面高度为 <span class="math inline">\(2 * viewZ *
\tan\alpha_1\)</span>，因此与屏幕高度的比值为 <span class="math display">\[
\begin{align}
&amp; 2 * viewZ * \tan\alpha_1 * \frac{1}{rectH} = 2 * viewZ *
\frac{1}{project[1]} * \frac{1}{rectH} \\
&amp; = viewZ * \frac{1}{project[1] * 0.5 * rectH}
\end{align}
\]</span>
因此屏幕空间像素乘上以上比值，可以投影回viewZ处，得到几何半径。</p></li>
</ul></li>
<li><p><code>blurRadius</code> ：使用一个 hitDistFactor 与 specular
magic curve 缩放输入radius参数。不理解原理 :question: :confused:</p>
<ul>
<li><p><code>float hitDistFactor = hitDist * NoD / frustumSize</code> 并
clamp 到 0~1</p>
<ul>
<li><p><code>float frustumSize</code>：(2 / aspect) *
viewZ，<strong>视锥体在 viewZ 处截面的高度</strong> :confused:
:question:</p>
<p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> <span class="title function_">GetFrustumSize</span><span class="params">(<span class="type">float</span> minRectDimMulUnproject, <span class="type">float</span> orthoMode, <span class="type">float</span> viewZ)</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">return</span> minRectDimMulUnproject * lerp(viewZ, <span class="number">1.0</span>, <span class="built_in">abs</span>(orthoMode));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>gMinRectDimMulUnproject :
<code>(float)ml::Min(rectW, rectH) * unproject</code></p>
<p>前述已经讲过 viewZ * unproject 为视锥体在 viewZ
处的截面高度与屏幕高度的比值，因此 frustumSize 描述的是 viewZ
处截面的高度</p></li>
</ul></li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> blurRadius = gSpecPrepassBlurRadius;</span><br><span class="line">blurRadius *= hitDistFactor * smc;</span><br><span class="line">blurRadius = min( blurRadius, minBlurRadius );</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="权重参数计算">5.1.4 权重参数计算</h3>
<p>一些使用到的默认参数</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> specNonLinearAccumSpeed = REBLUR_PRE_BLUR_NON_LINEAR_ACCUM_SPEED = <span class="number">1</span>/<span class="number">9</span>;</span><br><span class="line">gPlaneDistSensitivity = <span class="number">0.005</span>;	<span class="comment">// 用于调整几何权重影响，越大表示增大几何权重</span></span><br><span class="line">gLobeAngleFraction = <span class="number">0.15</span>;		<span class="comment">// 用于调整法线权重影响，越大表示增大法线权重</span></span><br><span class="line">gRoughnessFraction = <span class="number">0.15</span>;		<span class="comment">// 用于调整粗糙度权重影响，越大表示增大粗糙度权重</span></span><br><span class="line">fractionScale = REBLUR_PRE_BLUR_FRACTION_SCALE = <span class="number">2.0</span>;</span><br></pre></td></tr></table></figure>
<p>这里的各种权重设计都是基于平常见到的权重之上，再增加一个调控参数设计。对于每项权重，实现上先计算好其所需参数，再最后计算得到权重，这样实现更能够利用MAD指令。最后每项权重都会乘上各自的调控参数，而调控参数都与最后的权重呈反比，细节可查看
4.1.2.6 小节。</p>
<ol type="1">
<li><p><code>float2 geometryWeightParams</code>
：用于计算基于平面距离的权重</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 <span class="title function_">GetGeometryWeightParams</span><span class="params">(<span class="type">float</span> planeDistSensitivity, <span class="type">float</span> frustumSize, float3 Xv, float3 Nv, </span></span><br><span class="line"><span class="params">                               <span class="type">float</span> nonLinearAccumSpeed)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">float</span> relaxation = lerp(<span class="number">1.0</span>, <span class="number">0.25</span>, nonLinearAccumSpeed);</span><br><span class="line">    <span class="type">float</span> a = relaxation / (planeDistSensitivity * frustumSize);</span><br><span class="line">    <span class="type">float</span> b = -dot(Nv, Xv) * a;</span><br><span class="line">    <span class="keyword">return</span> float2(a, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a_g &amp;= \frac{lerp(1.0, \space 0.25,\space 1/9)}{g_{sensitivity} *
frustumSize}\\
g_{params} &amp;= \begin{pmatrix}a_g, &amp; -a_g \cdot (N_v \cdot
X_v)\end{pmatrix}
\end{align}
\tag{1}\label{geometry-weight-params}
\]</span></p></li>
<li><p><code>float normalWeightParams</code>
：用于计算基于法线夹角的权重。</p>
<p>从下式可以看出，这里的设计从specular对法线朝向的敏感度出发，specular
lobe angle越小，权重对法线夹角越敏感。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> lobeAngleFractionScale = gLobeAngleFraction * fractionScale;	<span class="comment">// fraction 参数</span></span><br><span class="line"><span class="type">float</span> <span class="title function_">GetNormalWeightParams</span><span class="params">(<span class="type">float</span> nonLinearAccumSpeed, <span class="type">float</span> fraction, <span class="type">float</span> roughness = <span class="number">1.0</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">float</span> angle = STL::ImportanceSampling::GetSpecularLobeHalfAngle(roughness);</span><br><span class="line">    angle *= lerp(saturate(fraction), <span class="number">1.0</span>, nonLinearAccumSpeed); <span class="comment">// <span class="doctag">TODO:</span> use as &quot;percentOfVolume&quot; instead?</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1.0</span> / max(angle, REBLUR_NORMAL_ULP);		<span class="comment">// REBLUR_NORMAL_ULP (2.0 / 255.0)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a_n &amp;= 1.0 / \Big( halfAngle * lerp(g_{lobeAngleF} * 2.0,\space
1.0,\space 1/9) \Big)\\
n_{params} &amp;= a_n
\end{align}
\tag{2}\label{normal-weight-params}
\]</span></p></li>
<li><p><code>float2 hitDistanceWeightParams</code>
：基于反射距离差异的权重</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 <span class="title function_">GetHitDistanceWeightParams</span><span class="params">(<span class="type">float</span> hitDist, <span class="type">float</span> nonLinearAccumSpeed, <span class="type">float</span> roughness = <span class="number">1.0</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// IMPORTANT: since this weight is exponential, 3% can lead to leaks from bright objects in reflections.</span></span><br><span class="line">    <span class="comment">// Even 1% is not enough in some cases, but using a lower value makes things even more fragile</span></span><br><span class="line">    <span class="type">float</span> smc = GetSpecMagicCurve2(roughness);</span><br><span class="line">    <span class="type">float</span> norm = lerp(NRD_EPS, <span class="number">1.0</span>, min(nonLinearAccumSpeed, smc));	<span class="comment">// NRD_EPS 1e-6</span></span><br><span class="line">    <span class="type">float</span> a = <span class="number">1.0</span> / norm;</span><br><span class="line">    <span class="type">float</span> b = hitDist * a;</span><br><span class="line">    <span class="keyword">return</span> float2(a, -b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a_h &amp;= 1.0 / \Big( lerp(1\times 10^{-6}, \space 1.0, \space
\min(1/9, smc) \Big)\\
h_{params} &amp;= \begin{pmatrix} a_h, &amp; -a_h\cdot hitDist
\end{pmatrix}
\end{align}
\tag{3}\label{hit-weight-params}
\]</span></p>
<p>根据 <span class="math inline">\(\eqref{hit-weight}\)</span>
可知，超参 <span class="math inline">\(a_h\)</span>
越大，基于反射距离的权重越小</p></li>
<li><p><code>float2 roughnessWeightParams</code>
：用于计算基于roughness差异的计算</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> roughnessFractionScale = gRoughnessFraction * fractionScale;	<span class="comment">// fraction 参数</span></span><br><span class="line">float2 <span class="title function_">GetRoughnessWeightParams</span><span class="params">(<span class="type">float</span> roughness, <span class="type">float</span> fraction)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">float</span> a = rcp(lerp(<span class="number">0.01</span>, <span class="number">1.0</span>, saturate(roughness * fraction)));</span><br><span class="line">    <span class="type">float</span> b = roughness * a;</span><br><span class="line">    <span class="keyword">return</span> float2(a, -b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
\begin{align}
a_r &amp;= 1.0 / \Big(lerp(0.01, \space 1.0,\space roughness *
g_{roughness} * 2.0)\Big)\\
r_{params} &amp;= \begin{pmatrix} a_r, &amp; -a_r \cdot roughness
\end{pmatrix}
\end{align}  \tag{4}\label{roughness-weight-params}
\]</span></p></li>
</ol>
<h3 id="sampling-space">5.1.5 Sampling Space</h3>
<ol type="1">
<li><p>TB坐标系轴 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2x3 TvBv = GetKernelBasis(Dv.xyz, Nv, NoD, roughness, specNonLinearAccumSpeed);</span><br></pre></td></tr></table></figure></p></li>
<li><p>blurRadius 转为世界空间下的 worldRadius，对TB进行缩放
<code>TvBv[0] *= worldRadius; TvBv[1] *= worldRadius;</code></p></li>
</ol>
<h3 id="poisson-sampling">5.1.6 Poisson Sampling</h3>
<ol type="1">
<li><p>当前样本的 uv 偏移量：泊松样本生成二维向量，再乘上 blurRadius *
texelSize <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 uv = pixelUv + STL::Geometry::RotateVector( rotator, offset.xy ) * gInvScreenSize * blurRadius;</span><br></pre></td></tr></table></figure></p>
<p><code>offset</code>：(x, y) 是单位圆盘内的点坐标，z
是点距圆心的距离</p></li>
<li><p>如果是checkboard模式（gSpecCheckerboard !=
2），当前采样样本是否有有效数据，如果没有则向左或向右偏移一个，表示取相邻像素数据</p></li>
<li><p>采样当前样本的 viewZ 与 spec，以及得到当前样本在view space的坐标
Xvs</p></li>
<li><p>计算当前样本的权重，为下面几项的乘积</p>
<ul>
<li><p>比较当前样本与像素的材质ID，材质ID不同，返回权重 0，相同返回
1。</p></li>
<li><p>gaussian权重：当前样本在单位泊松盘中距圆心的距离
offset.z，代入权重<code>exp(-0.66 * r * r)</code></p></li>
<li><p>使用计算好的权重参数计算 combinded
weight：包括基于平面距离的权重、基于法线夹角的权重、基于粗糙度差异的权重，三项乘积</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> <span class="title function_">GetCombinedWeight</span><span class="params">(</span></span><br><span class="line"><span class="params">    float2 geometryWeightParams, float3 Nv, float3 Xvs,</span></span><br><span class="line"><span class="params">    <span class="type">float</span> normalWeightParams, float3 N, float4 Ns,	<span class="comment">// Ns = (sample normal, sample roughness), N 当前像素的世界空间法线</span></span></span><br><span class="line"><span class="params">    float2 roughnessWeightParams = <span class="number">0</span></span></span><br><span class="line"><span class="params">)</span> &#123;</span><br><span class="line">    float3 a = float3(geometryWeightParams.x, normalWeightParams, roughnessWeightParams.x);</span><br><span class="line">    float3 b = float3(geometryWeightParams.y, <span class="number">0.0</span>, roughnessWeightParams.y);</span><br><span class="line"></span><br><span class="line">    float3 t;</span><br><span class="line">    t.x = dot(Nv, Xvs);</span><br><span class="line">    t.y = STL::Math::AcosApprox(saturate(dot(N, Ns.xyz)));</span><br><span class="line">    t.z = Ns.w;</span><br><span class="line"></span><br><span class="line">    float3 w = _ComputeWeight(t, a, b);</span><br><span class="line">    <span class="keyword">return</span> w.x * w.y * w.z;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// _ComputeWeight</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _ComputeNonExponentialWeight(x, px, py) \</span></span><br><span class="line"><span class="meta">    STL::Math::SmoothStep(0.999, 0.001, abs((x) * (px) + (py)))</span></span><br></pre></td></tr></table></figure>
<p>根据 <span class="math inline">\(\eqref{geometry-weight-params}\)</span>、<span class="math inline">\(\eqref{normal-weight-params}\)</span>、<span class="math inline">\(\eqref{roughness-weight-params}\)</span>
可简化上述代码 <span class="math display">\[
\begin{align}
a &amp;= \begin{pmatrix} a_g, &amp; a_n, &amp; a_r \end{pmatrix} \\
b &amp;= \begin{pmatrix} -a_g \cdot (N_v \cdot X_v), &amp; 0, &amp;
-a_r\cdot roughness \end{pmatrix} \\
t &amp;= \begin{pmatrix} N_v \cdot X_{vs}, &amp; \arccos(N\cdot N_s),
&amp; r_s\end{pmatrix} \\
X = t * a + b &amp;= \begin{pmatrix} a_g \cdot (N_v \cdot X_{vs} -
N_v\cdot X_v), &amp;a_n \cdot \arccos(N\cdot N_s), &amp;a_r\cdot (r_s -
roughness)\end{pmatrix}
\end{align} \tag{5} \label{combined-weight}
\]</span> STL::Math::SmoothStep 的定义如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _SmoothStep01(x) 							(x * x * (3.0 - 2.0 * x))	<span class="comment">// 相比y=x，在0，1两端更平缓</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _LinearStep(a, b, x) 						saturate((x - a) / (b - a))</span></span><br><span class="line">float3 <span class="title function_">SmoothStep01</span><span class="params">(float3 x)</span> 						&#123; <span class="keyword">return</span> _SmoothStep01(saturate(x)); &#125;</span><br><span class="line">float3 <span class="title function_">SmoothStep</span><span class="params">(float3 a, float3 b, float3 x)</span> 	&#123; x = _LinearStep(a, b, x); <span class="keyword">return</span> _SmoothStep01(x); &#125;</span><br></pre></td></tr></table></figure>
<p><span class="math display">\[
x = saturate\left(\frac{x-a}{b-a}\right) \\
w=\_SmoothStep01(x) = x^2 \cdot (3 - 2x)
\]</span></p>
<p>_LinearStep 中 a&lt;b，则得到的是反比关系。因此 <span class="math inline">\(X\)</span> 由 0.001 ~ 0.999 递增，w 由 1 到
0递减。当 <span class="math inline">\(X&lt;=0.001\)</span> 时，w = 1；当
<span class="math inline">\(X &gt;= 0.999\)</span> 时，w = 0</p>
<p>_SmoothStep01 得到的是在0，1两端更平缓的效果，图像如下所示</p>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820201305538.png" alt="image-20230820201305538" style="zoom: 50%;"></p>
<p><span class="math inline">\(\eqref{combined-weight}\)</span>
式计算的每项权重的意义：</p>
<ul>
<li><p><span class="math inline">\(a_g \cdot (N_v \cdot X_{vs} -
N_v\cdot X_v)\)</span>
：小括号里是样本到当前像素平面的距离，基于平面距离的几何权重，距离越小对应权重越大</p></li>
<li><p><span class="math inline">\(a_n \cdot \arccos(N\cdot
N_s)\)</span>：样本法线与当前像素法线的夹角，夹角越小对应权重越大</p></li>
<li><p><span class="math inline">\(a_r\cdot (r_s -
roughness)\)</span>：样本粗糙度与当前像素粗糙度的差值（后面有取绝对值），差值越小对应权重越大</p></li>
</ul>
<p>因此这些权重的设计都是基于平常所见到的设计，但在此之上还有复杂超参设计，即
<span class="math inline">\(a_g,a_n,a_r\)</span></p></li>
<li><p>基于反射距离的权重：使用一个最小权重到1.0之间的插值，<code>lerp(minHitDistWeight, 1.0, ...)</code></p>
<ul>
<li><p>最小权重：<code>float minHitDistWeight</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> minHitDistWeight = REBLUR_HIT_DIST_MIN_WEIGHT * fractionScale;	<span class="comment">// REBLUR_HIT_DIST_MIN_WEIGHT 0.1</span></span><br></pre></td></tr></table></figure></li>
<li><p>基于反射距离差异的权重作为插值权重 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _ComputeExponentialWeight(x, px, py) \</span></span><br><span class="line"><span class="meta">    ExpApprox(-NRD_EXP_WEIGHT_DEFAULT_SCALE * abs((x) * (px) + (py)))	<span class="comment">// NRD_EXP_WEIGHT_DEFAULT_SCALE 3.0</span></span></span><br><span class="line"><span class="type">float</span> <span class="title function_">GetHitDistanceWeight</span><span class="params">(float2 params, <span class="type">float</span> hitDist)</span>	<span class="comment">// 样本的反射距离</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> _ComputeExponentialWeight(hitDist, params.x, params.y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>代入 <span class="math inline">\(\eqref{hit-weight-params}\)</span>
定义的 params，有 <span class="math display">\[
\exp\Big(-3 \cdot abs(s_{hitDist} \cdot a_h - a_h \cdot hitDist)\Big) =
\exp\Big(-3 \cdot abs\big(a_h\cdot (s_{hitDist}-hitDist)\big)\Big)
\tag{6} \label{hit-weight}
\]</span></p></li>
</ul></li>
<li><p>处理样本距反射物距离影响，距离越近，反射越shaper。但为什么roughness越大，权重越大:question::confused:
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> d = length(Xvs - Xv); <span class="comment">// 当前样本到像素的距离</span></span><br><span class="line"><span class="type">float</span> h = ExtractHitDist(s) * hitDistScale; <span class="comment">// roughness weight will handle the rest，当前样本的反射距离</span></span><br><span class="line"><span class="type">float</span> t = h / (hitDist + d);	<span class="comment">// hitDist 为当前像素的反射距离</span></span><br><span class="line">w *= lerp(saturate(t), <span class="number">1.0</span>, STL::Math::LinearStep(<span class="number">0.5</span>, <span class="number">1.0</span>, roughness));</span><br></pre></td></tr></table></figure></p></li>
</ul></li>
<li><p>样本speular的加权平均
如果checkboard模式最终带来无效数据，则使用当前像素的左右相邻像素、进行bilateral
filter</p></li>
</ol>
<h2 id="temporal-accumulation-1">5.2 Temporal Accumulation</h2>
<h3 id="估算沿运动方向的-curvature">5.2.1 估算沿运动方向的
curvature</h3>
<h4 id="着色点的运动方向">5.2.1.1 着色点的运动方向</h4>
<p>将相机的运动转为着色点的运动 Xprev -
gCameraDelta(将上一帧的着色点施加上一帧相机到当前帧的运动)，再变换到当前帧
screen uv空间，得到运动后的着色点的
<code>motionUv</code>。因此，uv空间的运动方向
<code>cameraMotion2d</code> 计算如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">cameraMotion2d = normalize((motionUV-pixelUV) * gRectSize) * gInvRectSize;	<span class="comment">// 转为像素单位标准化，再转为uv单位</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意：这里的 cameraMotion2d
不只是相机的运动矢量，如果着色点具有动画，由于 Xprev
是动画前的世界坐标，因此还有动画带来的运动</p>
</blockquote>
<p>接下来选择两个运动方向上的视差点，基于这两点以及着色点的法线、位置以及观察方向来计算曲率：</p>
<ul>
<li><p>在运动方向上走一个单位得到一个低 parallax 点</p>
<p><code>float2 uv = pixelUv + cameraMotion2d * 0.99;</code></p></li>
<li><p>在运动方向上走前述计算的视差距离个单位</p>
<p><code>float2 uvHigh = pixelUv + cameraMotion2d * smbParallaxInPixels;</code></p></li>
</ul>
<h4 id="low-parallax">5.2.1.2 Low parallax</h4>
<p>像素的bilinear区域以及描述定义如下</p>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230822111025809.png" alt="image-20230822111025809" style="zoom: 67%;"></p>
<p><code>Bilinear f</code>的定义与计算如下，origin 是 bilinear 2x2
区域起始坐标(像素单位)，weights是距 origin 像素中心的偏移量</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Bilinear</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    float2 origin;	<span class="comment">// Bilinar 2x2 区域起点</span></span><br><span class="line">    float2 weights;	<span class="comment">// 插值权重</span></span><br><span class="line">&#125;;</span><br><span class="line">Bilinear <span class="title function_">GetBilinearFilter</span><span class="params">(float2 uv, float2 texSize)</span> &#123;</span><br><span class="line">	float2 t = uv * texSize - <span class="number">0.5</span>;</span><br><span class="line">    Bilinear result;</span><br><span class="line">    result.origin = <span class="built_in">floor</span>(t);</span><br><span class="line">    result.weights = t - result.origin;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol type="1">
<li><p>当前像素的bilinear区域考虑了小的视差，所位于的bilinear区域大多在Preload的数据
<code>s_Normal_MinHitDist</code> 中。存储位置计算如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// threadPos+BORDER为当前像素在tile数据中的位置，int2(f.origin)-pixelPos为bilinear区域相对于当前像素的偏移量</span></span><br><span class="line">int2 pos = threadPos + BORDER + int2(f.origin) - pixelPos;</span><br><span class="line">pos = clamp(pos, <span class="number">0</span>, int2(BUFFER_X, BUFFER_Y) - <span class="number">2</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>Bilinear filter后的法线 <code>n</code>，2x2区域的4个像素的法线为
n00 (pos + (0, 0))、n10 (pos + (1, 0))、n01 (pos + (0, 1))、n11 (pos +
(1, 1))
bilinear计算如下，两个水平方向的插值，再加上一个垂直方向的插值</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">lerp(lerp(s00, s10, f.weights.x), lerp(s01, s11, f.weights.x), f.weights.y);</span><br></pre></td></tr></table></figure></li>
</ol>
<h4 id="high-parallax">5.2.1.3 High parallax</h4>
<p>与 low parallax 计算normal不同的是 uvHigh
的视差较大，因此其bilinear区域大多不在preload
tile数据内，从贴图中采样并执行 bilinear filter，得到
<code>nHigh</code>。</p>
<p>同时bilinear filter uvHigh 的 view
space深度得到<code>zHigh</code>。计算 zHigh 与当前着色点 viewZ
之间的相对误差</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> zError = <span class="built_in">abs</span>(zHigh - viewZ) * rcp(max(zHigh, viewZ));</span><br></pre></td></tr></table></figure>
<p>如果相对误差 <code>zError</code> &lt; 0.1，则选择high parallax的
uvHigh 与 nHigh；否则，选择 low parallax 的 uv 与 n。</p>
<h4 id="计算-curvature">5.2.1.4 计算 curvature</h4>
<p>使用当前像素的
<code>X</code>(世界坐标)、<code>N</code>(法线)、<code>Navg</code>
以及所选parallax 点的
<code>v</code>(世界空间观察方向)、<code>n</code>(世界空间法线)计算这两点曲率
:question:</p>
<h3 id="累积速度更新">5.2.5 累积速度更新</h3>
<p>本例中 gSpecMaterialMask = 0，因此 <code>specOcclusionWeights</code>
采用前面计算的不带材质比较的
smbOcclusionWeights；<code>specHistoryConfidence</code> 采用
smbFootprintQuality，即<code>smbPixelUv</code>的bilinear区域的可信度。</p>
<p>历史帧<code>smbPixelUv</code>的bilinear区域的累积速度
specAccumSpeeds，使用specOcclusionWeights得到加权平均<code>specAccumSpeed</code>。根据bilinear区域的可信度调整历史信息的累积帧数：如果
confidence = 1，累积帧数不变；如果confidence &lt;
1，累积帧数减小，则混合权重更倾向于当前帧。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// specAccumSpeed *= ((specAccumSpeed * confidence + 1) / (1 + specAccumSpeed));</span></span><br><span class="line">specAccumSpeed *= lerp(specHistoryConfidence, <span class="number">1.0</span>, <span class="number">1.0</span> / ( <span class="number">1.0</span> + specAccumSpeed));	<span class="comment">// +1 避免除 0 </span></span><br><span class="line">specAccumSpeed = min(specAccumSpeed, gMaxAccumulatedFrameNum);	<span class="comment">// fast history下 gMaxAccumulatedFrameNum = 5，否则 30</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>注意：累积速度在保存时除以最大累积帧数
<code>REBLUR_MAX_ACCUM_FRAME_NUM</code>(63)，在提取时再乘上最大累积帧数。因此计算过程中，表示的是历史帧信息的累积帧数</p>
</blockquote>
<h3 id="virtual-motion-based-reprojection">5.2.6 Virtual motion based
reprojection</h3>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230903135344354.png" alt="image-20230903135344354" style="zoom:50%;"></p>
<p>对于反射的重投影，反射的世界具有自己的运动，例如反射点具有动画，而着色点与相机是静止的，这时着色点的反射也发生了运动。而反射点的运动常常使用虚拟反射点来追踪，如上图所示的镜面反射的虚拟反射点可以通过在着色点处，沿着相机到着色点的方向延长反射距离得到，计算如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float3 Xvirtual = X - V * hitDist;</span><br><span class="line">float2 pixelUvVirtualPrev = GetScreenUv(gWorldToClipPrev, Xvirtual);</span><br></pre></td></tr></table></figure>
<p>但这种方法只对镜面反射有效，而实际的glossy反射，虚拟反射点会更加接近表面。</p>
<ol type="1">
<li><p>计算虚拟反射点 <code>Xvirtual</code></p>
<ul>
<li><p>对preload阶段得到的当前像素2x2区域最小反射距离进行一定scale
<code>hitDistForTracking *= hitDistScale</code></p></li>
<li><p>计算着色点处的GGX Dominant Direction <code>float4 D</code>，D.w
是 n 到 r(镜面反射方向) 的插值，roughness增大会导致specular
lobe主方向偏向法线。</p></li>
<li><p>虚拟反射点计算如下:question:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> <span class="title function_">ApplyThinLensEquation</span><span class="params">( <span class="type">float</span> NoV, <span class="type">float</span> hitDist, <span class="type">float</span> curvature )</span></span><br><span class="line">&#123; <span class="comment">// https://www.geeksforgeeks.org/sign-convention-for-spherical-mirrors/</span></span><br><span class="line">    <span class="type">float</span> hitDistFocused = hitDist / (<span class="number">2.0</span> * curvature * hitDist * NoV + <span class="number">1.0</span>);</span><br><span class="line">    <span class="keyword">return</span> hitDistFocused;</span><br><span class="line">&#125;</span><br><span class="line">float3 <span class="title function_">GetXvirtual</span><span class="params">(<span class="type">float</span> NoV, <span class="type">float</span> hitDist, <span class="type">float</span> curvature, float3 X, float3 Xprev, float3 V, <span class="type">float</span> dominantFactor)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">float</span> hitDistFocused = ApplyThinLensEquation(NoV, hitDist, curvature);</span><br><span class="line">    <span class="type">float</span> closenessToSurface = saturate(<span class="built_in">abs</span>(hitDistFocused) / (hitDist + NRD_EPS));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> lerp(Xprev, X, closenessToSurface * dominantFactor) - V * hitDistFocused * dominantFactor;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Xvritual 变换到上一帧screen uv空间，得到
<code>vmbPixelUv</code></p></li>
<li><p>使用虚拟反射点与着色点在上一帧的像素距离
<code>vmbPixelsTraveled</code> 表达virtual motion</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float2 vmbDelta = vmbPixelUv - smbPixelUv;</span><br><span class="line"><span class="type">float</span> vmbPixelsTraveled = length(vmbDelta * gRectSize);</span><br></pre></td></tr></table></figure></li>
<li><p>根据virtual
motion对curvature进行调整，再重新计算上述虚拟反射点相关变量，例如
Xvirtual、vmbPixelUv、vmbDelta、vmbPixelsTraveled。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> curvatureCorrection = <span class="type">float</span>(vmbPixelsTraveled &lt; <span class="number">3.0</span> * smbParallaxInPixels + gInvRectSize.x);</span><br><span class="line">curvature *= curvatureCorrection;</span><br></pre></td></tr></table></figure></li>
</ul></li>
<li><p>occlusion判断：虚拟反射点在上一帧的vmbPixelUv，对应bilinear区域<code>vmbBilinearFilter</code>
，计算该区域的occlusion。</p>
<ul>
<li><p>获取上一帧数据：</p>
<ul>
<li><code>vmbViewZs</code>：vmbBilinearFilter区域的view z</li>
<li><code>vmbVv</code>：vmbPixelUv像素取view z = 1得到的view
space坐标，指向vmbPixelUv像素并且z=1的view space下的向量</li>
<li><code>Nvprev</code>：当前帧像素的 <code>Navg</code>转到上一帧view
space</li>
</ul></li>
<li><p>使用到着色点平面的距离的差异来评估occlusion</p>
<ul>
<li><p>上一帧bilinear区域到着色点平面的距离计算如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">float4 NoX = (Nvprev.x * vmbVv.x + Nvprev.y * vmbVv.y) * (gOrthoMode == <span class="number">0</span> ? vmbViewZs : gOrthoMode) + Nvprev.z * vmbVv.z * vmbViewZs;</span><br><span class="line"><span class="comment">// gOrthoMode = 0</span></span><br><span class="line">NoX = vmbViewZs * dot(Nvprev, vmbVv);</span><br></pre></td></tr></table></figure>
<p>由于 vmbVv 是 vmbPixelUv像素在 view z = 1 的点，通过变换可以得到
vmbViewZs * vmbVv
则是bilinear区域的四个点（这里忽略了像素点的不同）。因此NoX计算的是bilinear区域4个点到着色点平面法线的距离（这里的平面方程常数项为0）</p></li>
<li><p>着色点在上一帧的平面方程常数项：<code>float NoXreal = dot(Navg, X - gCameraDelta);</code></p>
<p>由于 Navg 位于世界空间，因此到上一帧世界空间不需要变换</p>
<p>X - gCameraDelta：着色点世界坐标变换到上一帧的世界空间。</p>
<ul>
<li><p>注意，正常情况下是不需要此变换的，因为世界空间是绝对的，但由于前面所述，这里的变换都取消掉了当前帧的相机平移，也就是以当前帧相机为原点。</p></li>
<li><p>简单推导：gCameraDelta是指向上一帧的相机运动向量，X是当前帧着色点的世界坐标，又是(着色点-&gt;相机)的向量。因此
X-gCameraDelta为(上一帧相机-&gt;着色点)的向量。</p></li>
<li><p>dot(Navg, x-gCameraDelta) = ||Navg|| * ||x-gCameraDelta|| * cos =
||x-gCameraDelta|| * cos</p>
<p>相当于将 Navg与X转到上一帧的view space 下，再求平面方程常数项。保持与
NoX 的坐标空间一致。</p></li>
</ul></li>
<li><p><code>float4 vmbPlaneDist = abs(NoX-NoXreal)</code>：计算的是虚拟反射点在上一帧对应的bilinear区域到着色点平面的距离。</p></li>
</ul></li>
<li><p><code>float4 vmbOcclusion = step(vmbPlaneDist, disocclusionThresholdMulFrustumSize);</code></p>
<p>之后就和surface motion计算类似</p>
<ul>
<li><p><code>float4 vmbOcclusionWeights</code>：bilinear权重施加vmbOcclusion
得到</p></li>
<li><p>是否可以使用 catrom filter：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> vmbAllowCatRom = dot( vmbOcclusion, <span class="number">1.0</span> ) &gt; <span class="number">3.5</span> &amp;&amp; REBLUR_USE_CATROM_FOR_VIRTUAL_MOTION_IN_TA;</span><br><span class="line">vmbAllowCatRom = vmbAllowCatRom &amp;&amp; specAllowCatRom;</span><br></pre></td></tr></table></figure></li>
<li><p>bilinear可信度 <code>vmbFootprintQuality</code>： 对 vmbOcclusion
执行 bilinear filter 再开方得到</p></li>
</ul></li>
</ul></li>
</ol>
<h4 id="累积速度更新-1">5.2.6.1 累积速度更新</h4>
<p>使用 vmbBilinearGatherUv 获取上一帧 vmbBilinearFilter
区域的累积速度，使用vmbOcclusionWeights加权平均得到
<code>vmbSpecAccumSpeed</code>，再使用 vmbBilinearFilter
区域的可信度调整累积速度</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vmbSpecAccumSpeed *= ((vmbSpecAccumSpeed * confidence + 1) / (1 + vmbSpecAccumSpeed));</span></span><br><span class="line">vmbSpecAccumSpeed *= lerp(vmbFootprintQuality, <span class="number">1.0</span>, <span class="number">1.0</span> / (<span class="number">1.0</span> + vmbSpecAccumSpeed));</span><br></pre></td></tr></table></figure>
<p>与surface motion不同的是，最大帧数进行了如下限制，本例中
<code>gResponsiveAccumulationRoughnessThreshold=0</code>，因此无作用</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> <span class="title function_">GetResponsiveAccumulationAmount</span><span class="params">(<span class="type">float</span> roughness)</span>	</span><br><span class="line">&#123;	<span class="comment">// 当 roughness 大于阈值时为0，roughness越小，返回值越大。但 gResponsiveAccumulationRoughnessThreshold 为 0，返回值恒为0</span></span><br><span class="line">    <span class="type">float</span> amount = <span class="number">1.0</span> - (roughness + NRD_EPS) / (gResponsiveAccumulationRoughnessThreshold + NRD_EPS);</span><br><span class="line">    <span class="keyword">return</span> STL::Math::SmoothStep01(amount);</span><br><span class="line">&#125;</span><br><span class="line"><span class="type">float</span> responsiveAccumulationAmount = GetResponsiveAccumulationAmount(roughness);</span><br><span class="line">responsiveAccumulationAmount = lerp(<span class="number">1.0</span>, GetSpecMagicCurve(roughness), responsiveAccumulationAmount);</span><br><span class="line"></span><br><span class="line"><span class="type">float</span> vmbMaxFrameNum = gMaxAccumulatedFrameNum * responsiveAccumulationAmount;</span><br><span class="line">vmbSpecAccumSpeed = min(vmbSpecAccumSpeed, vmbMaxFrameNum);</span><br></pre></td></tr></table></figure>
<h4 id="surface-与-virtual-之间的混合权重">5.2.6.2 Surface 与 Virtual
之间的混合权重</h4>
<p><code>virtualHistoryAmount</code>，用于混合 virtual-motion based 与
surface-motion based重投影。</p>
<ul>
<li><p>初始为 GGX-D 的 lerpFactor</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> virtualHistoryAmount = IsInScreen(vmbPixelUv) * D.w;</span><br></pre></td></tr></table></figure></li>
<li><p>virtual motion的bilinear区域可信度
<code>vmbFootprintQuality</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= saturate(vmbFootprintQuality / <span class="number">0.5</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>normal：根据着色点法线
<code>N</code>与虚拟反射点法线<code>vmbN</code>、virtual
motion走过的弧度 <code>angle</code> 得到
<code>virtualHistoryNormalBasedConfidence</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= lerp(<span class="number">1.0</span> - saturate(vmbPixelsTraveled), <span class="number">1.0</span>, virtualHistoryNormalBasedConfidence)</span><br></pre></td></tr></table></figure></li>
<li><p>back-facing: 虚拟反射点法线 vmbN 与着色点的平均法线 Navg。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= <span class="type">float</span>(dot(vmbN, Navg) &gt; <span class="number">0.0</span>)</span><br></pre></td></tr></table></figure></li>
<li><p>roughness: <code>virtualHistoryRoughnessBasedConfidence</code>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= lerp(<span class="number">1.0</span> - saturate(vmbPixelsTraveled), <span class="number">1.0</span>, virtualHistoryRoughnessBasedConfidence)</span><br></pre></td></tr></table></figure></p></li>
<li><p>在virtual motion方向上的少量像素累积roughness权重
<code>wr</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= <span class="number">0.1</span> + wr * <span class="number">0.9</span>;</span><br><span class="line">virtualHistoryRoughnessBasedConfidence *= wr;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="混合">5.2.7 混合</h3>
<h4 id="历史数据filter">5.2.7.1 历史数据filter</h4>
<p>当 <code>specAllowCatRom == true</code>时，使用 catrom
filter；否则使用 specOcclusionWeights 进行bilinear
filter，得到着色点在上一帧的specular历史filter结果
<code>smbSpecHistory, smbSpecFastHistory</code>。</p>
<p>同理，当 <code>vmbAllowCatRom == true</code>时，使用 catrom
filter；否则使用 vmbOcclusionWeights 进行bilinear
filter，得到虚拟反射点在上一帧的 specular
历史filter结果<code>vmbSpecHistory, vmbSpecFastHistory</code></p>
<p>SpecHistory 是 float4(radiance, dist)，SpecFastHistory 是
float2(luma, hitDistForTrackingPrev)</p>
<h4 id="virtual-history-confidence">5.2.7.2 virtual history
confidence</h4>
<p>用于历史radiance clamp，以及控制累积速度</p>
<ul>
<li><p>virtual parallax difference：使用 SpecFastHistory 的 hitDist
得到虚拟反射点 <code>XvirtualPrev</code>，变换到上一帧的sreen uv空间得到
<code>vmbPixelUvPrev</code>。得到与 vmbPixelUv的像素距离
<code>deltaParallaxInPixels</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lobeRadiusInPixels 着色点处反射lobe半径</span></span><br><span class="line"><span class="type">float</span> virtualHistoryConfidence = STL::Math::SmoothStep(lobeRadiusInPixels + <span class="number">0.25</span>, <span class="number">0.0</span>, deltaParallaxInPixels);</span><br></pre></td></tr></table></figure></li>
<li><p>在virtual motion方向上的少量像素累积roughness权重
<code>wr</code>、normal权重 <code>w1</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryConfidence *= isInScreen ? w1 : <span class="number">1.0</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="累积速度与混合权重更新">5.2.7.3 累积速度与混合权重更新</h4>
<p><code>vmbSpecAccumSpeed *= virtualHistoryConfidence;</code></p>
<p>surface motion based：<code>smbSpecAccumSpeed</code></p>
<p>由 virtualHistoryAmount 混合得到最终的
<code>specAccumSpeed</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">virtualHistoryAmount *= saturate(vmbSpecAccumSpeed / (smbSpecAccumSpeed + NRD_EPS)); <span class="comment">// gNonReferenceAccumulation = 1</span></span><br><span class="line">specAccumSpeed = lerp( smbSpecAccumSpeed, vmbSpecAccumSpeed, virtualHistoryAmount );</span><br></pre></td></tr></table></figure>
<h4 id="specular-混合">5.2.7.4 Specular 混合</h4>
<ul>
<li><p>混合速度选用
<code>float specNonLinearAccumSpeed = 1.0 / (1.0 + specAccumSpeed);</code></p>
<p>当checkboard模式下，当前无有效数据时，<code>specNonLinearAccumSpeed *= lerp(1.0 - gCheckerboardResolveAccumSpeed, 1.0, specNonLinearAccumSpeed);</code></p></li>
<li><p>混合surface motion 与 virtual
motion的历史specular：<code>specHistory = lerp(smbSpecHistory, vmbSpecHistory, virtualHistoryAmount);</code></p></li>
<li><p>混合历史specular与当前specular 得到最终累积结果
<code>specResult</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">specResult = MixHistoryAndCurrent(specHistory, spec, specNonLinearAccumSpeed, roughnessModified);</span><br><span class="line">float4 <span class="title function_">MixHistoryAndCurrent</span><span class="params">(float4 history, float4 current, <span class="type">float</span> f, <span class="type">float</span> roughness = <span class="number">1.0</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    float4 r;</span><br><span class="line">    r.xyz = lerp(history.xyz, current.xyz, f);</span><br><span class="line">    r.w = lerp(history.w, current.w, max(f, GetMinAllowedLimitForHitDistNonLinearAccumSpeed(roughness)));</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>specular混合结果的 hit dist 与 lum矫正（Anti-firefly
suppressor）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// REBLUR_FIREFLY_SUPPRESSOR_RADIUS_SCALE 0.1, gBlurRadius 15</span><br><span class="line">float specAntifireflyFactor = specAccumSpeed * gBlurRadius * REBLUR_FIREFLY_SUPPRESSOR_RADIUS_SCALE * smc;</span><br><span class="line">specAntifireflyFactor /= 1.0 + specAntifireflyFactor;</span><br></pre></td></tr></table></figure>
<p>virtual motion 与 surface motion 的混合结果 与
最终时序累积结果之间的混合</p></li>
<li><p>fast history混合</p></li>
<li><p>计算误差
GetColorErrorForAdaptiveRadiusScale，之后blur基于此调整blur
radius</p></li>
</ul>
<h2 id="history-fix-1">5.3 History Fix</h2>
<h3 id="preload">5.3.1 Preload</h3>
<p>从 fast history 加载 lum 到
<code>float s_SpecLuma[BUFFER_Y][BUFFER_X]</code></p>
<h3 id="history-reconstruction">5.3.2 History Reconstruction</h3>
<p>当累积帧数相对于 <code>gHistoryFixFrameNum</code> 小于
(1-REBLUR_HISTORY_FIX_THRESHOLD_1) 比例时 (0.111)，执行 history
reconstruction。</p>
<ol type="1">
<li><p>采样步长
<code>scale.y</code>（像素单位）：与前面计算相同，得到specular lobe的
<code>lobeRadius</code>(世界空间)，再转到屏幕空间得到
<code>minBlurRadius</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// gHistoryFixStrideBetweenSamples = 14.0，frameNum 是 gHistoryFixFrameNum 范围内的平滑帧数</span></span><br><span class="line">scale.y = min(gHistoryFixStrideBetweenSamples / (<span class="number">2.0</span> + frameNum.y), minBlurRadius / <span class="number">2.0</span>);</span><br></pre></td></tr></table></figure></li>
<li><p>权重参数：normal、geometry、roughness 与之前计算相同</p></li>
<li><p>在 [-2, 2] x [-2, 2] 区域进行加权平均specular得到
<code>spec</code>，</p>
<ul>
<li>每个样本为
<code>float2 uv = pixelUv + float2(i, j) * gInvRectSize * scale.y;</code></li>
<li>使用权重参数计算每个样本的权重</li>
</ul></li>
<li><p>在以当前像素为中心的 (BORDER * 2 + 1) x (BORDER * 2 + 1) 区域计算
luma 一阶矩<code>specM1</code>、二阶矩<code>specM2</code>。</p>
<p>每个样本的luma数据都在shared data <code>s_SpecLuma</code> 中</p></li>
<li><p>如果开启了 antiFirefly ，则在 [-4, 4] x [-4, 4] 区域计算 luma
的一阶矩 m1、二阶矩 m2，限制filter结果spec的luma
<code>specLuma</code></p>
<p><code>clamp(specLuma, m1-sigma, m1+sigma)</code>，sigma为标准差</p></li>
<li><p>使用 specM1、specM2以及当前像素的lum specCenter 对 specLuma 进行
clamp 得到 <code>specLumaClamped</code></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> specMin = min(specM1 - specSigma, specCenter);</span><br><span class="line"><span class="type">float</span> specMax = max(specM1 + specSigma, specCenter);</span><br><span class="line"><span class="type">float</span> specLumaClamped = clamp(specLuma, specMin, specMax);</span><br></pre></td></tr></table></figure></li>
<li><p>specLumaClamped 到 specLuma 之间的插值得到最终的
specLuma，插值权重由累积帧数决定</p></li>
</ol>
<p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">specLuma = lerp(specLumaClamped, specLuma, </span><br><span class="line">                <span class="number">1.0</span> / (<span class="number">1.0</span> + <span class="type">float</span>(gMaxFastAccumulatedFrameNum &lt; gMaxAccumulatedFrameNum) * frameNumUnclamped.y));</span><br></pre></td></tr></table></figure></p>
<p>对filter结果ChangeLuma得到最终输出
<code>spec = ChangeLuma(spec, specLuma);</code></p>
<h1 id="appendix">Appendix</h1>
<h2 id="ggx-dominant-direction">1 GGX Dominant Direction</h2>
<p>Off-Specular 现象：通常都认为 BRDF lobe
是以镜面反射方向为中心，但由于光源方向与 shadow-masking 项，在 roughness
增大时，BRDF lobe 会朝着法线方向偏移，称为 Off-specular peak。如下图所示
<a href="#%5B1%5D">[1]</a>(page 69)</p>
<p><img src="/images/Rendering Blogs/Graphics Algorithm/2. Reblur解析.assets/image-20230820113637853.png" alt="image-20230820113637853" style="zoom: 80%;"></p>
<p>为了模拟这种变化，引入一个参数 lerpFactor
来得到法线到镜面反射方向之间的dominant direction，即</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">lerp(N, R, lerpFactor);</span><br></pre></td></tr></table></figure>
<p>这个 lerpFactor 使用 roughness、NoV来建模。在reblur的实现中，
<code>float4 GetSpecularDominantDirection</code> 返回 (dominant
direction, lerpFactor)</p>
<h1 id="reference">Reference</h1>
<p><a name="[1]">[1]</a>
https://seblagarde.files.wordpress.com/2015/07/course_notes_moving_frostbite_to_pbr_v32.pdf</p>
]]></content>
      <categories>
        <category>Rendering Blogs</category>
        <category>Graphics Alogrithm</category>
      </categories>
  </entry>
</search>
